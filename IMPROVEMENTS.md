# IMPROVEMENTS.md — KSCW `wiedisync` improvement backlog

Generated 2026-07-02 from a 9-reader inventory of `src/` (212 units). **311 improvements**: 13 high · 118 medium · 180 low. **209 safe** (internal-only, auto-applyable) · **102 gated** 🔒 (alter user-facing behavior / schema / permissions — need sign-off).

**Legend:** `[ ]` todo · category in _italics_ · 🔒 = gated (Phase 2 pauses to ask) · ✅ = safe.

**Phase 2 order:** top→bottom. Within each impact tier, safe items come first (auto-applied, build-verified, committed `improve: <item>`), then gated items (paused for approval).

## High impact (13)

### ✅ Safe (7)

- [x] ✅ `src/components/FormField.tsx` — _(done: added `htmlFor` prop, threaded `inputId` from FormInput/FormTextarea)_ `FormField` renders `<Label className="mb-2">{label}</Label>` with no `htmlFor`, while `FormInput`/`FormTextarea` compute an `inputId` (from `id` or a slugified label) and set it on the control — so the label is never actually associated with its input. Clicking the label doesn't focus the field and AT reads label and control as unrelated. Thread the id into `FormField` and set `htmlFor`. _(accessibility)_
- [x] ✅ `src/hooks/teamAbsencesFetch.ts` — _(done: replaced per-team fetchItem loop with one batched fetchAllItems `id _in`)_ N+1 sequential waterfall: the coach/TR expansion loops `for (const teamId of validTeamIds) { await fetchItem('teams', teamId, {fields:[...,'coach.members_id','team_responsible.members_id']}) }` — one awaited round-trip per team. For the 'all teams' export this is N serial requests. Replace with a single `fetchAllItems('teams', { filter: { id: { _in: validTeamIds } }, fields: [...] })` and build teamRef/coach/TR sets from the batch; the correct `.members_id` expand is preserved. _(efficiency)_
- [ ] ✅ `src/hooks/useAuth.tsx` — `loadTeamContext` runs 6 collections in a single `Promise.all`; one failing query rejects the whole batch, the catch sets `teamsReady(true)` with ALL scope arrays empty, so a transient failure on any one collection makes the user look like they have no teams/roles (the documented Promise.all-fails-all footgun). Switch to `Promise.allSettled` and populate per-collection so a single failure degrades gracefully instead of wiping every role. _(error-handling)_
- [x] ✅ `src/modules/calendar/ICalModal.tsx` — _(done: extract team id via relId before comparing)_ In download mode the team filter reads `const teamId = (src.kscw_team ?? src.team ?? '') as string` (line 152), but games in the calendar have `kscw_team` EXPANDED to an object (useCalendarData fetches `kscw_team.*`). So `selectedTeamIds.includes(teamId)` compares against an object and never matches → applying a team filter drops ALL games from the downloaded .ics. Extract the id via relId() before comparing. _(error-handling)_
- [ ] ✅ `src/modules/gameScheduling/pages/AdminDashboardPage.tsx` — Heavy derived data is computed inline in the render body without useMemo: slotByIdAll (line 513), the awayMismatches/awayUnbooked/homeVmAlerts IIFEs (each rebuilding oppById/teamNameById/bookingById Maps, lines 637-690), summary reduce, and opponentSearchText. Every search keystroke (search state) re-runs all of it. Wrap these in useMemo keyed on their real deps (slots/opponents/bookings/checks) so typing in the search box doesn't rebuild every Map. _(efficiency)_
- [ ] ✅ `src/modules/messaging/hooks/useReactions.ts` — N+1 fetch/subscription: ReactionBar calls useReactions(messageId) once per MessageBubble, so a 50-message thread fires 50 separate fetchAllItems('message_reactions') requests AND opens 50 useRealtime subscriptions on the same collection. Lift reaction loading to the conversation level (one fetch filtered by message _in [...] passed down, or a single realtime subscription in useConversation) instead of per-message. _(efficiency)_
- [x] ✅ `src/modules/news/NewsArchivePage.tsx` — _(done: `hours / 60 / 24` → `hours / 24`)_ NotificationTableRow's relative-time math is wrong: `const days = Math.floor(hours / 60 / 24)` (line 211) divides hours by 1440, so anything from 1 day to ~2 months old renders as '0 days ago'. HomePage's NewsRow does it correctly as `Math.floor(hours / 24)`. Fix to `hours / 24`. _(error-handling)_

### 🔒 Gated — need approval (6)

- [ ] 🔒 `src/components/AttendanceTable.tsx` — This is a homogeneous data-record list but the desktop view is a raw `<table>` (not the shadcn `<Table>` from src/components/ui/table.tsx) and the mobile view is a `divide-y` stack of `<button>` rows — exactly the card/space-y row-list the 'Lists → tables, always' convention forbids. Migrate to `<Table>` with the documented mobile compaction rules. _(consistency · GATED: Rewriting the markup to <Table> changes the rendered DOM/visual layout on both desktop and mobile.)_
- [ ] 🔒 `src/components/ui/SearchableSelect.tsx` — Options are rendered as `<li onClick>` with `role="option"` but are not keyboard-focusable and there is no arrow-key handling; the only keyboard path is Enter when exactly one item is filtered. `role="combobox"` is set on the trigger but without `aria-controls`/`aria-activedescendant`, and the `<Label>` has no `htmlFor` linking it to the control. Keyboard and screen-reader users effectively cannot operate this control — a real WCAG operability gap for a widely-used form input. _(accessibility · GATED: Adds keyboard navigation behavior that changes how the component responds to input.)_
- [ ] 🔒 `src/hooks/useFinance.ts` — `useMyPayouts` calls `fetchAllItems('finance_payouts', { fields, sort })` with NO member filter, relying entirely on the read policy to scope to the current user. `useMyInvoices` deliberately uses a dedicated server endpoint for exactly this reason. For a finance/board user (whose policy is NOT scoped to own) this 'My finances' hook returns EVERY member's payouts as if they were the viewer's own. Add an explicit `filter: { member: { _eq: user.id } }` (matching the queryKey it already carries) rather than trusting the policy. _(security · GATED: Changes what finance/board users see on the My-finances page (payout scoping))_
- [ ] 🔒 `src/modules/admin/components/TableBrowser.tsx` — Pagination is broken: `total = records.length` uses only the current page's rows (limited to PER_PAGE=25), so `totalPages = ceil(total/25)` is always 1 and the Next button (disabled when page>=totalPages) never enables — the admin can never browse past the first 25 records. Fetch a real count (countItems) or a total from the query meta. _(efficiency · GATED: Enables paging to further records — changes what the admin can view.)_
- [ ] 🔒 `src/modules/carpool/hooks/useCarpool.ts` — carpools and carpool_passengers are fetched with no `fields` param, so `driver` and `passenger` come back as bare ID strings (types confirm both are `string`). CarpoolCard then calls asObj(carpool.driver)/asObj(p.passenger) which returns null for a scalar → the card shows the generic label 'Driver' and passenger chips render '...'. Expand `driver.id/first_name/last_name` and `passenger.id/first_name/last_name` so names actually display. _(error-handling · GATED: Adds member-name (PII) expansion to the query and changes what's displayed.)_
- [ ] 🔒 `src/modules/feedback/FeedbackPage.tsx` — The form lets users attach up to 5 screenshots (UI shows a '(n/5)' counter and multi-file list), but handleSubmit only uploads and saves `files[0]` (lines 158-171) — the other 4 are silently discarded. The `screenshot: string | string[]` record field implies multi was intended. Upload all selected files (or drop the multi-select UI). _(error-handling · GATED: Changes what data is persisted per submission (user-facing behavior).)_

## Medium impact (118)

### ✅ Safe (70)

- [ ] ✅ `src/components/CategoryMultiSelect.tsx` — The custom dropdown has no combobox/listbox ARIA: the trigger lacks `aria-expanded`/`aria-haspopup`, the popup has no `role="listbox"`, option rows no `role="option"`/`aria-selected`, and the fake `Checkbox` conveys checked/indeterminate purely visually. Add the ARIA roles/state so it's usable with a screen reader. _(accessibility)_
- [ ] ✅ `src/components/ConfirmProvider.tsx` — The prompt `<input>` has no associated `<label>` and no `aria-label`/`aria-describedby`, so AT announces it as an unlabeled textbox. Wire it to the existing `DialogDescription` message (via `aria-describedby` or `aria-label`) — no new copy needed. _(accessibility)_
- [ ] ✅ `src/components/LocationCombobox.tsx` — The input has no combobox ARIA (`role="combobox"`, `aria-expanded`, `aria-controls`, `aria-autocomplete`) and the result rows are plain buttons with no `role="option"`, so the suggestion list is invisible to AT and there's no arrow-key navigation. Add the combobox/option roles (keyboard arrow-nav would be a follow-up). _(accessibility)_
- [ ] ✅ `src/components/MoreSheet.tsx` — The admin section and (especially) the superadmin section hand-write the same ~8-line `<NavLink>` + active-className block per item — data-health, infra, audit-log, sql, bugfixes are five near-identical copies. Hoist them into arrays like the existing `adminItems` and render via the existing `renderItem`/a shared item component to cut the duplication. _(consistency)_
- [ ] ✅ `src/components/NotificationPanel.tsx` — The overlay behaves like a modal but has no role="dialog"/aria-modal, no focus trap, and no Escape-to-close keydown handler (only backdrop click + swipe). Keyboard users can't dismiss it and focus is not managed. _(accessibility)_
- [ ] ✅ `src/components/ParticipationButton.tsx` — The RSVP popup menu (menuOpen block) is a plain div with a click-catcher overlay — no role="menu"/menuitem, no Escape handling, and no arrow-key navigation, so it's mouse-only. The guest +/- buttons (lines 375-390) are also icon-only with no aria-label. _(accessibility)_
- [ ] ✅ `src/components/ParticipationRosterModal.tsx` — Per member row the render does repeated linear scans over `participations` — getMemberStatus(), getMemberAbsenceReason(), the inline `participations.find(p => p.member === member.id)` (lines 1354, 1467, 1479), attribution — making the list O(members × participations). Build a Map<memberId, Participation> once (byMember dedupe already exists for playerParticipations) and reuse it. _(efficiency)_
- [ ] ✅ `src/components/ParticipationSummary.tsx` — When `prefetched` isn't passed, each instance runs its own useCollection('participations') fetch AND a useRealtime subscription (lines 42-52). Rendered once per card in a list this is an N+1 fetch + N websocket subscriptions. Prefer hoisting a single fetch and passing `participations` down, or share via a query key. _(efficiency)_
- [ ] ✅ `src/components/SessionParticipationSheet.tsx` — Each SessionRow independently calls useParticipation('event', activityId, ...) (line 21), so an event with N sessions fires N separate participation fetches/subscriptions. Fetch all sessions' participations once in the parent and pass each row its slice. _(efficiency)_
- [ ] ✅ `src/components/TeamSelect.tsx` — Correctly avoids native <select> and handles dark mode, but as a custom widget it has no listbox/option roles, no aria-expanded on the trigger, and no keyboard support (arrow/Enter/Escape) — only a mousedown-outside close (lines 30-37). Add ARIA + keyboard handling. _(accessibility)_
- [ ] ✅ `src/components/ui/DatePicker.tsx` — When `error` is set the message renders in a `<p>` with no id, and the trigger button has neither `aria-invalid` nor `aria-describedby`, so screen readers never associate the error with the field. Add an id to the error `<p>`, wire `aria-describedby`, and set `aria-invalid` on the trigger (the same gap applies to `helperText`). _(accessibility)_
- [ ] ✅ `src/hooks/teamAbsencesFetch.ts` — The per-team `catch {}` swallows every team-fetch failure with no logging — a team whose coaches/TRs fail to load silently drops those staff from the absence scope. The remaining fetch should route through captureApiError so a permission/500 is observable per the 'all errors → Sentry' convention. _(error-handling)_
- [ ] ✅ `src/hooks/useAuth.tsx` — `matchesRole` uses `(user.role ?? []).includes(role as any)` and `(user as any)[role] === true` for per-flag licences — untyped index access that will not catch a renamed licence flag at compile time. Type the licence flags as a keyof-Member union (or a typed record) to remove the `any` casts. _(type-safety)_
- [ ] ✅ `src/hooks/useMutation.ts` — This hook re-implements what lib/query's useCreate/useUpdate/useDelete already do (mutation + invalidation), but with a different invalidation helper (`invalidateForCollection`, which uniquely also busts the `activities-with-participations` key). The two mutation paths have drifted: TanStack callers do NOT get the activities-with-participations bust. Extract `invalidateForCollection` into lib/query and have both paths call it so RSVP writes refresh the card grid regardless of which mutation API a component used. _(consistency)_
- [ ] ✅ `src/hooks/useNotifications.ts` — The realtime 'update' branch calls `setUnreadCount(...)` from INSIDE a `setNotifications` updater (`setNotifications(prev => { setUnreadCount(prev.filter(...).length); return prev })`) — a side effect in a state updater, which React double-invokes under StrictMode and is fragile. Recompute unread in a single updater or derive `unreadCount` via useMemo from `notifications` instead of tracking it as separate state. _(error-handling)_
- [ ] ✅ `src/hooks/usePushNotifications.ts` — subscribe/unsubscribe log failures with `console.error(...)` only — these never reach Sentry/the JSONL error log, violating the 'ALL frontend errors → Sentry' convention. Route the catch through captureException/captureApiError so push failures (Brave/FCM blocks, network) are observable, not just toasted and dropped. _(error-handling)_
- [ ] ✅ `src/hooks/useTeamAbsences.ts` — `useRealtime('absences', fetch)` refetches the entire multi-collection team query on ANY absence create/update/delete anywhere in the club, not just for the teams in view. Gate the realtime callback on whether `e.record.member` is in the current member set (like useNotifications/useParticipation do) to avoid full refetch storms during busy periods. _(efficiency)_
- [ ] ✅ `src/hooks/useTeamMembers.ts` — In `useTeamMembers` the two independent fetches run serially: `await fetchItem('teams', ...)` then `await fetchAllItems('member_teams', ...)`. The member_teams query does not depend on the team result (sport is only used afterwards for normalization), so wrap them in `Promise.all` to halve the latency on every roster/team-detail open. _(efficiency)_
- [ ] ✅ `src/hooks/useUserVisibleFormIds.ts` — This hook, useUserVisibleEventIds, and the junction branch of useMultiTeamMembers all repeat the same 'fetch <x>_teams junction filtered by teams_id _in, map relId(<x>_id), dedupe into a Set' pattern with hand-rolled cancelled/key effect plumbing. Extract a generic `useJunctionIds(junction, foreignKey, teamIds)` helper to remove three copies and guarantee the deep-filter-vs-policy workaround stays uniform. _(consistency)_
- [ ] ✅ `src/lib/api.ts` — The token-refresh-retry pattern (isPermissionError/401→refreshAuth→retry once) is hand-implemented three times: fetchItems (l.280-292), fetchItem (l.322-329), and kscwApi (l.519-524). Three copies drift independently (kscwApi already keys off HTTP status while fetchItems keys off message text). Extract a single withAuthRetry(fn) wrapper so the race-recovery logic lives in one place. _(consistency)_
- [ ] ✅ `src/lib/api.ts` — uploadFile (l.431) and openPrivateAsset (l.453) use raw fetch and throw bare Errors (`Upload failed (${status})`, `Asset ${status}`) WITHOUT routing through captureApiError — so file-upload and private-asset (invoice PDF) failures never reach Sentry or the JSONL log, unlike every other helper in this module. Wrap both in captureApiError for parity. _(error-handling)_
- [ ] ✅ `src/lib/api.ts` — stringifyId's KEEP_AS_NUMBER allowlist (l.196) is a denylist-by-omission: any numeric data field NOT listed is silently coerced to a string, breaking arithmetic/`===` comparisons downstream. It already carries ~40 hand-maintained field names; a new numeric column on any collection is a latent bug until someone remembers to add it here. At minimum add a regression test pinning the set; ideally derive numeric fields from schema metadata. _(type-safety)_
- [ ] ✅ `src/lib/sentry.ts` — scrubPii (l.422) recurses into nested plain objects but explicitly skips arrays (`&& !Array.isArray(val)`), so PII inside an array of objects (e.g. a payload `{ members: [{ email, first_name }] }` or a bulk mutation body) is forwarded to Sentry AND the JSONL log unredacted. Recurse into array elements too — the PII_FIELDS guard is nFADP-relevant. _(security)_
- [ ] ✅ `src/lib/sentry.ts` — The chunk-load-error regex is duplicated inline in beforeSend (l.88) rather than importing CHUNK_ERROR_RE / isChunkLoadError from chunkReload.ts — the comment even says 'Mirror the same regex here so ... doesn't surface as Sentry noise.' Two hand-synced copies of a long alternation is a guaranteed drift bug the next time an engine adds a variant. Import the single source. _(consistency)_
- [ ] ✅ `src/modules/admin/InfraHealthPage.tsx` — runChecks() awaits each independent health probe sequentially (prod, dev, tunnel, push, hooks, DB, error-log, CF pages, then cron/stats blocks) — the page's boot gate waits for the whole serial chain. Group the independent checkEndpoint/fetch calls into Promise.all batches to cut first-paint time substantially. _(efficiency)_
- [ ] ✅ `src/modules/admin/RefereeExpensesPage.tsx` — exportCsv hand-rolls CSV escaping (only quote-doubling) with NO spreadsheet-formula-injection guard, even though the `notes` and team-name cells are user-controllable — a note starting with =/+/-/@ executes on open. admin/utils/exportResults.ts (toCSV) already neutralizes this; reuse it instead of re-implementing. _(security)_
- [ ] ✅ `src/modules/admin/VolleyFeedbackPage.tsx` — exportCSV lacks the formula-injection guard while feedback_text/ideas_text/other_text are free-text user input; and TEAM_COLORS hardcodes hex per team instead of reusing the shared TeamChip/teamNameToColorKey system used elsewhere. Page title 'Volley Feedback', 'CSV Export', and headers Name/Team/Text are hardcoded English. _(security)_
- [ ] ✅ `src/modules/admin/components/ExplorerDetail.tsx` — Pervasive `as unknown as {...}` / `as never` casts (e.g. renderMember(entity as never), gAny casts in renderTeam/renderGame) defeat type-checking; the cache entities could carry typed shapes so accessor casts aren't needed per call site. _(type-safety)_
- [ ] ✅ `src/modules/auth/ProfileEditModal.tsx` — The custom position multi-select (button + absolute dropdown) has no aria-expanded/role/keyboard support and the round 'i' info button has no aria-label — keyboard/screen-reader users can't operate or identify them. _(accessibility)_
- [ ] ✅ `src/modules/auth/ProfileEditModal.tsx` — The language <Select> + flagMap + LANGUAGES.map block is duplicated almost verbatim in SignUpPage.tsx (and the flag imports too). Extract a shared <LanguageSelect> to remove the drift. _(consistency)_
- [ ] ✅ `src/modules/calendar/CalendarPage.tsx` — This page defines its own TypeIcon (inline SVGs) and `iconColors` map (lines 36-96), while MonthGrid/WeekGrid/MobileMonthView/MobileWeekGrid each define ANOTHER TypeIcon + barColors/dotColors for the same entry types. Extract one shared calendar entry-style module (icon + palette + colorKey) so a new source type or color tweak is a one-file change instead of six. _(consistency)_
- [ ] ✅ `src/modules/calendar/MobileWeekView.tsx` — Dead code: not imported anywhere (CalendarPage renders MobileWeekGrid for mobile week view). This 175-line file plus its own duplicated typeStyles map can be deleted to shrink the bundle and the maintenance surface. _(consistency)_
- [ ] ✅ `src/modules/calendar/UnifiedCalendarView.tsx` — Dead code: no importers (CalendarPage uses MonthGrid/MobileMonthView). Delete this 100-line file and its duplicated typeStyles. _(consistency)_
- [ ] ✅ `src/modules/calendar/UnifiedListView.tsx` — Dead code: no importers. Note it's ironically the only calendar view that correctly uses <Table> with responsive column hiding — worth salvaging as the reference impl if an agenda/list view is ever reintroduced, otherwise delete. _(consistency)_
- [ ] ✅ `src/modules/calendar/components/MobileWeekGrid.tsx` — Duplicates WeekGrid's full time-grid engine (see WeekGrid note) — extract the shared layout util to fix them together. _(consistency)_
- [ ] ✅ `src/modules/calendar/components/MonthGrid.tsx` — barColors, dotColors, TypeIcon and colorKey/paintKey are duplicated across MonthGrid, WeekGrid, MobileMonthView, MobileWeekGrid (and the dead MobileWeekView) — five near-identical copies. Extract a shared calendar entry-style module. _(consistency)_
- [ ] ✅ `src/modules/calendar/components/WeekGrid.tsx` — The entire time-grid engine — HOUR_HEIGHT/TOP_PAD, getDayTimeRange, getVisibleRange, timeToMinutes, minutesToOffset, formatHour, layoutOverlaps, PositionedEvent — is duplicated almost verbatim in MobileWeekGrid.tsx. Extract a shared weekGridLayout util; the two currently even disagree on min-height (18 vs 20) and a stale range comment. _(consistency)_
- [ ] ✅ `src/modules/calendar/hooks/useCalendarData.ts` — The absences query uses `fields: ['id', 'member.*', ...]` (line 373) — `member.*` pulls every member column (email, phone, birthdate, IBAN, etc.) when the code only reads id/first_name/last_name for the disambiguated label. Scope to `member.id`, `member.first_name`, `member.last_name` to cut payload and avoid shipping unneeded PII to the client. _(security)_
- [ ] ✅ `src/modules/events/EventCard.tsx` — asTeams(), teamId(), eventTypeColors and isHtml() are defined identically here, in EventDetailModal.tsx and EventsPage.tsx (three copies). Extract to a shared events util so the M2M junction extraction and type palette live in one place. _(consistency)_
- [ ] ✅ `src/modules/events/EventDetailModal.tsx` — The date line (lines 148-149) shows an end date whenever `event.start_date !== event.end_date`, comparing FULL datetime strings. A same-day timed event (start 16:00, end 18:00) has differing strings and renders a redundant range like '10.05.2026 — 10.05.2026'. EventCard already handles this by comparing `.split('T')[0]`; apply the same day-portion comparison here. _(ui-polish)_
- [ ] ✅ `src/modules/events/EventsPage.tsx` — Third copy of asTeams()/teamId() (lines 24-37). Move to the shared events util referenced on EventCard. _(consistency)_
- [ ] ✅ `src/modules/feedback/FeedbackPage.tsx` — The GitHub issues fetch (lines 96-121) runs on every mount with no caching (bypasses react-query) and, on a rate-limited/error response where `data` isn't an array, `.filter` throws into a bare `.catch(() => {})` — the user just sees 'no issues' with no error state. Move to useQuery with a staleTime and render an explicit error state. _(efficiency)_
- [ ] ✅ `src/modules/finance/FinanceMemberExplorer.tsx` — Invoice-status → colour/label mapping is implemented a third time here (useStatusPill) alongside InvoiceManager's STATUS_TONE+statusLabel and DuesRunManager's STATUS_TONE. Consolidate into one shared invoiceStatus helper to keep pill colours/labels in sync across the finance tabs. _(consistency)_
- [ ] ✅ `src/modules/finance/InvoiceManager.tsx` — LinkMemberModal.submit() has try/finally but no catch (lines 219-226): if linkInvoiceMember rejects, onDone/onClose never run, the promise rejection is unhandled, and the user gets no error — the modal just stays open. Add a catch with an inline/toast error like the sibling modals. _(error-handling)_
- [ ] ✅ `src/modules/finance/LedgerTab.tsx` — labelCls, inputCls, selectCls and apiErr are re-declared here identically to DuesRunManager, DunningConsole, PaymentLedgerModal, TeamFinance and DuesEmail (apiErr in 6 files). Extract to a shared finance/formStyles.ts + finance/apiError.ts. _(consistency)_
- [ ] ✅ `src/modules/fines/FinesSettings.tsx` — Tier edits save on every keystroke: each `updateTier`/number-input onChange calls `handleTiersChange` → `save()`, firing a full `fine_rules` PATCH per digit typed in an offense/amount field — debounce the tier save (or save on blur) to stop the request storm. _(efficiency)_
- [ ] ✅ `src/modules/gameScheduling/pages/AdminDashboardPage.tsx` — This is a 1584-line component holding ~15 useState plus 4 standalone data-fetch useEffects (derby games, away-vm-check, home-vm-check, season-summary) in addition to useAdminBookings/useInvites/useMailbox. Extract the per-effect fetches into dedicated hooks (e.g. useAwayVmChecks(seasonId)) to cut re-render surface and make the load paths testable. _(consistency)_
- [ ] ✅ `src/modules/gameScheduling/pages/OpponentFlowPage.tsx` — handleConfirmSide loops `await proposeHome(...)` / `await proposeAway(...)` sequentially, and each call internally awaits fetchSlots() (a full slots-payload refetch). For a multi-game (junior triple round-robin) side this does N sequential submit+refetch round-trips. Submit all legs, then refetch once — e.g. add a skipRefetch flag to propose* and call refetch() after the loop. _(efficiency)_
- [ ] ✅ `src/modules/games/components/GameCard.tsx` — The whole `GameCardParticipation` sub-component (RSVP buttons, optimistic status, guest counter, note input, save-confirm popovers, absence-note sync-during-render) is duplicated almost verbatim in TrainingCard.tsx's `TrainingParticipation` and again in TrainingDetailModal — extract one shared `<ActivityParticipation>` to stop the 3-4x drift. _(consistency)_
- [ ] ✅ `src/modules/games/components/RankingsTable.tsx` — `useCollection('games', { limit: 500, enabled: !compact })` fires for every rankings table on the page (one per league) even though the games are only used when a row is expanded — defer the fetch until first expansion (or fetch once at page level) to avoid N eager 500-row queries on the rankings tab. _(efficiency)_
- [ ] ✅ `src/modules/games/components/useGameAttendanceStats.ts` — The core loop is O(games × members × participations): for every (game, member) it does `participations.find(...)` and `absences.some(...)` full scans (lines 111-133, and again for trend/future). Pre-index participations into a `Map<'member|activity'>` and absences per member once — same output, drops to O(games × members). _(efficiency)_
- [ ] ✅ `src/modules/hallenplan/components/DaySlotView.tsx` — Shares ~150 lines of overlap/team-resolution/handleSwap logic with WeekSlotView.tsx (see that file) and repeats the hardcoded English `title="Switch overlap"` (line 313); dedupe both into a shared module. _(consistency)_
- [ ] ✅ `src/modules/hallenplan/components/WeekSlotView.tsx` — The overlap-detection, overlapGroups, boostedMap/handleSwap, and teamMap/resolveTeam/getTeamName/getTeamSport blocks are duplicated near-verbatim in DaySlotView.tsx (~150 lines) — extract a shared hook/util. _(consistency)_
- [ ] ✅ `src/modules/home/HomePage.tsx` — The `statusBorderColor` map (confirmed/tentative/declined/waitlisted/absent → bg classes) is copy-pasted verbatim in five sub-components here (CompactGameRow, CompactTrainingRow, AppointmentRow, AppointmentTableRow, EventRow) and again in events/EventCard.tsx. Extract one shared `participationStatusColor` constant (utils/participationColors.ts already exists and is the natural home). _(consistency)_
- [ ] ✅ `src/modules/messaging/hooks/useConversations.ts` — Each useConversations() call independently fetches listConversations() and opens its own useRealtime('messages') subscription. It is invoked separately by InboxPage, ConversationPage, and useUnreadTotal (nav badge), so the same list is fetched 2-3x and 2-3 duplicate realtime subscriptions run concurrently. Hoist into a shared context/provider so all consumers share one fetch + one subscription. _(efficiency)_
- [ ] ✅ `src/modules/messaging/hooks/useMemberProfiles.ts` — useMemberProfiles, useMemberDisplayNames and useTeamProfiles are three near-identical copies of the same 'fetchAllItems keyed by sorted-id-string, latest-wins, empty-map fallback' hook. Extract one generic useBatchLookup(collection, fields, mapFn). Also ConversationList uses useMemberProfiles while ConversationPage uses useMemberDisplayNames for the same other_member id, duplicating the members fetch for the peer. _(consistency)_
- [ ] ✅ `src/modules/news/NewsArchivePage.tsx` — The notification message-decoding (JSON.parse body → t(title) → strip trailing '@' and :SS) and the announcement timeAgo logic are copy-pasted from HomePage.tsx's NewsRow/AnnouncementRow. Extract shared `formatNotificationMessage()` / relative-time helpers so the two feeds can't diverge (they already have — see the days bug above). _(consistency)_
- [ ] ✅ `src/modules/polls/PollCard.tsx` — The vote button's `disabled={selected.length === 0 || isLoading}` uses `isLoading` from usePollVotes, which is the votes-fetch loading flag, NOT the create/update mutation state. During a slow save the button stays enabled → a second tap fires a duplicate create/update. Track the mutation's pending state instead. _(error-handling)_
- [ ] ✅ `src/modules/polls/PollCard.tsx` — handleVote awaits vote() with no try/catch, and usePoll's create/update don't catch either, so a failed vote rejects unhandled and leaves the UI showing the old state with no toast. Surface the failure and keep the selection. _(error-handling)_
- [ ] ✅ `src/modules/polls/PollForm.tsx` — handleSubmit calls the async onSubmit fire-and-forget then immediately resets state and calls onClose() (lines 37-50). If addPoll's createPoll rejects, the dialog is already closed and the typed question/options are lost with no error shown. Await the result and only close on success. _(error-handling)_
- [ ] ✅ `src/modules/scorer/ScorerAssignPage.tsx` — `handleSaveAll` writes each assignment with a serial `await updateRecord` loop (lines 154-173) — for a whole season that's hundreds of sequential PATCHes; chunk/parallelize. _(efficiency)_
- [ ] ✅ `src/modules/teams/PlayerProfile.tsx` — Two large effects (training + game attendance) each fire 3 parallel `fetchAllItems`, so a profile load runs ~9 queries; participations are fetched twice (once per activity_type) and absences three times (top-level + each effect). Consolidate the shared fetches and DRY the two near-identical effects. _(efficiency)_
- [ ] ✅ `src/modules/teams/TeamsPage.tsx` — `useCollection('member_teams', { all: true, filter: {season} })` pulls EVERY member_teams row for the season just to compute per-team player/guest counts (playersByTeam/guestsByTeam). Use a Directus aggregate/groupBy on `team` (or count endpoints) instead of shipping the full junction table to the client. _(efficiency)_
- [ ] ✅ `src/modules/trainings/RecurringTrainingModal.tsx` — `handleGenerate` creates each occurrence with a serial `await createRecord` loop (lines 264-285) — a full-season generation is ~30 sequential POSTs. Directus supports array-body batch create; send one request (dedup is already re-checked up front). _(efficiency)_
- [ ] ✅ `src/modules/trainings/TrainingCard.tsx` — `TrainingParticipation` duplicates GameCard's `GameCardParticipation` (optimistic status, guest counter, note-sync-during-render, auto-dismiss confirm) almost line-for-line — extract a shared participation control. _(consistency)_
- [ ] ✅ `src/modules/trainings/TrainingDetailModal.tsx` — Contains a THIRD copy of the participation UI (`TrainingParticipation` here uses the `useParticipation` hook while TrainingCard's copy hand-rolls it) — consolidating the participation control (see GameCard/TrainingCard) would remove three parallel implementations. _(consistency)_
- [ ] ✅ `src/modules/trainings/TrainingForm.tsx` — `bulkUpdateSiblings` updates siblings with a serial `await updateRecord` loop (lines 397-410) — for an 'all recurring' edit that's dozens of sequential PATCHes; batch/parallelize (chunked Promise.all). _(efficiency)_
- [ ] ✅ `src/modules/trainings/useAttendanceStats.ts` — Same O(trainings × members × participations) scan pattern as useGameAttendanceStats — `participations.find`/`absences.some` inside the nested member loop (lines 130-153, 166-203). Pre-index participations by `member|activity_id` and absences by member once. _(efficiency)_
- [ ] ✅ `src/utils/dateUtils.ts` — Two modules named-export a function called `formatDate` with different signatures and semantics: dateUtils' `formatDate(date: Date, pattern)` (date-fns, enUS) vs dateHelpers' `formatDate(str, locale?)` (Intl, de-CH, dd.mm.yyyy). Same name, different behavior invites importing the wrong one. Rename one (e.g. dateUtils→formatDatePattern). _(consistency)_
- [ ] ✅ `src/utils/teamColors.ts` — getTeamColor (l.114-129) and teamNameToColorKey (l.147-164) each independently re-implement the 'match longest BB- shortcode against a long team name' loop, with slightly different predicates — getTeamColor tests `key.includes(\` ${sc} \`)` while teamNameToColorKey tests `name.includes(\`${sc} \`)`. They can therefore disagree on which color a long BB name maps to. Extract one shared matcher used by both. _(consistency)_

### 🔒 Gated — need approval (48)

- [ ] 🔒 `src/components/AttendanceTable.tsx` — Trend dots render `title={status}` with the raw untranslated value ('present'/'absent') as the tooltip, and the percentage badges (`bg-green-100 text-green-800`, amber, red) carry no `dark:` variants so they stay light-on-light in dark mode. Translate the tooltip and add dark-mode badge tokens. _(ui-polish · GATED: Adds new i18n strings across 5 locales and changes badge appearance in dark mode.)_
- [ ] 🔒 `src/components/BottomTabBar.tsx` — The unread-notification indicator on the More tab is a bare red dot (`<span class="...bg-red-500">`) with no text — screen-reader users get zero signal that there are unread notifications. Add an sr-only label conveying the count. _(accessibility · GATED: Adds a new user-facing (AT) i18n string across 5 locales.)_
- [ ] 🔒 `src/components/CalendarGrid.tsx` — Accessibility gaps on the icon-only controls: the empty-day quick-add button hardcodes `aria-label="Add manual game"` (untranslated, and wrong wording for the non-game consumers of this generic grid), and the prev/next month chevron buttons have no `aria-label` at all. Make the add label a prop/i18n key and label the month-nav buttons. _(accessibility · GATED: Requires new i18n strings (and a new prop) that surface as user-facing AT copy.)_
- [ ] 🔒 `src/components/FormField.tsx` — The password show/hide toggle uses hardcoded English `aria-label={showPassword ? 'Hide password' : 'Show password'}`, bypassing i18n in a 5-locale app. Route it through `useTranslation`. _(consistency · GATED: Adds new i18n strings across 5 locales (user-facing AT copy).)_
- [ ] 🔒 `src/components/MemberMultiSelect.tsx` — It fetches every active member with `limit: -1` and filters client-side, then silently caps the dropdown at `filtered.slice(0, 50)` with no 'showing 50 of N' hint — on a ~1000+ member club this over-fetches on mount and can hide valid matches beyond the first 50 with no indication to narrow the search. Consider server-side search/pagination and a truncation hint. _(efficiency · GATED: Changes the fetch strategy and the visible results/UX (truncation hint).)_
- [ ] 🔒 `src/components/ParticipationRosterModal.tsx` — Role/staff/guest suffixes are hardcoded English: on-screen badges `Coach`/`C`/`TR` (line 1400) and export names ` (Coach)`/` (C)`/` (TR)` (line 855), ` (Staff)` (line 943). Non-English users see English in the roster and in exported PDFs/CSVs. Route through i18n. _(consistency · GATED: Changes visible on-screen + exported text across all locales.)_
- [ ] 🔒 `src/components/StatusBadge.tsx` — labelMap (lines 34-65) is fully hardcoded English and several are Title Case (`VB Admin`, `Website Admin`, `SuperAdmin`, `Team Resp.`) — violates the multilingual + Sentence-case conventions; badges render English Title Case in all 5 locales. Drive labels through i18n keys. _(consistency · GATED: Changes visible badge text across all locales.)_
- [ ] 🔒 `src/components/TeamMultiSelect.tsx` — The >5-selected summary appends an English plural manually: `{n} {t('team')}{n>1?'s':''}` (line 79) — wrong for DE/FR/IT/GSW. Use an i18n plural key (t('teamCount',{count})). _(consistency · GATED: Changes displayed count text across locales.)_
- [ ] 🔒 `src/components/ui/DatePicker.tsx` — The trigger displays `formatDateLocale(selectedDate, 'd. MMM yyyy', lang)` → e.g. "5. Mai 2026" / "May 5, 2026", which violates the app-wide Swiss date convention (dd.mm.yyyy, dot-separated, day-first). Use the central `formatDateZurich`/`formatDateCompactZurich` helper so the displayed value matches every other date surface. _(consistency · GATED: Changes the user-visible date string format in the trigger.)_
- [ ] 🔒 `src/components/ui/SearchableSelect.tsx` — The mobile branch (`!isDesktop`) renders the full unfiltered `options` list with no search box, while desktop filters via the search input. For large lists (member pickers ~1000 rows) mobile users must scroll the entire list, and all option nodes are rendered at once. Consider a filter input on mobile too and/or windowing. _(efficiency · GATED: Introduces a mobile search field / changes the mobile interaction and rendered output.)_
- [ ] 🔒 `src/components/ui/button.tsx` — The base cva string hardcodes `min-h-[44px]`, which overrides the height of the `sm` (h-8/32px) and `icon` (h-9 w-9/36px) size variants — min-height wins over height, so every button renders at least 44px tall. Result: `size="icon"` buttons are 36px wide x 44px tall (not square), and `sm` buttons are never actually shorter. The size variants' height values are effectively dead for height. Either move min-h onto the `default` size only, or drop the per-size h-* values. _(ui-polish · GATED: Changes rendered button dimensions app-wide (icon buttons become square, sm buttons shorter).)_
- [ ] 🔒 `src/components/ui/calendar.tsx` — `formatMonthDropdown` hardcodes `date.toLocaleString("de-CH", { month: "short" })`, so the month dropdown always shows German abbreviations (Mai, Okt) even though DatePicker passes a real `locale` (de/enUS) for weekday names. English/French/Italian users get German months in the same picker where weekdays are localized — inconsistent. The de-CH hardcode rule is about numeric date formatting, not month-name UI text; use the DayPicker locale here. _(consistency · GATED: Changes visible month-name text in the picker for non-German locales.)_
- [ ] 🔒 `src/components/ui/calendar.tsx` — `--cell-size: 2rem` (32px) makes every day cell/button 32px, below the 44px minimum touch target the rest of the app enforces (Button, DatePicker trigger, SearchableSelect all use min-h-[44px]). On mobile the calendar is hard to tap accurately. _(accessibility · GATED: Enlarging cells changes the calendar's rendered size/layout.)_
- [ ] 🔒 `src/components/ui/popover.tsx` — `PopoverContent` sets no `data-slot`, yet calendar.tsx targets `[[data-slot=popover-content]_&]:bg-transparent` to make the calendar background transparent inside a popover. Because the attribute is never emitted, that rule is dead — the calendar keeps its opaque `bg-background` inside popovers. Either add `data-slot="popover-content"` here (enabling the intended transparent styling) or remove the dead selector from calendar.tsx. Same issue with card.tsx / `data-slot=card-content`. _(consistency · GATED: Adding the data-slot activates the calendar's transparent-background styling, a visible change (removing the dead selector instead would be internal-only).)_
- [ ] 🔒 `src/components/ui/select.tsx` — `SelectTrigger` is `h-9` (36px), below the 44px touch-target minimum enforced by Button, DatePicker and SearchableSelect. Selects in forms sit next to 44px controls and are the odd, smaller tap target on mobile. Align to `min-h-[44px]`. _(accessibility · GATED: Increases the trigger height, a visible layout change.)_
- [ ] 🔒 `src/components/ui/table.tsx` — `TableCell` and `TableHead` default to `whitespace-nowrap`. This is the documented "nowrap trap": long text cells (names, notes) overflow horizontally on mobile unless every consumer manually adds `whitespace-normal break-words`. Given the CLAUDE.md rule that ALL data lists must use this Table, the default forces per-call workarounds. Consider making text cells wrap by default (or a `nowrap` opt-in variant) so the mobile compaction rules are the default, not the exception. _(ui-polish · GATED: Changing the default wrapping alters layout of every table across the app.)_
- [ ] 🔒 `src/hooks/useParticipation.ts` — `setStatus` and `clearStatus` catch the mutation error, revert the optimistic value, and then swallow it — the user gets no toast/feedback that their RSVP failed to save (the row silently snaps back). The underlying api layer captures to Sentry, but the UI is silent. Surface a `toast.error` on the revert so a failed save is visible. _(error-handling · GATED: Adds a user-facing error toast that does not exist today)_
- [ ] 🔒 `src/modules/admin/AnmeldungenPage.tsx` — Several user-facing strings are hardcoded German and bypass i18n: FilePreview 'PDF — im neuen Tab öffnen' / 'In neuem Tab öffnen', the `{countDocs(reg)} docs` label, and alt="Document". These render German for en/fr/it/gsw users. _(consistency · GATED: Translating alters displayed text for non-German locales.)_
- [ ] 🔒 `src/modules/admin/AuditLogPage.tsx` — setFilterAndSearch() and clearFilters() call setState then `setTimeout(() => handleSearch()/fetchLogs(1), 0)`; the deferred closure captures the CURRENT render's handleSearch/fetchLogs, which read the OLD filter values — so clicking a collection/actor/record chip filters by stale state and 'Clear filters' refetches with the pre-clear filters. Pass the new value explicitly or drive the fetch from a useEffect on the filter state. _(error-handling · GATED: Fixes what results are actually returned when filter chips / clear are used (user-facing).)_
- [ ] 🔒 `src/modules/admin/ClubStatsPage.tsx` — All the data tables (roster, schreiber coverage, participation, results, delegations) use raw <table>/<th>/<td> instead of the shadcn <Table> required for data-record lists; they also don't get the convention's mobile compaction behavior. _(consistency · GATED: Migrating raw tables to <Table> changes visual styling.)_
- [ ] 🔒 `src/modules/admin/VolleyFeedbackPage.tsx` — The text-detail popup is a hand-rolled `fixed inset-0` overlay with no role="dialog"/aria-modal, no focus trap, no Escape-to-close, and an unlabelled × button — unusable by keyboard/AT users. Use the shared <Modal> like the rest of the app. _(accessibility · GATED: Switching to <Modal> changes focus/escape/DOM behavior.)_
- [ ] 🔒 `src/modules/admin/components/ExplorerDetail.tsx` — Every subtable (teams, participations, absences, referee expenses, trainings, games, hall slots, scoring duties) is rendered via the local raw-<table> CompactTable helper instead of the shadcn <Table> from src/components/ui/table.tsx, violating the 'data-record lists MUST use <Table>' convention. Migrating to <Table> (or documenting the dense-panel exception) would unify styling and mobile/dark behavior. _(consistency · GATED: Swapping raw tables for shadcn Table changes padding/borders/visual output.)_
- [ ] 🔒 `src/modules/auth/SignUpPage.tsx` — Existing-team dedup is broken: existingTeams from /check-email carry only {name,sport} and are given synthetic index ids (id:String(i)), but availableTeams filters `!existingTeamIds.includes(t.id)` against real team ids — the exclusion never matches, so a claimed member's already-assigned teams can reappear in the 'join additional teams' checklist. Dedup by team name instead. _(error-handling · GATED: Fixing changes which teams appear in the additional-teams list (user-facing).)_
- [ ] 🔒 `src/modules/calendar/CalendarEntryModal.tsx` — renderTrainingDetails/renderEventDetails hardcode English: `label="Coach"` (line 238), fallback `'Cancelled'` (line 235), and an English event `typeMap` ('Club','Social','Meeting','Tournament','Other', lines 271-277) that is also incomplete (missing 'trainingsweekend'/'friendly' → shows the raw key). These bypass i18n and render English in all 5 locales. Route through t()/StatusBadge like EventDetailModal does. _(consistency · GATED: Adds/changes user-facing translated strings across the 5 locales.)_
- [ ] 🔒 `src/modules/calendar/CalendarEntryModal.tsx` — renderTrainingDetails renders `value={training.coach}` directly (line 238), but useCalendarData fetches trainings without expanding `coach`, so it's a bare member ID → the modal shows 'Coach: 5'. Resolve the coach name (expand the relation) or omit the row. _(error-handling · GATED: Requires expanding the coach relation and changes what's displayed.)_
- [ ] 🔒 `src/modules/calendar/components/MonthGrid.tsx` — Hardcoded English strings bypass i18n: `+{overflow} more` (line 443) and `${seg.count} absent` (line 491) render in English for all locales. Move to translation keys. _(consistency · GATED: Adds user-facing translated strings across the 5 locales.)_
- [ ] 🔒 `src/modules/calendar/components/WeekGrid.tsx` — weekLabel uses `formatDate(weekMonday, 'MMM d')` / `'MMM d, yyyy'` (line 238) → US month-first order like 'Mai 5 – Mai 11, 2026'. CLAUDE.md mandates Swiss day-first dd.mm; use a day-first format (e.g. 'd. MMM' / dd.mm.yyyy). _(ui-polish · GATED: Changes the visible date format in the week header.)_
- [ ] 🔒 `src/modules/finance/DuesRunManager.tsx` — Issuing a real dues run (creates invoices for every billable member) is gated only behind window.confirm(t('duesIssueSure')) — a high-stakes, irreversible batch action behind a browser confirm that is easy to reflexively accept. Use the app's AlertDialog (as GameDetailDrawer does) with an explicit destructive-style confirm and the count/amount summary. _(ui-polish · GATED: Replaces the native confirm dialog with an in-app dialog (user-facing flow change))_
- [ ] 🔒 `src/modules/finance/InvoiceManager.tsx` — MemberPicker loads the entire active-members collection client-side (useCollection('members', { limit: -1 })) then filters in JS — ~1000+ rows shipped to the browser for a dropdown. A dedicated searchable-members endpoint already exists in messaging; reuse a server-side search here. _(efficiency · GATED: Switches the picker to server-side search, changing fetch/UX (debounce, min-chars))_
- [ ] 🔒 `src/modules/finance/LedgerTab.tsx` — Error feedback for reverse/delete/toggle-account uses native alert(apiErr(...)) (lines 74-75, 178), inconsistent with the rest of the app which uses sonner toasts / inline red text. Replace with toast.error so ledger errors match app conventions and aren't blocking modal dialogs. _(error-handling · GATED: Changes how errors are surfaced to the user (blocking alert vs toast))_
- [ ] 🔒 `src/modules/games/EmbedGamesPage.tsx` — GameCard is rendered with `variant="compact"` inside a `space-y-3` wrapper (line 110-113), but the compact desktop layout uses `md:grid-cols-subgrid` and needs an 8-column grid parent (as GamesPage provides); here the desktop columns won't align — wrap in the same `md:grid md:grid-cols-[...]` container or force the mobile row layout. _(ui-polish · GATED: changes the rendered embed layout on desktop)_
- [ ] 🔒 `src/modules/hallenplan/components/SlotBlock.tsx` — `typeLabels = { training:'Training', game:'Game', event:'Event', away:'Away' }` (lines 51-57) are hardcoded English and rendered as the slot's visible label and tooltip (lines 124, 155, 255) on every slot regardless of locale; use the existing `t('typeTraining')` etc. keys (SlotEditor already has them). _(consistency · GATED: i18n change alters the label text shown on every hall-plan slot)_
- [ ] 🔒 `src/modules/hallenplan/components/WeekSlotView.tsx` — `DAY_HEADERS = ['Mon','Tue',...]` (line 10) are hardcoded English weekday abbreviations rendered in the grid headers regardless of locale (SlotEditor uses `t('dayMonday')` etc.), and the overlap 'Switch overlap' button `title` (line 519) is a hardcoded English string with no `aria-label`. _(consistency · GATED: i18n change alters the weekday headers shown to de/fr/it/gsw users)_
- [ ] 🔒 `src/modules/messaging/components/ConversationList.tsx` — formatDistanceToNowStrict(new Date(...), { addSuffix: true }) is called with no date-fns locale, so relative timestamps ('2 hours ago') always render in English regardless of the DE/FR/GSW/IT UI locale. Pass the active locale (date-fns/locale) so the relative time matches the chosen language. _(consistency · GATED: Changes the displayed timestamp text for non-English users)_
- [ ] 🔒 `src/modules/polls/hooks/usePoll.ts` — Deadline filtering compares `new Date(p.deadline) >= new Date()` (line 75; mirrored in PollCard line 25). Poll `deadline` is a date-only string, which `new Date()` parses as UTC midnight — in Europe/Zurich (UTC+1/+2) a poll silently disappears/locks 1–2 hours before local midnight of its deadline day. Compare against Zurich end-of-day. _(error-handling · GATED: Changes when a poll stops accepting votes (user-facing behavior near the boundary).)_
- [ ] 🔒 `src/modules/scorer/ScorerPage.tsx` — `toggleReminders` writes `updateRecord('app_settings', id, { requestKey: null })` (line 120) — it never sets `enabled`, yet the button label/state reads `reminderSetting.enabled`; unless a backend hook flips `enabled` off `requestKey`, clicking the toggle won't change the reminders state. Verify the field being written actually toggles the setting. _(error-handling · GATED: correcting the write changes the reminder-toggle behavior)_
- [ ] 🔒 `src/modules/scorer/ScorerPage.tsx` — Every mutation handler (`toggleReminders`, `handleUpdate`, `handleDelegate`, accept/decline) only `console.error`s on failure — an admin gets no feedback when an assignment/delegation save fails; add toasts. `app_settings` is typed `any` (line 107). _(error-handling · GATED: adds user-facing failure toasts)_
- [ ] 🔒 `src/modules/spielplanung/ImportPanel.tsx` — parseRow returns hardcoded English error strings ('unknown team', 'out of scope', 'missing opponent', 'missing date', 'unknown hall') and the preview renders `{p.error ?? 'OK'}` — user-facing status text that never localizes. Return message keys and translate, and localize the 'OK' label. _(consistency · GATED: Changes user-visible preview status text and adds translation keys)_
- [ ] 🔒 `src/modules/spielplanung/ListView.tsx` — Hardcoded, untranslated English string in ByTeamView: `({group.games.length} games)` (line 193). Move to an i18n key (e.g. games count) so it localizes across the 5 locales. _(consistency · GATED: Changes user-visible text and adds a translation key)_
- [ ] 🔒 `src/modules/spielplanung/ListView.tsx` — StatusBadge and TypeBadge colour classes (bg-brand-50/text-brand-700, bg-green-50/text-green-700, bg-orange-50/text-orange-700, bg-amber-50/text-amber-700) have no dark: variants, so in dark mode they render light badges with light text (poor contrast). Add dark: variants (the 'completed' status already does) or use semantic tokens. _(accessibility · GATED: Alters badge appearance in dark mode)_
- [ ] 🔒 `src/modules/tasks/hooks/useTasks.ts` — Applying a template (TasksSection.handleApplyTemplate loops addTask synchronously) is buggy because addTask computes maxOrder from the `tasks` array captured in its useCallback closure — the same stale snapshot for every iteration — so all imported tasks receive identical sort_order (maxOrder+1) and their order is unstable; it also fires N sequential create+refetch round-trips. Compute a running order or accept a batch create. _(efficiency · GATED: Fixing changes the resulting task ordering after a template apply (user-facing).)_
- [ ] 🔒 `src/modules/teams/MemberRow.tsx` — `saveField` on success mutates the underlying member object in place (`(memberRef as Record<string, unknown>)[field] = value`, lines 85-88) instead of a state update; it only appears to work because `setEditingField(null)` re-renders. `toggleRole`/`saveField` also `catch { // ignore }` with no toast on failure. _(error-handling · GATED: adding failure feedback introduces a user-facing message)_
- [ ] 🔒 `src/modules/teams/TeamDetail.tsx` — The staff/roster/guest sections use three raw `<table>` blocks with duplicated `SortHeader` header rows (lines 638-666, 693-720, 730-759), while the sibling RosterEditor uses the shadcn `<Table>` primitive; extract one `<RosterTable>` and move to `<Table>` for consistency. _(consistency · GATED: swapping raw tables for the shadcn Table primitive alters rendered markup/styling)_
- [ ] 🔒 `src/modules/teams/TeamDetail.tsx` — `handleApprove`/`handleReject`/`handleApproveRequest`/`handleRejectRequest` all `catch { // ignore }` with no user feedback — a failed approve/reject silently does nothing; surface a toast on failure. _(error-handling · GATED: adds a new user-facing error message/toast)_
- [ ] 🔒 `src/modules/teams/TeamSponsorsEditor.tsx` — The sponsor list is rendered as a `space-y-2` card stack (lines 205-241), but sponsors are explicitly named in the data-record convention as a case that MUST use `<Table>` (mobile-compacted). _(consistency · GATED: converting card stack to a table changes rendered layout)_
- [ ] 🔒 `src/utils/dateUtils.ts` — formatDate (l.26-28) hardcodes { locale: enUS } but is called across the app with month/weekday patterns — 'EEEE, d. MMMM yyyy' (CalendarEntryModal), 'MMMM yyyy' (MobileMonthView), 'EEEE, d MMMM' (TeamAbsenceView) — so month and weekday names always render in English regardless of the user's UI language (DE/FR/IT/GSW). Route these calls through formatDateLocale, or make formatDate locale-aware. _(consistency · GATED: Changes user-visible month/weekday text in the 4 non-English locales)_
- [ ] 🔒 `src/utils/icalGenerator.ts` — formatICalLocalOffset (l.116-123) computes `newH = h + hoursOffset` with no 24h/day rollover, so an event starting 22:00-23:59 with no endTime produces the default +2h end as `T24xx00`/`T25xx00` — an invalid iCal time that calendar apps reject or mis-render. Normalize the end via a real Date (date + time + offset) instead of raw hour arithmetic. _(error-handling · GATED: Changes the generated .ics output (user-facing calendar export))_
- [ ] 🔒 `src/utils/rosterExport.ts` — Export column headers (COLUMNS, l.46: 'Name','Number','Positions',…) and CSV meta labels ('Filter:', 'Positions:', 'Exported:', l.91-93) are hardcoded English while the app is 5-locale — a DE/FR coach exporting a roster gets English-headed files, inconsistent with the localized on-screen table. Thread i18n keys through from the caller. _(consistency · GATED: Changes user-facing text inside exported CSV/PNG/PDF files)_

## Low impact (180)

### ✅ Safe (132)

- [ ] ✅ `src/components/AdminRoute.tsx` — AdminRoute, AdminOrSpielplanerRoute, FinanceRoute, MailboxRoute and AuthRoute all repeat the identical `if (isLoading || teamsLoading) return null; if (!<predicate>) return <Navigate to=... replace/>; return <>{children}</>` shape. Extract one `<RoleGuard predicate={...} redirect="/">` helper and express each guard as a one-line predicate — removes ~5 copies of the same boot-gating logic. _(consistency)_
- [ ] ✅ `src/components/AffectsMultiSelect.tsx` — In AFFECTS_COLORS the inactive fields (`bg: 'bg-transparent'`, `text: 'text-gray-500 dark:text-gray-400'`, `border: 'border-gray-300 dark:border-gray-600'`) are byte-for-byte identical across all four entries — only the `*Active` values differ. Collapse to one shared inactive style plus a per-value active map to kill the dead differentiation and shrink the config. _(consistency)_
- [ ] ✅ `src/components/BottomTabBar.tsx` — The trainings tab icon is a hand-inlined SVG while every other tab uses a lucide/WhistleIcon component; extract it to an icon component for consistency and to drop ~7 lines of inline path markup from the tab config. _(consistency)_
- [ ] ✅ `src/components/CancelActivityButton.tsx` — `actionLabel` and `confirmText` are built from three-level nested ternaries over (isCancelled × kind). Replace with a small lookup keyed by kind (e.g. `{ training: {...}, event: {...}, game: {...} }`) — same output, far more readable and safer when a fourth kind is added. _(consistency)_
- [ ] ✅ `src/components/CategoryMultiSelect.tsx` — `hasGroups`, `groups`, `selectedOptions` and the per-group filter/every/some passes recompute on every render (and every keystroke of the parent). Memoize the derived option groupings since category option sets can be sizeable. _(efficiency)_
- [ ] ✅ `src/components/LoadingSpinner.tsx` — The spinner wrapper has no `role="status"`/`aria-live="polite"`, so the rotating captions and progress percentage are never announced — AT users get no loading feedback (the `<img alt="Loading…">` is the only cue and doesn't re-announce). Add a status live region around the caption/progress. _(accessibility)_
- [ ] ✅ `src/components/MemberMultiSelect.tsx` — The suggestion dropdown lacks combobox/listbox ARIA and the custom check indicator is visual-only (no `aria-selected`/`role="option"`), so it's not screen-reader navigable. _(accessibility)_
- [ ] ✅ `src/components/NotificationPanel.tsx` — renderMessage() runs JSON.parse + a regex replace for every notification on every render, and timeAgo() is computed once at render with no interval so relative times go stale while the panel stays open. Memoize parsing per notification id. _(efficiency)_
- [ ] ✅ `src/components/OtpInput.tsx` — The resend countdown useEffect depends on `countdown`, so it tears down and recreates the setInterval every second (lines 24-30). Use a stable interval with a functional updater that stops at 0. _(efficiency)_
- [ ] ✅ `src/components/ParticipationButton.tsx` — handleGuestChange (line 234) issues a full setStatus() server write on every +/- tap; rapid increments fire many sequential mutations and can race. Debounce the guest-count write. _(efficiency)_
- [ ] ✅ `src/components/ParticipationSummary.tsx` — MIXED_TOURNAMENT_EVENT_ID = '5' (line 9) hardcodes a specific prod record ID into an otherwise generic counter; a data reseed / different environment silently breaks the non-member add-on. Source it from config/props. _(consistency)_
- [ ] ✅ `src/components/ParticipationWarningBadge.tsx` — The sort comparator (line 20) only distinguishes red vs non-red, so amber-vs-amber and any future severity order is undefined; a simple rank map (`{red:0,amber:1}`) reads clearer and is stable. Otherwise clean (has aria-label, dark variants). _(consistency)_
- [ ] ✅ `src/components/PrivacyNotice.tsx` — localStorage.getItem/setItem (lines 12, 20) are unguarded; in Safari private mode or with storage disabled setItem throws, leaving an unhandled exception on dismiss. Wrap in try/catch. _(error-handling)_
- [ ] ✅ `src/components/RichText.tsx` — DOMPurify.sanitize(html, ...) runs on every render (line 11); when many RichText nodes render in a list this re-sanitizes unchanged HTML each time. Memoize on `html`. (Security whitelist itself is solid.) _(efficiency)_
- [ ] ✅ `src/components/RoleChipPicker.tsx` — Toggle chips convey selected state only via color (lines 33-45) with no aria-pressed, so AT users can't tell which roles are active. Add aria-pressed={active} to mirror the visual state. _(accessibility)_
- [ ] ✅ `src/components/SpielplanerOrAdminRoute.tsx` — Good pattern: access logic is extracted into the pure, testable canAccessSpielplanung() helper. SuperAdminRoute/VorstandRoute/AdminRoute repeat the same `isLoading||teamsLoading → null; !role → Navigate` skeleton inline — consider a shared RoleGuard so all guards follow this model. _(consistency)_
- [ ] ✅ `src/components/StatusBadge.tsx` — The `colorMap` prop is @deprecated (line 69) but the legacy inline-style branch (lines 76-86) is still carried; once callers are migrated, delete it to cut the dual code path. _(consistency)_
- [ ] ✅ `src/components/SuperAdminRoute.tsx` — Duplicates the guard skeleton (loading→null, role check→Navigate) shared by VorstandRoute/AdminRoute/SpielplanerOrAdminRoute. Extract a single <RoleGuard allow={...}> to remove the copy-paste. _(consistency)_
- [ ] ✅ `src/components/TabBar.tsx` — Container/buttons carry no tab semantics (no role="tablist"/role="tab"/aria-selected) — AT users can't perceive it as a tab set or which tab is active. Add roles + aria-selected={active===tab.key}. _(accessibility)_
- [ ] ✅ `src/components/TeamFilter.tsx` — Always fetches all active teams with a hardcoded `limit: 50` (line 22) and, when `limitToTeamIds` is set, filters client-side (lines 25-29). If the club ever exceeds 50 active teams the list silently truncates; and the limited-scope case could filter server-side with `{ id: { _in } }`. _(efficiency)_
- [ ] ✅ `src/components/TeamMultiSelect.tsx` — Custom listbox: trigger lacks aria-haspopup/aria-expanded, the dropdown has no role="listbox"/option, and there's no keyboard navigation or Escape-to-close (only mousedown-outside). Not operable by keyboard/AT. _(accessibility)_
- [ ] ✅ `src/components/TopNav.tsx` — useUnreadTotal() runs unconditionally (line 128) even when messagingOn is false, so the unread subscription/fetch fires for users with messaging disabled. Its result is only consumed behind messagingOn — gate the work. _(efficiency)_
- [ ] ✅ `src/components/VorstandRoute.tsx` — Same duplicated guard skeleton as SuperAdminRoute; fold into the shared RoleGuard rather than maintaining N near-identical files. _(consistency)_
- [ ] ✅ `src/components/ui/badge.tsx` — The base class carries `focus:outline-none focus:ring-2 focus:ring-ring focus:ring-offset-2`, but Badge renders a non-interactive `<div>` with no tabIndex and no `asChild`/`as` support, so these focus utilities can never apply — dead classes. Drop them, or add `asChild` so badges used as links/buttons actually get the focus ring. _(consistency)_
- [ ] ✅ `src/components/ui/button.tsx` — When `loading` is true the spinner replaces the icon but there is no `aria-busy` on the element and no accessible "loading" text, so screen-reader users get no signal that the button is working. Add `aria-busy={loading}` (and optionally an sr-only status). _(accessibility)_
- [ ] ✅ `src/components/ui/card.tsx` — `CardTitle` and `CardDescription` render `<div>` elements, losing heading/paragraph semantics. Screen readers get no document outline for card headers. Newer shadcn renders a heading + `<p>`; using an appropriate semantic element (or `role`) improves the a11y tree with no visual change. _(accessibility)_
- [ ] ✅ `src/components/ui/checkbox.tsx` — Import-style and component-style inconsistency across the primitive set: checkbox/collapsible/sheet/tabs import from the unified `radix-ui` package and use plain function components + `data-slot`, while alert-dialog/dialog/dropdown-menu/popover/select/etc. import scoped `@radix-ui/react-*` and use `forwardRef` + `displayName` with no `data-slot`. Two generations of shadcn coexist; standardize to avoid subtle behavioral gaps (ref forwarding, data-slot selectors). _(consistency)_
- [ ] ✅ `src/components/ui/dropdown-menu.tsx` — Uses the older shadcn generation (scoped `@radix-ui/react-dropdown-menu`, forwardRef, no `data-slot`) — consistent internally but part of the two-generation split noted in checkbox.tsx. No functional bug; flagged for the standardization effort. _(consistency)_
- [ ] ✅ `src/components/ui/input.tsx` — Unlike checkbox.tsx (which has `aria-invalid:border-destructive aria-invalid:ring-destructive/20`), Input has no aria-invalid styling hook, so forms can't surface validation state visually/consistently through the standard aria mechanism. Add the aria-invalid variants for parity. Same gap in textarea.tsx. _(accessibility)_
- [ ] ✅ `src/components/ui/label.tsx` — `labelVariants` is a cva call with only a base string and zero variants — it adds a dependency and indirection for no benefit. Replace with a plain string constant (or actual variants if size/tone are ever needed). _(consistency)_
- [ ] ✅ `src/components/ui/sheet.tsx` — sheet.tsx (Radix Dialog, function-component + data-slot style) and drawer.tsx (vaul, forwardRef + displayName style) are two overlapping bottom/side-panel primitives with divergent APIs and modernization levels. Consider consolidating to one to reduce duplication and inconsistent conventions. _(consistency)_
- [ ] ✅ `src/components/ui/textarea.tsx` — Same as input.tsx — no `aria-invalid:*` styling hook for validation state, inconsistent with checkbox.tsx's aria-invalid handling. _(accessibility)_
- [ ] ✅ `src/hooks/useAnnouncements.ts` — The audit-note comment claims audience narrowing is 'client-side only' and that 'the server returns all published announcements and this hook narrows by primarySport', but the code actually builds `sportFilter` into the Directus `_and/_or` filter (server-side). The stale comment misdescribes the security posture — reconcile it (the sport narrowing IS applied server-side; only teams/roles audiences are unhandled/reserved). _(consistency)_
- [ ] ✅ `src/hooks/useAnnouncements.ts` — The fetch catch does `setItems([])` with the comment 'silently fail (collection may not exist yet on dev)' — this also masks real prod failures (permission/500) as an empty feed. Narrow the swallow to the collection-missing case (or log via captureApiError) so a genuine error is not indistinguishable from 'no announcements'. _(error-handling)_
- [ ] ✅ `src/hooks/useBulkParticipationStatuses.ts` — The composite-key (`type:id`) handling for filter, partByKey and the output map is solid; keep it — the only nit is `getStatus` is wrapped in useMemo returning a closure over statusMap, which is fine but could be a plain function since it is cheap and already re-created only when statusMap changes. _(efficiency)_
- [ ] ✅ `src/hooks/useFinance.ts` — The board list hooks (useFinanceInvoices, useFinanceTransactions, useFinanceAccounts, useFinanceImports) all use `all: true` with no `fields` selection, pulling every column of potentially large tables into memory on each dashboard open. Add explicit field lists (as useFinanceMembers/PAYOUT_FIELDS already do) to cut payload/parse cost. _(efficiency)_
- [ ] ✅ `src/hooks/useGooglePlacesSearch.ts` — Suggestion/detail parsing is untyped throughout (`suggestions.slice(0,5).map(async (s: any) ...)`, `(c: any)`, `fallbackResult(place: any)`). Define a minimal response interface for the autocomplete + place-details shapes so field renames in the Places API surface at compile time instead of yielding empty results at runtime. _(type-safety)_
- [ ] ✅ `src/hooks/useGooglePlacesSearch.ts` — The outer catch swallows all non-AbortError failures into `setResults([])` with no signal to the user or Sentry; a mis-restricted/over-quota key (the very risk the file header warns about) silently yields 'no results'. Capture non-abort errors so billing/quota failures are diagnosable. _(error-handling)_
- [ ] ✅ `src/hooks/useHallSearch.ts` — The effect depends on `halls` which is `hallsRaw ?? []` — while the halls query is loading, `?? []` produces a fresh array reference every render, so the filter effect re-runs each render. Memoize the fallback (`const halls = hallsRaw ?? EMPTY` with a module-level const, or `useMemo`) so the effect only runs on real query/data changes. _(efficiency)_
- [ ] ✅ `src/hooks/useNotifications.ts` — `markAllAsRead` awaits `Promise.all(unread.map(update))` and only updates local state on full success; a partial failure (some PATCHes succeed) rejects and the catch 'silently fails', leaving the UI showing everything still unread while the server marked some read. Use allSettled + reconcile, or optimistic-then-rollback like deleteNotification already does. _(error-handling)_
- [ ] ✅ `src/hooks/useParticipation.ts` — Two separate useCollection queries (participations limit 1 + absences limit 5) fire per detail modal; the absence lookup duplicates what useMyCoveringAbsence already encapsulates. Reuse useMyCoveringAbsence here to remove the duplicated absence filter/coverage logic and keep the covering-absence rule in one place. _(consistency)_
- [ ] ✅ `src/hooks/usePushNotifications.ts` — Both callbacks read `t(...)` for toast copy but omit `t` from their dependency arrays (`[state.supported, state.loading]`). After a language switch the memoized callbacks keep the stale translator and would toast in the previous language. Add `t` to deps. _(consistency)_
- [ ] ✅ `src/hooks/useTeamAbsences.ts` — The dedup key uses `teamIds.join(',')` (order-sensitive) whereas the sibling useMultiTeamMembers sorts first (`safeIds.slice().sort().join(',')`). Passing the same teams in a different order triggers a needless refetch here. Sort the ids into the key for consistency. _(consistency)_
- [ ] ✅ `src/hooks/useTeamMembers.ts` — `useMultiTeamMembers` correctly does the single-level junction fetch (`member_teams` filtered by `team: { _in }`) to sidestep the deep-M2M-filter-vs-policy silent-[] trap, but its single-team branch and multi-team branch duplicate nearly identical fetch/dedupe/error logic; collapse into one path (the multi-team dedupe is a no-op for one team) to reduce drift. _(consistency)_
- [ ] ✅ `src/hooks/useUserVisibleEventIds.ts` — The Promise.all pairs the team-junction and member-junction fetches with `Promise.resolve([])` fallbacks (good), but if either rejects the whole load fails and BOTH id arrays are left empty — a failed team-junction fetch also hides the user's directly-invited events. Since they are independent, allSettle them so a direct-invite list still resolves when the team-scope query fails. _(error-handling)_
- [ ] ✅ `src/lib/query.tsx` — useItem (l.167-172) passes `id!` non-null assertions into both queryKey (keys.detail(collection, id!)) and queryFn even though the hook explicitly supports null/undefined id and gates via `enabled: id != null`. The assertions mask the nullable contract from the type checker; use a sentinel or narrow inside the guard instead of `!`. _(type-safety)_
- [ ] ✅ `src/lib/query.tsx` — Copy-paste artifact: the JSDoc line 'Fetch items from a Directus collection with automatic caching.' is duplicated (l.75-76) above useCollection. Trivial cleanup. _(consistency)_
- [ ] ✅ `src/lib/sentry.ts` — API_BASE (l.453-455) re-derives the Directus host from window.location independently of api.ts's API_URL, and omits api.ts's localhost→dev-always and prod-proxy handling — a second, subtly different copy of the same host-resolution logic (kept separate to avoid a sentry→api circular import). Extract the URL resolver into its own tiny module both can import. _(consistency)_
- [ ] ✅ `src/modules/absences/AbsenceCard.tsx` — The edit/delete action buttons use min-h-[36px], below the ≥44px mobile touch-target size the CLAUDE.md mobile rules require for row action toggles. _(ui-polish)_
- [ ] ✅ `src/modules/absences/AbsenceForm.tsx` — The member-picker fetch block (allMembersRaw useCollection + useMultiTeamMembers + visibleMembers derivation with the same filter/sort) is duplicated verbatim in WeeklyUnavailabilityForm.tsx. Extract a shared useAbsenceMemberPicker(showMemberPicker, teamIds) hook. _(consistency)_
- [ ] ✅ `src/modules/absences/AbsenceImportModal.tsx` — handleImport inserts each valid row with a sequential `await createRecord` in a loop; a large import serializes N round-trips. Batch (Promise.all with concurrency, or a bulk create) while still counting per-row failures. _(efficiency)_
- [ ] ✅ `src/modules/absences/AbsencesPage.tsx` — `today = new Date().toISOString().slice(0,10)` computes today in UTC, but the codebase has toISODate() (Zurich) which TeamAbsenceView already imports; near midnight in CH the upcoming/past absence split (and TeamWeeklySection range) can be off by a day. Use toISODate(new Date()). _(consistency)_
- [ ] ✅ `src/modules/admin/AnimatedUIPage.tsx` — File opens with `// @ts-nocheck`, disabling TypeScript for the entire 625-line page so any prop/type regressions in the showcased components go uncaught. If it must stay, scope the suppression to the offending lines instead of the whole file. _(type-safety)_
- [ ] ✅ `src/modules/admin/AnmeldungenPage.tsx` — buildClubDeskCSV re-implements CSV escaping (it does correctly guard formula-injection via csvEscape) but duplicates logic that already exists in admin/utils/exportResults.ts (toCSV). Reusing the shared helper removes a second escaping code path to maintain. _(consistency)_
- [ ] ✅ `src/modules/admin/ClubStatsPage.tsx` — In participation/results/delegations the `groupBySport(...).map(group => (<> ...</>))` wraps each group in a keyless Fragment shorthand (keys are only on inner SportHeading/rows), producing a React 'unique key' warning. Use <Fragment key={group.sport}>. _(consistency)_
- [ ] ✅ `src/modules/admin/components/ChartView.tsx` — colMeta (isNumeric/isDateColumn over every column × every row) is recomputed in detectChartType and again inside each Bar/Line/Pie subcomponent on every render; compute once and pass down. _(efficiency)_
- [ ] ✅ `src/modules/admin/components/ExplorerDetail.tsx` — The `sectionLabelKey` Record<SectionKey,string> literal is duplicated verbatim in renderMember, renderTraining and renderGame — hoist to one module-level constant. _(consistency)_
- [ ] ✅ `src/modules/admin/components/TableBrowser.tsx` — Row selection is derived by DOM traversal (target.closest('tr') + indexOf into parent children) and cast `records[idx] as never`; fragile against any header-in-tbody or extra rows in ResultsTable. Pass an onRowClick(record) from ResultsTable instead. _(type-safety)_
- [ ] ✅ `src/modules/admin/hooks/useRelatedEntities.ts` — The load() re-entry guard `if (cache[key]?.data.length || cache[key]?.loading) return` treats a legitimately empty result (data.length===0) as 'not cached', so a section that truly has no rows re-fetches on every re-expand. Track a `loaded` flag instead of relying on data.length. _(efficiency)_
- [ ] ✅ `src/modules/auth/ProfilePage.tsx` — handleCancelRequest swallows failures silently (`catch { // ignore }`) — a failed team-request cancel gives no feedback and the row stays. Surface via toast like handleLeaveTeam does. _(error-handling)_
- [ ] ✅ `src/modules/auth/ProfilePage.tsx` — AutoSignInCard and EmailNotificationCard seed toggle state from `user` only in the useState initializer; if useAuth refreshes the user with new server values (e.g. after a save elsewhere) the toggles won't resync until remount. _(efficiency)_
- [ ] ✅ `src/modules/auth/SignUpPage.tsx` — handleCompleteProfile creates team_requests one-by-one in a sequential `for...await` loop; batch with Promise.all so multi-team claims aren't serialized network round-trips. _(efficiency)_
- [ ] ✅ `src/modules/calendar/CalendarFilters.tsx` — Source colors are hardcoded hex here (`{ bg: '#4A55A2', ... }`, lines 25-32) while the grids color the same source types with Tailwind classes (barColors/dotColors). Two independent color sources of truth for the identical entry taxonomy — a palette change updates one and silently diverges. Derive both from a single shared map. _(consistency)_
- [ ] ✅ `src/modules/calendar/CalendarPage.tsx` — CalendarPage's TypeIcon uses hand-rolled inline SVG paths for training/event/closure/absence while the grids use lucide-react (TrafficCone/Star/CircleX/CalendarOff) for the same concepts — the two icon sets don't match visually. Standardize on the lucide set. _(ui-polish)_
- [ ] ✅ `src/modules/calendar/HallenplanView.tsx` — freedSlotInfos builds dateStr manually as `${slotDate.getDate()}.${slotDate.getMonth() + 1}.` (line 136) — no zero-padding ('5.3.' not '05.03.') and bypasses the central formatDateCompactZurich helper. Use the helper for a consistent Swiss compact date. _(consistency)_
- [ ] ✅ `src/modules/calendar/HallenplanView.tsx` — teamIdOf() (line 29) reimplements the id-from-relation coercion that utils/relations already provides (relId/asObj). Reuse the shared helper. _(consistency)_
- [ ] ✅ `src/modules/calendar/components/MobileMonthView.tsx` — Shares the duplicated dotColors/colorKey maps flagged on MonthGrid — consolidate into the shared entry-style module. _(consistency)_
- [ ] ✅ `src/modules/calendar/hooks/useCalendarData.ts` — gameToEntry and scorerDutyToEntry are near-identical (same title/date/time/hall mapping, differing only in id prefix, type and teamNames). Fold into one builder with a `duty` flag to remove the divergence risk. _(consistency)_
- [ ] ✅ `src/modules/carpool/CarpoolCard.tsx` — `departure_time` is rendered raw (line 53) instead of via formatTimeZurich — if the stored value ever carries seconds ('14:30:00') it shows them, and it bypasses the Swiss 24h helper the rest of the app uses. Route it through the shared time formatter. _(consistency)_
- [ ] ✅ `src/modules/carpool/CarpoolCard.tsx` — driverId/isPassenger use `(carpool.driver as unknown as { id: string }).id` and `(p.passenger as unknown as { id: string }).id` double-casts to cope with maybe-expanded relations; once the hook expands the relation with a typed shape these casts (and the `DriverInfo`/`PassengerInfo` ad-hoc types) can be dropped. _(type-safety)_
- [ ] ✅ `src/modules/carpool/CarpoolOfferForm.tsx` — Same fire-and-forget pattern as PollForm: handleSubmit calls onSubmit then resets + onClose synchronously (lines 31-42), so an offerRide/createCarpool failure closes the dialog and loses input with no error shown. Await success before closing. _(error-handling)_
- [ ] ✅ `src/modules/carpool/CarpoolSection.tsx` — Loading state renders a literal `...` (line 52), matching the same anti-pattern in PollsSection. Use LoadingSpinner for a consistent affordance. _(ui-polish)_
- [ ] ✅ `src/modules/carpool/hooks/useCarpool.ts` — joinRide computes 'full' from stale closure state (`passengers.filter(...).length + 1 >= seats`) and both join/leave read the unfiltered `carpools` closure even though the returned list is filtered to non-cancelled — two simultaneous joins can both miss the full transition. Recompute from fresh data or let a server trigger own the status. _(error-handling)_
- [ ] ✅ `src/modules/events/EventCard.tsx` — asTeams/teamId use `(t: any)` and `(val as any)?.teams_id` casts; the CLAUDE.md M2M-extraction pattern can be typed against the junction shape (`{ teams_id?: Team | number }`) to drop the `any`. _(type-safety)_
- [ ] ✅ `src/modules/events/EventDetailModal.tsx` — Same duplicated asTeams()/teamId()/eventTypeColors/isHtml() helpers as EventCard/EventsPage — consolidate with the shared events util noted on EventCard. _(consistency)_
- [ ] ✅ `src/modules/events/EventForm.tsx` — syncSessions() (lines 706-737) and the whole-mode cleanup delete/create/update sessions one-by-one with sequential awaits inside for-loops. For multi-day events with many sessions this serializes N round-trips; batch independent deletes/creates with Promise.all. _(efficiency)_
- [ ] ✅ `src/modules/events/EventForm.tsx` — The teams/invited_members extraction in the edit-prefill effect (lines 136-153) uses `(t: any)`/`(m: any)` casts that re-implement the junction-ID extraction pattern; reuse a shared typed helper to avoid drift with the read-side asTeams. _(type-safety)_
- [ ] ✅ `src/modules/events/EventsPage.tsx` — useRealtime('events', () => refetch()) is registered, yet handleFormSave/handleDelete also call refetch() manually — every mutate triggers two fetches. Let realtime own the refetch (or debounce) to avoid the redundant round-trip. _(efficiency)_
- [ ] ✅ `src/modules/finance/DuesRunManager.tsx` — cancelRun/removeRate swallow errors into a generic message or `/* noop */` (removeRate has empty catch), so a failed rate delete leaves the row on screen with no feedback. Surface the API error like addRate does. _(error-handling)_
- [ ] ✅ `src/modules/finance/DunningConsole.tsx` — toggleNeverDun() has try/finally with no catch, so a failed setMemberNeverDun silently leaves the badge unchanged with no feedback. Add a catch surfacing apiErr like EscalateModal does. _(error-handling)_
- [ ] ✅ `src/modules/finance/FinancePage.tsx` — SyncNowButton.go() runs an unbounded in-render polling loop (for(;;) with setTimeout-await for up to 240s). If the user leaves the Sync tab or unmounts mid-poll it keeps polling and will setState after unmount. Add an AbortController/mounted guard and clear on unmount. _(error-handling)_
- [ ] ✅ `src/modules/finance/TeamFinance.tsx` — remove() uses window.confirm and its try/finally has no catch — a failed deleteTeamEntry silently leaves the row (busyDel resets, no error shown). Add a catch surfacing apiErr, matching the add-entry path. _(error-handling)_
- [ ] ✅ `src/modules/fines/FinesDashboardCard.tsx` — Clean and efficient (single windowed fetch split client-side, memoized stats, self-hides when empty). No change needed. _(consistency)_
- [ ] ✅ `src/modules/fines/FinesPage.tsx` — `memberMap`/`teamMap` are rebuilt with `new Map(...)` on every render (lines 103-104) instead of useMemo — minor given small sets. _(efficiency)_
- [ ] ✅ `src/modules/fines/FinesSettings.tsx` — `const [window, setWindow]` (line 86) shadows the global `window` object throughout the component — rename to `resetWindow` to avoid the footgun. _(type-safety)_
- [ ] ✅ `src/modules/forms/FormFillModal.tsx` — isMissing() is duplicated byte-for-byte in PublicFormPage.tsx; and the 'required field missing' error uses the base `missing.label` rather than the locale-resolved label (resolveFieldLabel) shown in the field itself. Share the predicate and localize the error field name. _(consistency)_
- [ ] ✅ `src/modules/gameScheduling/hooks/useAdminBookings.ts` — vmPush() schedules setTimeout(() => fetchAll(), 6000) that is never cleared; if the dashboard unmounts within 6s it calls fetchAll and setState on an unmounted hook. Track the timer and clear it on unmount (or guard with latestKeyRef). _(error-handling)_
- [ ] ✅ `src/modules/gameScheduling/hooks/useMailbox.ts` — messagesForOpponentThread() runs the full classifyMessages() pass (union-find over all messages) on every call; if invoked per opponent row it becomes O(rows × messages). Callers rendering many rows should build the classification once (classifyMessages) and use the precomputed messagesForOwner — worth a doc note or a memoized wrapper to prevent the O(n²) trap. _(efficiency)_
- [ ] ✅ `src/modules/games/GamesPage.tsx` — The upcoming (lines 356-388) and results (409-441) sections are near-identical IIFEs that split games into league/cup sections and render GameCard — extract a shared `renderGameSections(items, variant)` helper to kill the duplication. _(consistency)_
- [ ] ✅ `src/modules/games/GamesPage.tsx` — `(g as any).kscw_team` at line 249 casts away the type just to read `.sport`; use the existing `asObj<Team>()` relations helper (as GameCard/GameDetailModal do) instead of `any`. _(type-safety)_
- [ ] ✅ `src/modules/games/components/GameCard.tsx` — The roster/edit/delete action buttons use hand-inlined SVG paths (lines 313-337) while the same file imports lucide icons; swap to lucide `Users`/`Pencil`/`Trash2` for consistency, and add `aria-label` (they only have `title`). _(accessibility)_
- [ ] ✅ `src/modules/games/components/GameCoachDashboard.tsx` — This is a near-clone of trainings/CoachDashboard.tsx (same range-persist/reset/AttendanceTable scaffold); consolidate into one parameterized dashboard shell so range-persistence logic stays in sync. _(consistency)_
- [ ] ✅ `src/modules/games/components/GameDetailModal.tsx` — The dialog has `role=dialog`/`aria-modal` and an Escape handler but no focus trap or initial-focus management, and the close X is a hand-inlined SVG despite `X` from lucide being imported at the top. _(accessibility)_
- [ ] ✅ `src/modules/games/components/KscwScoreboard.tsx` — Per-metric rankings (`computeMetricRankingPerGame`/`computeMetricRanking`/totals) are recomputed inside the JSX map on every render (lines 162-167); `sections` is memoized but these derived rows are not — memoize them keyed by (mode, section) to avoid recompute on unrelated state (e.g. expand/collapse). _(efficiency)_
- [ ] ✅ `src/modules/games/components/RefereeExpenseSection.tsx` — Correctly expands `coach.members_id.*`/`team_responsible.members_id.*` (M2M rule) and handles the FormData/uploadFile pitfall — solid. Minor: the cancel `✕` button (line 256) has no `aria-label`. _(accessibility)_
- [ ] ✅ `src/modules/games/components/useGameAttendanceStats.ts` — This hook and trainings/useAttendanceStats.ts share the same members→games/trainings→participations→absences fetch + classify pipeline; consider a shared core so the two attendance engines can't diverge. _(consistency)_
- [ ] ✅ `src/modules/guide/TourProvider.tsx` — saveState() calls localStorage.setItem without a try/catch even though loadState() is guarded; in private-mode/quota-exceeded this throws inside the persistence useEffect and can break the render. Wrap it defensively like loadState. _(error-handling)_
- [ ] ✅ `src/modules/hallenplan/HallenplanPage.tsx` — Clean orchestration (debounced realtime refetch with unmount cleanup, memoized derived slot sets). No material issue found; the only nit is that `refetch` passed to child modals inherits the useHallenplanData gap above (games/hall_events not refreshed). _(consistency)_
- [ ] ✅ `src/modules/hallenplan/hooks/useHallenplanData.ts` — The exposed `refetch` (lines 188-193) only re-fetches slots/closures/claims/trainings — it omits games and hall_events, so a realtime-driven refetch can leave stale game/GCal-derived virtual slots. Include the games/hall_events refetchers (or document why they're excluded). _(consistency)_
- [ ] ✅ `src/modules/hallenplan/utils/virtualSlots.ts` — `mergeVirtualSlots` is a single ~340-line function doing 8+ sequential passes (suppress-by-training, by-away-game, by-home-game shortening, by-closure-event, by-BB-event, freed-from-away, annotate, freed-recurring). It's carefully commented but hard to test/modify in isolation — split each pass into a named pure helper with its own unit test. _(consistency)_
- [ ] ✅ `src/modules/home/HomePage.tsx` — EventRow (line 1316) extracts teams inline with `(t: any) => t?.teams_id ?? t` — the exact logic that events/*.tsx export as `asTeams()`. Reuse a shared typed helper instead of a fresh `any`-cast so the M2M junction shape is validated in one place. _(type-safety)_
- [ ] ✅ `src/modules/home/components/AnnouncementDetailModal.tsx` — The hero `<img>` (line 45) has no width/height or `loading`/`decoding` hints, so opening the modal causes a layout jump while the 1200px webp loads. Add explicit aspect ratio or `loading="lazy"`/skeleton. _(ui-polish)_
- [ ] ✅ `src/modules/home/components/AnnouncementRow.tsx` — The thumbnail-or-megaphone + pin + truncated-excerpt markup is duplicated by NewsArchivePage's AnnouncementTableRow. A shared `<AnnouncementRowContent>` presentational piece would keep the two feeds visually in sync when styling changes. _(consistency)_
- [ ] ✅ `src/modules/messaging/pages/MessagingSettingsPage.tsx` — toggle() casts the settings patch with `as any` (`messagingApi.updateSettings({ [key]: next } as any)`). Type the key union and give SettingsBody an index-safe partial shape so the computed-key write stays type-checked. _(type-safety)_
- [ ] ✅ `src/modules/polls/HomePollsCard.tsx` — The card-shell markup (rounded border + icon + title + count badge + divided list) is nearly identical to the 'Forms to fill' block inlined in HomePage.tsx (lines 536-561). A shared `<HomeSectionCard>` wrapper would dedupe the two and guarantee they stay visually aligned. _(consistency)_
- [ ] ✅ `src/modules/polls/PollForm.tsx` — The option text inputs (line 101) have only a `placeholder` ("Option 1"), no associated `<label>` or aria-label, so screen readers announce them as unlabeled. Question and deadline are correctly labeled; add sr-only labels to the option rows. _(accessibility)_
- [ ] ✅ `src/modules/polls/PollsSection.tsx` — The loading state renders a literal `...` string (line 48) — same placeholder appears in carpool/CarpoolSection.tsx line 52. Use a real spinner/skeleton (LoadingSpinner is already used elsewhere) for a consistent loading affordance. _(ui-polish)_
- [ ] ✅ `src/modules/polls/hooks/usePoll.ts` — usePolls and useActivePolls duplicate the same useMutation('polls') + useRealtime + close/delete-with-refetch scaffolding; and getResults() is a plain function recomputed on every PollCard render. Consolidate the shared mutation logic and memoize getResults. _(efficiency)_
- [ ] ✅ `src/modules/scorer/components/AssignmentEditor.tsx` — Well-built (aria-labels on selects, dark-mode via shadcn Select, 44px targets). Minor: `filteredMembers` uses `i18n.language` in its localeCompare sort but omits it from the useMemo deps (line 91), so the sort order won't refresh on a language switch until another dep changes. _(efficiency)_
- [ ] ✅ `src/modules/scorer/components/ScorerRow.tsx` — The self-assign confirm popup (line 508) is `role=dialog aria-modal` but has no Escape handler or focus trap (unlike GameDetailModal which at least handles Escape). _(accessibility)_
- [ ] ✅ `src/modules/scorer/components/TeamOverview.tsx` — `teamDuties`/final sort use `i18n.language` (line 94) but the memo deps (line 95) omit it, and `getDateFormatter` again derives locale from i18n.language rather than pinning de-CH. Clean otherwise. _(consistency)_
- [ ] ✅ `src/modules/scorer/hooks/useOfficialContacts.ts` — Clean and correct (enabled-gating, 60s poll with cancel+clearInterval cleanup, EMPTY fallback when disabled). No change needed. _(consistency)_
- [ ] ✅ `src/modules/scorer/hooks/useScorerDelegations.ts` — Clean (memoized derived lists, realtime refetch, logActivity on writes). Only nit: `createRecord<{id}>` result is cast `as unknown as ScorerDelegation` (line 64) — the created row lacks the full delegation fields, so callers reading anything beyond `id`/`same_team` off the returned object would get undefined. _(type-safety)_
- [ ] ✅ `src/modules/spielplanung/ManualGameModal.tsx` — auto_confirm_rsvp is read via an ad-hoc inline cast `(editingGame as Game & { auto_confirm_rsvp?: boolean | null })` because the Game type lacks the field. Add auto_confirm_rsvp to the Game type so the modal (and any other reader) stays type-safe instead of casting. _(type-safety)_
- [ ] ✅ `src/modules/spielplanung/SpielplanungPage.tsx` — `const filteredEntries = useMemo(() => entries, [entries])` is a no-op memo (returns its input unchanged) — dead code left from a removed filter; drop it and use entries directly. _(efficiency)_
- [ ] ✅ `src/modules/spielplanung/hooks/useSpielplanungData.ts` — The games query selects `fields: ['*', 'kscw_team.*', 'hall.*']`, pulling every column of games plus full team+hall objects for the whole season when the calendar entry only needs date/time/home_team/away_team/type/league/round/status + team.name/sport + hall.name. Narrow the field list to cut payload size on a season-wide fetch. _(efficiency)_
- [ ] ✅ `src/modules/tasks/hooks/useTasks.ts` — Every mutation (add/update/remove/claim/toggle) calls refetch() while useRealtime('tasks') also refetches on the emitted event → duplicate refetch on the actor's own mutations. _(efficiency)_
- [ ] ✅ `src/modules/teams/InviteExternalUserModal.tsx` — `catch (err: any)` (line 46) — type the caught error as `unknown` and narrow, matching the `err instanceof Error` pattern used elsewhere. `handleCopy` also fails silently (no toast) when clipboard write throws. _(type-safety)_
- [ ] ✅ `src/modules/teams/ManageStaffModal.tsx` — Solid (correct M2M junction-object writes, toasts, ConfirmDialog, 44px targets). Minor: `fetchAllItems('members', ...)` on open pulls all ~1144 member rows (intentionally unfiltered for name resolution) — acceptable but could scope to current staff ids + a search endpoint if the modal is opened often. _(efficiency)_
- [ ] ✅ `src/modules/teams/MemberRow.tsx` — The member photo `<img>` opens the lightbox via `onClick` (line 143) with no keyboard handler/role, so it's not reachable via keyboard. _(accessibility)_
- [ ] ✅ `src/modules/teams/RosterEditor.tsx` — The debounce timers in `handleUrlChange` (social/facebook/tiktok) and `DebouncedNumberInput` are never cleared on unmount — a pending `setTimeout(..., 500)` fires the update after the component leaves; store and clear them in a cleanup effect. _(efficiency)_
- [ ] ✅ `src/modules/teams/RosterEditor.tsx` — The optimistic rollback `delete next[memberId]?.number` (saveNumber) / `.position` (savePosition) mutates the shared nested override object rather than a copy — `next` is a shallow clone but `next[memberId]` is the same reference as in prev state; clone the inner object before deleting. _(type-safety)_
- [ ] ✅ `src/modules/trainings/CoachDashboard.tsx` — Near-duplicate of games/GameCoachDashboard.tsx (range persist/reset + AttendanceTable). `defaultFrom`/`defaultTo`/`today` are recomputed each render and passed into effect deps — memoize (they're value-stable so it's benign, but the shared shell should be extracted). _(consistency)_
- [ ] ✅ `src/modules/trainings/TrainingCard.tsx` — Roster/edit/delete actions are hand-inlined SVGs with only `title` (no `aria-label`); reuse lucide icons and add accessible names. _(accessibility)_
- [ ] ✅ `src/modules/trainings/TrainingForm.tsx` — If a sibling update throws mid-loop, earlier siblings are already updated but the catch only shows a generic 'errorSaving' — leaving a partially-applied recurring edit with no indication of which succeeded. _(error-handling)_
- [ ] ✅ `src/modules/trainings/TrainingsPage.tsx` — Well structured (single batch fetch, memoized maps). Minor: the empty-state basketball/volleyball icon is a hand-inlined SVG (lines 214-219) while the rest of the app uses the VolleyballIcon/BasketballIcon components; reuse those. _(consistency)_
- [ ] ✅ `src/utils/dateHelpers.ts` — formatDateShortZurich (l.89-98) and its alias formatDateShort (l.198-200) emit US `MM/DD` (comment: 'matches legacy formatDateShort en-US format'), violating the app-wide Swiss dd.mm rule — and grep shows they have NO non-test importers (components define their own local formatDateShort). This is dead code that also encodes the wrong format; remove it (or, if resurrected, flip to dd.mm). _(consistency)_
- [ ] ✅ `src/utils/dateHelpers.ts` — parseRespondByTime (l.353) and getDeadlineDate (l.363) re-inline the `respondBy.includes('T') ? … : replace(' ','T')+'Z'` parse instead of calling the module's own parseFlexible, so they miss its Safari-safe bare-date branch and duplicate parse logic that is supposed to be centralized. Reuse parseFlexible. _(consistency)_
- [ ] ✅ `src/utils/fileUrl.ts` — getFileUrl (l.7) is a passthrough to assetUrl that ignores its first two params (_collection, _recordId, kept 'for backward compat'). It's still imported in ~4 team components purely to pass dead arguments; callers could use assetUrl(fileId) directly and this file could be deleted, removing the misleading signature. _(consistency)_
- [ ] ✅ `src/utils/formatNumber.ts` — formatNumberSwiss hand-rolls the `\B(?=(\d{3})+(?!\d))` apostrophe-grouping regex that amount.ts's formatAmountCH (l.18) also hand-rolls; extract a single groupThousands(intString) helper so the Swiss grouping rule lives in one spot. _(consistency)_
- [ ] ✅ `src/utils/gameHalls.ts` — normalizeRelId (l.3-9) is a near-verbatim duplicate of relId in utils/relations.ts (both coerce a relation value or {id} object to a string ID). Two relation-ID extractors will drift; consolidate on the shared relId. _(consistency)_
- [ ] ✅ `src/utils/rosterExport.ts` — canvas.getContext('2d')! (l.211, l.221) uses non-null assertions; on a context-allocation failure (large multi-page roster on a memory-constrained mobile browser) this throws a raw `Cannot read properties of null` TypeError instead of the friendly ExportLibraryError this file otherwise raises. Guard the context and throw an actionable error. _(error-handling)_

### 🔒 Gated — need approval (48)

- [ ] 🔒 `src/components/FilterChips.tsx` — The individual chips set `aria-pressed`, but the bulk All/None toggle button does not, and its label logic `allSelected ? t('all') : noneSelected ? t('none') : t('all')` shows 'All' even in the partial-selection state — misleading (the title says 'Select none' while the label says 'All'). Add aria-pressed and give the partial state its own label. _(accessibility · GATED: Changes the visible button label in the partial state and needs a new i18n string.)_
- [ ] 🔒 `src/components/ImageLightbox.tsx` — The close button's `aria-label="Close"` is hardcoded English, and the `<dialog>` itself has no accessible name tying it to the image. Translate the close label (via i18n) and give the dialog an aria-label referencing the image alt. _(accessibility · GATED: Adds a new i18n string across 5 locales.)_
- [ ] 🔒 `src/components/LocationCombobox.tsx` — The club-hall accent bar hardcodes `bg-[#FFC832]` instead of the `gold-*` brand token used everywhere else (e.g. FilterChips/CalendarGrid `gold-400`). Swap to the semantic gold token for consistency. _(consistency · GATED: The token may not resolve to the exact same hex, so the rendered shade could shift slightly.)_
- [ ] 🔒 `src/components/MoreSheet.tsx` — The notifications row's unread badge (`unreadNotifications`) sits on the Bell button with no accessible text, so AT users can't tell there are unread notifications. Add an sr-only count label. _(accessibility · GATED: Adds a new user-facing (AT) i18n string across 5 locales.)_
- [ ] 🔒 `src/components/NotificationBell.tsx` — aria-label is built from hardcoded English (`Notifications`, ` (N unread)`) at line 15 — screen-reader users on DE/FR/IT/GSW hear English. Move to i18n (e.g. t('notifications', { count })). _(accessibility · GATED: Changes announced text per locale (user-facing string).)_
- [ ] 🔒 `src/components/OtpInput.tsx` — Digit inputs use hardcoded English aria-labels (`Digit N`, line 156) and there's no wrapping role="group"/aria-label describing the OTP field as a whole; localize and group for AT users. _(accessibility · GATED: Localizing aria-labels changes announced text per locale.)_
- [ ] 🔒 `src/components/RichTextEditor.tsx` — Every toolbar button has a hardcoded English aria-label (`Bold`, `Italic`, `Heading 2`, `Bullet list`, `Link`, lines 89-99) — no localization for AT users. Route through the existing i18n setup (it already imports useTranslation). _(accessibility · GATED: Localizing aria-labels changes announced text per locale.)_
- [ ] 🔒 `src/components/SchedulingLayout.tsx` — The theme toggle aria-label is static `t('darkMode','Dark mode')` (line 138) regardless of current theme, so it never describes the action (switch to light vs dark). Make it reflect the target state. _(accessibility · GATED: Changes the announced label text (user-facing).)_
- [ ] 🔒 `src/components/SetPasswordForm.tsx` — On failure the raw server/error message is shown verbatim (lines 43-45) — Directus/API errors are English and may be unfriendly. Fall back to a localized generic message for non-validation errors. _(error-handling · GATED: Changes user-facing error text.)_
- [ ] 🔒 `src/components/SportToggle.tsx` — The 'all' option's aria-label is the hardcoded English `All sports` (OPTIONS line 18); the buttons are icon-only so this is the sole accessible name. Localize it (VB/BB brand names are fine). _(accessibility · GATED: Localizing the accessible name changes announced text per locale.)_
- [ ] 🔒 `src/components/SwitchToggle.tsx` — The track color is `bg-gray-300 dark:bg-gray-600` in both states (line 45) — the only on/off cue is knob position + icon, giving weak state affordance and no color contrast between on/off. Add an enabled-state track color. _(ui-polish · GATED: Changes the on-state visual appearance.)_
- [ ] 🔒 `src/components/TeamChip.tsx` — Background/text/border come from getTeamColor via inline style (lines 20-26) with no dark-mode adaptation; fixed light-palette team colors can fall to low contrast on the dark background. Verify/adjust contrast (or provide dark variants) for dark mode. _(accessibility · GATED: Changes chip colors, a visual/user-facing change.)_
- [ ] 🔒 `src/components/TopNav.tsx` — The dark-mode SwitchToggle is given a hardcoded `ariaLabel="Toggle dark mode"` (line 260) while everything nearby uses t(); localize it. _(accessibility · GATED: Localizing the accessible name changes announced text per locale.)_
- [ ] 🔒 `src/components/ViewToggle.tsx` — The mobile <select> correctly has dark:bg-gray-800 and the desktop control uses aria-pressed, but the select's aria-label is the hardcoded English `View` (line 21). Localize it. _(accessibility · GATED: Localizing the accessible name changes announced text per locale.)_
- [ ] 🔒 `src/components/ui/alert-dialog.tsx` — `AlertDialogAction` always applies `buttonVariants()` (default = primary). Confirmation dialogs are most often destructive (delete/deactivate), yet the action button defaults to primary styling and each caller must remember to pass a destructive variant. Consider a destructive-aware default or a documented `variant` prop so dangerous confirmations aren't shipped with a benign-looking primary button. _(ui-polish · GATED: Would change the default appearance of confirm buttons.)_
- [ ] 🔒 `src/components/ui/card.tsx` — `CardContent` sets no `data-slot="card-content"`, but calendar.tsx relies on `[[data-slot=card-content]_&]:bg-transparent` — so that conditional is dead (see popover.tsx finding). Emit the data-slot or drop the dead calendar selector. _(consistency · GATED: Adding data-slot changes calendar rendering inside cards.)_
- [ ] 🔒 `src/components/ui/command.tsx` — `CommandItem` uses `cursor-default`, so selectable command/search results don't show a pointer cursor on hover — inconsistent with SearchableSelect's `cursor-pointer` option rows. Minor polish for perceived interactivity. _(ui-polish · GATED: Changes the hover cursor (a visible interaction detail).)_
- [ ] 🔒 `src/components/ui/dialog.tsx` — The close button's accessible label is a hardcoded English `<span className="sr-only">Close</span>`. In a 5-locale app this is announced as "Close" to every screen-reader user regardless of language. Route it through i18n (t('close')). Same hardcode exists in sheet.tsx. _(accessibility · GATED: Changes the announced accessible name per locale (user-facing for AT users).)_
- [ ] 🔒 `src/components/ui/sheet.tsx` — Hardcoded English `<span className="sr-only">Close</span>` accessible label (same issue as dialog.tsx) — not localized across the 5 locales. _(accessibility · GATED: Changes the announced accessible name per locale.)_
- [ ] 🔒 `src/components/ui/switch.tsx` — The switch is `h-5 w-9` (20x36px). The interactive target is far below the 44px minimum used elsewhere; on mobile it's a small tap target. Consider a larger hit area (padding/`before:` expansion) without necessarily enlarging the visual track. _(accessibility · GATED: Changes the switch's rendered/hit size.)_
- [ ] 🔒 `src/components/ui/tabs.tsx` — Inactive tab triggers use `text-foreground/60` (and `dark:text-muted-foreground`). 60% foreground opacity on the muted background risks failing WCAG AA contrast for the inactive label text. Verify contrast and consider a token with a guaranteed ratio. _(accessibility · GATED: Adjusting the inactive-tab color is a visible change.)_
- [ ] 🔒 `src/hooks/useBulkParticipationStatuses.ts` — Absences are fetched with a hard `limit: 50`; a user with many overlapping absence rows across the date window could have coverage silently dropped past row 50, mis-painting some rows as not-declined. Use `all: true` (the row count per user is small and bounded) or raise the cap with a comment on the bound. _(error-handling · GATED: Changes the query result set, which can change displayed RSVP/absence badges in edge cases)_
- [ ] 🔒 `src/hooks/useMutation.ts` — `logActivity(...)` is called fire-and-forget on every create/update/remove for all collections except user_logs; it is safe (never throws, self-catches) but doubles write traffic (an extra user_logs insert per mutation). Confirm this is intended for high-frequency collections like participations, or gate logging to state-changing domain collections only. _(efficiency · GATED: Suppressing activity-log rows changes the superadmin audit-log contents)_
- [ ] 🔒 `src/modules/absences/AbsenceImportModal.tsx` — The preview uses a raw <table> for a list of data records (import rows) rather than shadcn <Table>; and on partial failure the modal never calls onComplete so the parent list only refreshes via best-effort realtime. _(consistency · GATED: Table swap changes visual output; wiring onComplete changes refresh behavior.)_
- [ ] 🔒 `src/modules/admin/AnnouncementsPage.tsx` — aria-labels 'Edit'/'Delete'/'Remove image'/'Filled' and the link placeholder 'https://… oder /pfad' are hardcoded (German/English) instead of i18n; non-DE/EN users get an untranslated assistive/placeholder layer. _(consistency · GATED: Translating aria-labels/placeholders changes assistive-tech and placeholder text for other locales.)_
- [ ] 🔒 `src/modules/admin/BugfixDashboardPage.tsx` — A few user-facing strings bypass i18n while the rest of the page is translated: the 'Refresh' button, the 'Deployed' status text, and 'Role:'/'Sport:' labels in the expanded detail. _(consistency · GATED: Translating changes displayed text for non-EN locales.)_
- [ ] 🔒 `src/modules/admin/InfraHealthPage.tsx` — The slow-queries block uses a raw <table>, and many detail/section strings are hardcoded English ('VPS Resources', 'CORS (cross-origin)', 'Unreachable', 'Migrations applied', 'Uptime'/'CPU Load'/'Memory'/'Disk') while sibling cards use t(). Route through <Table> + i18n for consistency. _(consistency · GATED: Table markup swap + translating labels changes rendered output.)_
- [ ] 🔒 `src/modules/admin/components/ChartView.tsx` — All labels are hardcoded English — chart-type captions ('Bar chart'/'Line chart'/'Pie chart'), 'No numeric data to chart', 'row/rows', and the svg aria-labels — none go through i18n. _(consistency · GATED: Translating changes rendered/announced text for non-EN users.)_
- [ ] 🔒 `src/modules/calendar/components/MobileMonthView.tsx` — The expanded-day header uses `formatDate(selectedDay, 'EEEE, MMMM d')` (line 235) — US month-then-day order; align with the Swiss day-first convention used elsewhere. _(ui-polish · GATED: Changes the visible date format in the day panel header.)_
- [ ] 🔒 `src/modules/calendar/components/MobileWeekGrid.tsx` — Header label `formatDate(days[0], 'MMM d')` (line 256) is US month-first; use Swiss day-first format. _(ui-polish · GATED: Changes the visible date format in the header.)_
- [ ] 🔒 `src/modules/fines/FinesPage.tsx` — `handleMarkPaid` only `console.error`s on failure (line 127) — the row stays 'open' with no explanation; add a toast. Uses `<Table>` and dark-mode selects correctly. _(error-handling · GATED: adds a user-facing failure toast)_
- [ ] 🔒 `src/modules/fines/IssueFineModal.tsx` — Clean (dark-mode select, quote auto-fill respecting manual override, proper error surface). Minor: the amount-validation error message is built by string-concatenating a label + '…' (line 90) rather than a dedicated i18n validation key. _(consistency · GATED: i18n message change alters displayed error text)_
- [ ] 🔒 `src/modules/gameScheduling/components/ExcelImportPanel.tsx` — Renders its preview grid with a raw <table>/<thead>/<tr> instead of the shared shadcn <Table> (same as spielplanung/ImportPanel.tsx and gameScheduling/InvitesDrawer.tsx). Using the shared Table component gives consistent borders, dark-mode tokens and the mobile compaction the convention enforces. _(consistency · GATED: Swapping to shadcn Table changes rendered markup/appearance)_
- [ ] 🔒 `src/modules/gameScheduling/components/TeamAvailabilityDialog.tsx` — Weekday labels use `new Intl.DateTimeFormat(lang, { weekday: 'short' })` with the current UI language, whereas CLAUDE.md requires hardcoding 'de-CH' on all Intl.DateTimeFormat calls (en-US users otherwise get English weekday abbreviations in a Swiss scheduling context). Hardcode 'de-CH'. _(consistency · GATED: Changes weekday labels shown to non-German users)_
- [ ] 🔒 `src/modules/games/EmbedGamesPage.tsx` — User-facing strings are hardcoded English ('Games', 'No games found.', 'No rankings available.') instead of i18n; the empty-rankings branch also fires a sentinel `{ id: { _eq: -1 } }` query on the rankings tab instead of using `enabled`. _(consistency · GATED: introducing i18n changes displayed text per locale)_
- [ ] 🔒 `src/modules/games/components/GameAttendanceDrilldown.tsx` — Renders homogeneous game records (date · opponent · hall · status) as a `<ul>` `space-y-1.5` list; the data-record convention wants `<Table>` (mobile-compacted). Low priority since it's a nested drilldown, but it's a scan-a-record list. _(consistency · GATED: converting the list to a table changes the rendered layout)_
- [ ] 🔒 `src/modules/games/components/GameDetailModal.tsx` — Line 185-186 builds `intlLocale` from `i18n.language` and passes it to `Intl.DateTimeFormat` — this violates the app-wide rule to hardcode `de-CH`; en-US/other browsers can render a non-Swiss date order. Hardcode `de-CH` (or route through the dateHelpers). _(consistency · GATED: changes the displayed long-date wording for non-de users)_
- [ ] 🔒 `src/modules/hallenplan/components/ClaimModal.tsx` — Textarea `placeholder="Optional..."` (line 188) is hardcoded English instead of i18n. Otherwise solid (409/'already' conflict detection, dark:bg-gray-800 on the select). _(consistency · GATED: i18n change alters placeholder text per locale)_
- [ ] 🔒 `src/modules/hallenplan/components/ClosureManager.tsx` — `handleSave` (create/update/delete per hall) and `handleDeleteGroup` run serial `await` loops over the group's halls; parallelize with Promise.all. The reason `placeholder="e.g. Holidays, maintenance, renovation"` (line 372) is hardcoded English and the error box (line 376) lacks dark: variants. _(consistency · GATED: i18n + dark-mode changes alter displayed text/appearance)_
- [ ] 🔒 `src/modules/hallenplan/components/SlotEditor.tsx` — The label free-text placeholder `"e.g. Trial training, Home game vs. TVA"` (line 419) and the CommandGroup headings 'Volleyball'/'Basketball' (line 282) are hardcoded English instead of i18n keys. _(consistency · GATED: i18n change alters displayed placeholder/heading text per locale)_
- [ ] 🔒 `src/modules/hallenplan/components/SlotEditor.tsx` — The conflict-warning box (lines 434-435) and error box (line 449) use `bg-amber-50/text-amber-800` and `bg-red-50/text-red-700` with no `dark:` variants — they render as light boxes in dark mode; add explicit dark variants per the non-semantic-color rule. _(ui-polish · GATED: changes dark-mode appearance)_
- [ ] 🔒 `src/modules/hallenplan/components/SummaryView.tsx` — Hardcoded/mixed-language labels: `DAY_HEADERS` are English (line 7), `typeLabels` mixes English+German ('Spiel', line 11-17) and are rendered in cells + tooltip, and the empty state is a hardcoded English 'No data to display' (line 152). Route all through i18n. _(consistency · GATED: i18n change alters displayed labels per locale)_
- [ ] 🔒 `src/modules/messaging/components/ConversationThread.tsx` — The auto-scroll effect fires on messages.length change with `behavior: 'smooth'` and scrollIntoView on a bottom sentinel — on first thread open (50 messages) this animates a long smooth scroll, and scrollIntoView can scroll the whole page rather than just the message container. Use instant scroll on initial load (only animate for subsequent incoming messages) and scroll the container via scrollTop. _(ui-polish · GATED: Changes the visible scroll animation behavior)_
- [ ] 🔒 `src/modules/scorer/ScorerAssignPage.tsx` — The results table and both team-summary tables are raw `<table>` markup (data records) rather than the shadcn `<Table>` primitive used elsewhere. _(consistency · GATED: converting to the Table primitive changes rendered markup/styling)_
- [ ] 🔒 `src/modules/scorer/components/ScorerRow.tsx` — `getDateFormatter` (lines 40-43) derives the Intl locale from `i18n.language` instead of hardcoding `de-CH`; consistent with the app-wide date rule this presentational weekday/month format should still pin de-CH (or use a dateHelper). _(consistency · GATED: changes displayed date wording for non-de users)_
- [ ] 🔒 `src/modules/spielplanung/ImportPanel.tsx` — The preview uses a raw <table> instead of the shared shadcn <Table>, bypassing the app's table styling/dark-mode/mobile rules (same pattern as ExcelImportPanel and InvitesDrawer). _(consistency · GATED: Swapping to shadcn Table changes rendered markup/appearance)_
- [ ] 🔒 `src/modules/teams/PlayerProfile.tsx` — 'Player not found' (line 147) is hardcoded English instead of an i18n key like the rest of the page. _(consistency · GATED: i18n change alters displayed text per locale)_
- [ ] 🔒 `src/utils/icalGenerator.ts` — DTSTART;TZID=Europe/Zurich is emitted (l.30) but no VTIMEZONE component is included in the VCALENDAR; strict RFC 5545 parsers may drop the timezone or reject timed events. Add a static Europe/Zurich VTIMEZONE block (or emit UTC with a trailing Z). _(ui-polish · GATED: Changes the generated .ics output consumed by external calendar apps)_
