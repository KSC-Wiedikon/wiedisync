{
  "name": "kscw-live-relay",
  "version": "0.1.0",
  "private": true,
  "description": "Live-scoring relay (Worker + Durable Object) — fans the LedBox scoreboard state out to wiedisync viewers over SSE.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.0.0"
  }
}
