# Wiedisync — Dev / Deploy Log (Archive, pre-v1.0.0)

> **Archived at the v1.0.0 release (2026-06-19).** This is the full pre-1.0 operator/deploy
> history, preserved verbatim. The active log continues in [`DEVLOG.md`](DEVLOG.md).

Operator-facing history of backend, deploy, and migration work on the wiedisync platform —
the running record that used to live inline in `CLAUDE.md`. Newest first.

This is the dev/ops narrative (migration numbers, dev + prod deploy status, internal IDs,
file paths, root-cause notes). It is deliberately distinct from:

- **CHANGELOG.md** — curated, user-facing release notes (English, semver), mirrored in `src/modules/changelog/ChangelogPage.tsx`.
- **git log** — the full commit-level detail.

At the end of a substantive backend/deploy session, append a dated entry here (one line per entry).
`CLAUDE.md → Recent dev log` keeps only the last few entries for at-a-glance context.

---

- **2026-06-30** **ClubDesk member-link cleanup + duplicate-member merges + Data Health name-match check → dev+prod (no migration).** Continuation of the link-repair thread. **Data fixes on prod:** linked 12+2 more `members.clubdesk_id` (incl. name-only matches Nikos/Mateo where the ClubDesk contact was free); ran 2 on-demand sync-downs (queue flag → host dispatcher) after the user merged 6 duplicate ClubDesk *contacts* → 5 auto-linked, Daniela linked by hand (maiden-name parenthetical `Duc (Fölmli)` defeats the last-name guard). **NB the sync dispatcher runs `/opt/clubdesk-sync/import-clubdesk-csv.mjs` — a separate copy from `/opt/directus-kscw/scripts/` that `scripts:deploy` targets — copied the hardened linker there too.** Deleted Linus Ulrich (member+login+cascades; user deleted him in ClubDesk) and a duplicate Noel Ajvazi signup (543, typo email). **5 wiedisync duplicate-MEMBER merges** (same person, two rows: a never-logged-in 2026-03-29 ClubDesk-import shell + a later signup with a divergent email): kept the ACTIVE record, grafted `clubdesk_id`+licence (NULL-only, preserved Livio's VM licence 301382 over ClubDesk's 24242), stored the ClubDesk email as secondary `vm_email`, migrated invoices+team history, deleted the shell+login (Biland133/Fosco44/Graf221/Roth371/Rübsam374). **Feature:** Data Health "ClubDesk match (name only)" check — members whose name matches a ClubDesk contact but email AND licence diverge surface for manual **Link** (new superadmin `GET /kscw/clubdesk-name-matches` + `POST /kscw/clubdesk-link`, sets clubdesk_id + secondary vm_email, actor-logged; join reads the non-API `clubdesk_export` server-side); already-linked contact → flagged as duplicate (needs merge), no one-click. **Login with either email** (post-merge secondary): `password-reset` resolves a typed `vm_email`/`email` to the real login; `register` rejects signup on an active member's `vm_email` so a divergent address can't respawn a duplicate. Build+eslint+`node --check` green; frontend dev→prod, `ext:deploy:dev`+`:prod`; new endpoints smoke 403 (gated). Prod name-match query now returns 0 (batch fully resolved). i18n en/de/gsw/fr/it.
- **2026-06-30** **Trainings: tombstone so deleted/edited occurrences stop respawning → dev+prod (migration 162).** Reported by Daniela (D4 coach): after she moved D4's Monday training 20:00→19:30, a phantom 20:00 training kept reappearing every time she deleted it. Root cause: recurring trainings are generated from `hall_slots` by `kscw-hooks/src/slot-cascade.js` (initial-gen + on-edit fill + nightly 02:00 top-up), deduped ONLY by `(hall_slot, date)` with **no delete tombstone** — the 29-Jun instance (training 616) detached from slot 9 (`hall_slot→NULL`) when its time was edited while siblings kept the link, so the generator saw slot 9's Monday "empty" and re-created the old-time duplicate (deleted+respawned as 2196→2207→2208). **Prod data fix:** re-linked 616→slot 9, deleted phantom 2208 + its 20 orphan `participations`; 29-Jun is past so it stays gone. **Structural fix (migration 162** `training_slot_skips`, keyed `(hall_slot, date)`): new `trainings.items.delete`/`.update` hooks tombstone a vacated slot-linked occurrence (delete, detach, or date-move); all three generators in slot-cascade.js filter `expectedDates` against the skip set; `trainings.items.create` clears the skip so re-adding works. Cascade's own raw-knex writes don't fire items hooks → no feedback loop. **Validated on dev** end-to-end (admin-token API test): API-delete writes the tombstone w/ actor → slot touch does NOT refill it, while a raw-SQL-deleted control date DOES refill (generator still fills genuine gaps) → re-create clears the tombstone. `node --check` green; `migration 162` applied dev+prod; `ext:deploy:dev`+`ext:deploy:prod`. Real-world note: hall 5 (Rebhügel) Mon is DU14 18:30–20:00 (slot 8) then D4 20:00–22:00 (slot 9); D4 actually starts 19:30 → training overlaps DU14 by 30 min, which is fine (WiediSync doesn't block overlapping trainings — cosmetic only). Kept slot 9 at 20:00 (real booking); training shows 19:30 (call time).
- **2026-06-29** **Above batch → PROMOTED TO PROD + member-nationality localization + on-demand sync-down.** (1) Promoted the ClubDesk-link/registration-i18n batch to prod: wiedisync dev→prod (migration 161 applied via `db:migrate:prod`; `ext:deploy:prod`; `scripts:deploy:prod`), kscw-website dev→prod. (2) **member nationality localized** too: `utils/countryCodes.ts` (196 countries, de+en name→ISO, generated from the website list) + `localizeCountryName()` resolve free-text country names → viewer language in the Data Explorer (`ExplorerMemberFields`); dev+prod. NB members.nationalitaet is 99% empty (ClubDesk doesn't sync it). (3) **On-demand prod sync-down** run (set `clubdesk_member_sync.down_requested_at`; host dispatcher cron claimed it). The hardened linker only takes effect in the sync because the dispatcher runs `/opt/clubdesk-sync/import-clubdesk-csv.mjs` (a SEPARATE copy from `/opt/directus-kscw/scripts/` that `scripts:deploy` targets) — copied the updated file there. Sync OK: 503 linked, 35 still unlinked = 6 ClubDesk-dup members (need ClubDesk merge: Notebaert/Goricanec/Duc-Fölmli/Ulrich/Khong/Buchheister) + 29 genuinely-not-in-ClubDesk. ("Liceo artistico" in the KS list is legit — the art gymnasium at KS Rämibühl.)
- **2026-06-29** **ClubDesk link repair + registration i18n (gender/nationality/Kantonsschule) → dev (prod pending).** Follow-on. **(A) ClubDesk links:** the sync-up "not yet in ClubDesk" list is just `members.clubdesk_id IS NULL` (no live ClubDesk search); migration 158 set clubdesk_id by email + a ONE-directional first-name prefix (`member LIKE clubdesk||'%'`), so short forms ("Alex" vs "Alexander") never linked despite identical email+licence → 18/47 unlinked members actually exist in ClubDesk (duplication trap on sync-up). Backfilled **12 safe links on prod** (licence/single-match; licence disambiguates ClubDesk's own dup contacts); 6 left are **ClubDesk-side duplicates** (two clubdesk_ids each) the user must merge. Hardened `import-clubdesk-csv.mjs` to re-link every sync, NULL-only: licence (1:1) then email+last-name+SYMMETRIC first-name (deployed dev VPS via `scripts:deploy:dev`; **prod VPS scripts are stale (Jun 8) — `scripts:deploy:prod` pending user OK**). **(B) Registration display/form i18n:** gender stored canonical-German (männlich/weiblich) → AnmeldungenPage now localizes it (reuses dhSetMale/Female). Nationality was stored as the localized country NAME (locale-dependent) → form now also submits the ISO code (fixed a latent bug: `selectNationality` never set `natHidden.dataset.code`), `registration.js` stores it (**migration 161** `registrations.nationalitaet_code` + directus_fields + backfill Schweiz/Polen), AnmeldungenPage localizes via `Intl.DisplayNames` (`utils/countryName.ts`). Kantonsschule: when "other cantonal school" is chosen the form reveals a Zürich-KS dropdown (18 schools, excl. KS Wiedikon = the dedicated KWI option) so the actual school is stored, not the literal "Andere Kantonsschule"; **fixed value bug** (KWI option stored "KS Rämibühl" → now "KS Wiedikon"). kscw-website `anmeldung.astro` + `registration-form.js` (v=3.2.1) + i18n. All builds + eslint + `node --check` green; migration 161 applied dev, endpoint+scripts deployed dev. **Prod pending:** wiedisync dev→prod (db:deploy:prod for 161 + ext:deploy:prod) + kscw-website dev→prod. **Follow-up:** member-explorer nationality localization ("everywhere"); historical registrations `kantonsschule='KS Rämibühl'` are actually KWI.
- **2026-06-29** **members.sex backfill + gender→sex pipeline fix → dev+prod (no version bump).** Root cause (Florentin Daniel, member 539): `members.sex` was only ever set by the Volleymanager path (`backfill-member-gender.mjs` / `vm-sync-check.mjs`, keyed off `sv_vm_check.gender` = licensed VB only). **Signup** (`POST /kscw/register`, `kscw-endpoints/src/index.js`) created a bare member and never mapped gender→sex; **ClubDesk sync-down** (`import-clubdesk-csv.mjs`) imports `Geschlecht` to `clubdesk_export` but still only propagates birthdate/address/category/phone, never `sex`. Result: 314/536 members had empty sex (≈ all of basketball). Registration approval only flips `registrations.status` — never creates a member, so no duplicate-member risk. **Fixes:** (1) **prod backfill** 299 rows via psql, priority ClubDesk `geschlecht` (licence then email+vn1 token) → gendered team roster (empirical: VB `^H*`/Legends=m,`^D*`=f; BB Herren*/HU*/H-Classics=m, Damen*/DU*/D-Classics/Lions D1/Rhinos D3=f; MU8/MU10/MiniVB MIXED=skip) → registration `geschlecht`; 2 conflicts hand-set (Elena Alessandri 111→f = ClubDesk data error männlich; Claudio Riedi 367→m = male coach on women's youth team); 13 real + 1 system acct (470) left. (2) **signup carries registration gender→sex** by email (fill-only, both new-member + vm_email-claim branches). (3) **Data Health "Missing sex" check** (`DataHealthPage.tsx` + `utils/dataHealthChecks.ts`, new `missingSex` issueKey + `manualKind:'sex'` + `manualFix()`) — inline Male/Female buttons, skips system@/@kscw.clubdesk.com, excluded from "Fix all"; i18n en/de/gsw/fr/it. Build (tsc -b) + eslint + `node --check` green. Deployed: push dev → merge→prod (no conflicts) → `ext:deploy:prod` (signup endpoint). **Open follow-up:** add geschlecht→sex UPDATE to `import-clubdesk-csv.mjs` so ClubDesk-sourced members also get sex.
- **2026-06-29** **Spielplanung: Saturday hall rule (lone game → KWI C) → prod direct (v1.17.0).** Shared KWI gym with basketball: a lone Saturday home game only needs one court → place it in the single hall **KWI C**, freeing the double hall **KWI A+B** for BB; 2 games at a time → A+B, 3 → A+B+C. **No schema migration** (halls/slots already model it; KWI A=1/B=2/C=3, each with own `vm_hall_id`). `game-scheduling.js`: new `kwiHallIds()` (name-matched, robust to re-id), `rebalanceSaturdayHalls(season)` — groups **upcoming** (`s.date >= CURRENT_DATE`) booked Saturday KWI home slots by exact `date|start_time`, reassigns `game_scheduling_slots.hall` (lone→[C,A,B], pair/trio→[A,B,C]), **closure-aware** (skips halls in `hall_closures` that day), deterministic by slot_id, idempotent; returns moved bookings. `repushMovedBookings()` — background **staggered** (700ms) VM re-push of moved games (no batch VM API → avoid simultaneous-login flood). Hooked into `reconcileBookingsToGames` (top: rebalance; tail: repush; returns `moved`) so EVERY booking path self-heals; `confirm-home`/`manual-booking` pre-rebalance **before** their explicit `spawnVmPush` (so VM gets the final hall, no DB↔VM mismatch) + repush spill excluding the just-pushed booking; `delete-booking` now rebalances+reconciles (freed slot → survivor spills back to C). New `POST /terminplanung/admin/rebalance-saturday-halls` (admin/spielplaner, `writeUserLog`). Frontend `SaturdayHallPanel.tsx` ("Optimize now") in `AdminSetupPage` config grid; i18n en+de (`saturdayHall*`, plural `_one/_other`; fr/it/gsw fall back to en, matching clubBlock precedent). Build (tsc -b) + eslint + `node --check` green. **Note:** rule fires on the next booking action OR the button — deploying the code changes nothing until then.
- **2026-06-28** **Polls: per-member answers for managers → dev+prod (v1.16.0).** Follow-up to v1.15.0. Managers now see WHO voted for each option, not just totals — **frontend-only** (the leader `poll_votes` read grant from v1.15.0 already returns `member`; no perm change). `usePollVotes` now expands the voter (`fields: [...,'member.id','member.first_name','member.last_name']`) and `getResults().voters` returns `{id,name}` per option (`memberName`); `myVote` match switched to `relId(v.member)` since `member` arrives expanded. `PollCard`: `showVoters = canManage && !poll.anonymous` renders a "Voted by: …" line under each result bar (names `.filter(Boolean)` to drop unreadable/ex-member voters); anonymous polls show an `EyeOff` "individual answers hidden" note to managers instead. The **Anonymous toggle already existed** in `PollForm` — only its description copy was sharpened (anonymous ⇒ even managers see totals only). Nested member-name expansion under LEADER scope is the same proven pattern as `FormResponsesModal`. i18n `anonymousNote`/`votedBy` + reworded `anonymousDescription` (en+de). Build + eslint green. Deployed: push dev + merge→prod (no setup-perms needed).
- **2026-06-28** **Polls: home-screen surveys widget + managers see live replies → dev+prod (v1.15.0).** Two asks. (1) **Discoverability** — polls previously rendered only on the team page / in chats. New `HomePollsCard` (mirrors the "Forms to fill" card) on `HomePage`, between the forms card and Your-dues, listing open + not-past-deadline polls for the user's teams via new `useActivePolls(teamIds)` hook (`polls` already member-readable club-wide, no perm change for this part); full inline `PollCard` voting. Gated `user && isApproved && hasTeams`, renders null when none. (2) **"Daniela can't see the replies"** — two root causes: the client gate `showResults = hasVoted || !isOpen || deadlinePassed` AND `poll_votes` read scoped to `OWN_MEMBER` (only Vorstand/Sport-Admin had read-all). Fix: `PollCard` `showResults` now also true for `canManage` (result bars made tappable when the manager can still vote, so they keep the vote action); **`setup-permissions.mjs`** adds `LEADER_POLICY` `poll_votes` read scoped to `poll.team.{coach,team_responsible}.members_id.user = $CURRENT_USER` (unions on top of the member own-vote read; UI shows counts only, matching the board's existing posture). No schema migration. PERMISSIONS.md row added. i18n `activePolls` en+de. Build (tsc -b) + eslint green (changed files clean; HomePage's pre-existing purity lints untouched). Deployed: push (CF Pages) + `db:setup-perms:dev` then `:prod`.
- **2026-06-28** **Spielplanung: club-wide "blocked dates" (superadmin blackout) → dev.** New superadmin-only date-blocking that stops HOME games for EVERY team on the chosen dates (club holidays/tournaments/hall-wide events), layered ON TOP of the existing per-team `scheduling_blocks` (migration 085, coach/TR-set in the Absences view — kept). Migration **160** `scheduling_global_blocks` (start/end/reason/created_by). `game-scheduling.js`: new `isSuperadmin(req)` gate (Directus admin OR superuser/admin role — tighter than `isAdminOrSpielplaner`, a spielplaner must NOT edit the club blackout); `GET/POST/DELETE /terminplanung/admin/club-blocked-dates` (GET=planner-readable for the calendar, POST/DELETE=superadmin, `writeUserLog`); engine integration at every home chokepoint — `globalBlockDates` set fetched once + `club_block` reason in `validate()` (proposal scoring), a parallel `whereNotExists(scheduling_global_blocks …)` in `slots/:token` (so blocked dates aren't even OFFERED to opponents), and a manual-booking home-leg guard. Frontend `ClubBlockedDatesPanel.tsx` (list/add/remove date ranges) in `AdminSetupPage` config grid, gated `isSuperAdmin`; i18n en+de (`clubBlock*`). **Verified on dev:** migration applied + table exists; endpoints 403; functional SQL test — a club block over an available slot's date drops it from the pool (`survive_filter=0`). Build (tsc -b) + eslint green (panel avoids set-state-in-effect). Prod pending approval.
- **2026-06-28** **ClubDesk MEMBER sync — Phase 2 "Sync up" (wiedisync→ClubDesk) → dev.** Pushes new registrations + member info-changes INTO ClubDesk via a superadmin review-modal (not fire-and-forget). **Spike (2026-06-27):** drove ClubDesk's Kontakte import wizard headless on a throwaway ZZTEST contact — PROVED ClubDesk auto-matches by e-mail and shows a pre-commit summary "Neue Kontakte (neu erstellt)" vs "Veränderte Kontakte (aktualisiert)", committing only on "Ja" (re-import same e-mail → veraendert=1, no duplicate). Also mapped the import column auto-mapping: exact ClubDesk field-name headers required — `Telefon Privat` (not "Telefon"), `AHV Nummer` (not "AHV-Nummer"); push CONTACT fields only (never groups/teams). **Build:** (1) `clubdesk-scrape-import.mjs` — `<csv> preview|commit` (preview reads the summary + backs out; commit clicks Ja), emits JSON `{neu,veraendert,committed}`. (2) Migration **158** — `members.clubdesk_id` (+ `clubdesk_push_pending/_changes/_pushed_at`), backfilled from `clubdesk_export` staging via the proven safe e-mail+firstname match (unambiguous only); **prod would link 491/536**, 45 stay unlinked (review set). Migration **159** — `clubdesk_member_sync.up_csv/up_member_ids/up_result`. (3) Endpoints in `clubdesk-update.js`: `GET up-preview` (changed = pending+linked w/ field diff; unlinked = no clubdesk_id, flags likely-non-members), `POST up` (builds the 12-col semicolon CSV via `buildPushCsv`, stashes it + member-ids, enqueues), `GET up-status`. Dirty-flag set on `createMemberFromRegistration` (new) + the `/clubdesk-update` edit path (with the field diff). (4) Host `clubdesk-member-up-dispatch.sh` — claims `up_*`, pulls up_csv (iconv→CP1252), runs the scraper `commit` under the shared `.sync.lock`, clears `clubdesk_push_pending` for the pushed ids, writes `up_result`, NULLs up_csv (PII). ROOT cron `* * * * * CLUBDESK_ENV=dev DB=directus_kscw_dev …`. (5) Frontend `ClubdeskSyncUpModal.tsx` (preview→select unlinked→push→poll→result) + "Sync up" button on `AnmeldungenPage`, superadmin-only; i18n en+de (`clubdeskUp*`). **Verified end-to-end on dev:** seeded a ZZTEST member→ enqueued up-payload → up-dispatcher pushed to ClubDesk (`up_result {neu:1,committed:true}`), cleared the flag + stamped pushed_at + nulled up_csv; ZZTEST then deleted from ClubDesk + dev. Build (tsc -b) + eslint green (modal avoids set-state-in-effect). **Prod pending** approval (migrations 158+159 on prod DB + prod up-dispatcher cron). Commits `9f1b50ef` (scraper+158) + this batch.
- **2026-06-27** **ClubDesk MEMBER sync — superadmin "Sync down" button → dev (Phase 1 of a planned bidirectional member sync).** Twin of the finance "Sync now" pattern, for member data instead of finance. **(1)** Migration **157-clubdesk-member-sync** — new singleton `clubdesk_member_sync` table with `down_*` AND `up_*` state columns (`*_requested_at/*_state/*_message/*_finished_at`); up_* reserved for the sync-up phase so it needs no second migration. **(2)** `GET/POST /kscw/clubdesk-member-sync` added to `clubdesk-update.js` — superadmin-gated (`superGate`: Directus admin OR `superuser`/`admin` member role), POST enqueues + `writeUserLog` actor capture, GET polled by the button. No Directus permission rows needed (raw-knex endpoint, table never touched via items API) → `setup-perms` correctly skipped. **(3)** Host dispatcher `clubdesk-member-dispatch.sh` (repo copy + installed at `/opt/clubdesk-sync/`) — flock `.member-dispatch.lock`, atomic CTE-claim of the queued `down_*` request, runs the EXISTING `clubdesk-sync.sh` (member scrape→import), writes back `down_state`; stale-run auto-reset (>15min). Wired to ROOT cron `* * * * * CLUBDESK_ENV=dev DB=directus_kscw_dev …` on the VPS (alongside the finance dispatcher; the shared `.sync.lock` in clubdesk-sync.sh serializes against the weekly member cron + the one-ClubDesk-session constraint). **(4)** Frontend `ClubdeskMemberSyncButton.tsx` (self-polling like the finance button) wired into the `AnmeldungenPage` header, superadmin-only (`isGlobalAdmin`); i18n en+de (`clubdeskSync*`). **Deploy:** `npm run deploy:dev` applied 157 + redeployed the extension (snapshot taken first; setup-perms/smoke skipped — dev token not in shell env, no perms changed). **Verified end-to-end on dev:** enqueued → cron claimed (state `queued`→`running`→`done`) → real ClubDesk scrape imported into dev members (`members_with_address=507`). Endpoint returns 403 unauth (registered, not 404). Build (tsc -b) + eslint green. **Prod pending** explicit approval (needs migration 157 on prod DB + prod-targeted dispatcher cron). **Phase 2 (sync-up, wiedisync→ClubDesk, new members + info changes):** gated on a spike validating whether ClubDesk's contact import UPDATE-matches existing contacts vs insert-only; UX spec'd as a rich modal (preview of what will be pushed → live progress → success/fail result → echo of uploaded data → post-push verify in ClubDesk), NOT fire-and-forget.
- **2026-06-26** **Finance: ClubDesk sync automation + "Sync now" + Ledger-shows-ClubDesk + UX fixes → prod (part of v1.12.0).** Post-1.11.0 ledger refinements. **(1) Ledger shows the real books:** `GET /finance/ledger/entries` + `/trial-balance` widened from `source='native'` to `whereIn source ['native','clubdesk']` (the 754 imported ClubDesk bookings now show in the Journal/Trial-balance, "ClubDesk" badge, reverse/delete native-only) so the Ledger isn't empty (it had 0 native postings); commit cfb23041. **(2) Nightly finance sync — the "04:00" note was a LIE (no cron existed; finance imported manually, last 18.06):** the finance scraper `clubdesk-scrape-finance.mjs` (already in-repo, built 19.06, never deployed — drives ClubDesk Finanzen: Rechnungen→Alle→Export→Alle Spalten + Buchhaltung→Export Buchungen) was validated live on the VPS via headless runs + screenshots, then wired: `clubdesk-finance-sync.sh` (scrape→`import-clubdesk-finance.mjs --emit-sql`→psql apply, flock, PII CSVs trap-cleaned) on **ROOT cron `0 2 * * *`** (02:00 UTC ≈ 04:00 Swiss, before the 03:00 dev-refresh). Ran once: refreshed 954 invoices + 754 bookings + 46 accounts to prod. Commit 0d61b7d6. **(3) "Sync now" button:** migration **155-finance-sync-trigger** (`finance_ledger_settings.sync_requested_at/sync_state/...`), `POST/GET /finance/ledger/clubdesk-sync`, host dispatcher `clubdesk-finance-dispatch.sh` on ROOT cron `* * * * *` claims the request + runs the sync. **Bug (commit 9baf3c91):** `psql -tAc "UPDATE…RETURNING x"` prints value + "UPDATE 1" tag → claim check failed → dispatch set 'running' then exited → stuck forever; fixed via CTE claim (`WITH u AS (UPDATE…RETURNING 1) SELECT count(*)`) + stale-run auto-reset. Button polls in the click handler (NOT useEffect — react-hooks/set-state-in-effect is an ERROR here). Commit 194a533f. **(4) UX:** LedgerTab dropped its own fiscal-year `<select>` → follows the single global FinancePage selector (were 2 diverging); global `placeholderData: keepPreviousData` on the queryClient → year/filter changes keep prev data on-screen, no boot-screen flash (`useReportPageLoading(isLoading)` fired on every refetch). **Deploy notes:** `finance-ledger.js` **surgically scp'd** to both containers (NOT `ext:deploy:prod` — shared tree had the user's uncommitted endpoint edits a full rsync would ship); `db:migrate` also applied the user's pending 155-member-email-notify-prefs (later renumbered →156 by the user). VPS debug artifacts (discovery script, PII screenshots, temp CSVs) cleaned afterward.
- **2026-06-26** **Anmeldungen admin: show uploaded ID front/back → dev + prod.** Basketball registrations collect `id_front` (required) + `id_back` (optional) on the website, uploaded via `POST /kscw/registration/:id/files` into the `registrations.id_upload_front` / `id_upload_back` columns — but the `AnmeldungenPage` DOCUMENTS section only rendered the 3 BB docs (`bb_doc_lizenz/selfdecl/natdecl`), so the actual ID photos were stored-but-invisible. Frontend-only fix: added `id_upload_front`/`id_upload_back` to the `Registration` interface, extracted a `renderDoc` helper, and render the two ID previews in a second row beneath the BB docs (same basketball-gated block). New i18n keys `anmeldungenDocIdFront`/`anmeldungenDocIdBack` (en + de; fr/gsw/it fall back, matching the existing doc keys). No schema/backend change — columns + data already existed and the collection fetch has no `fields` filter so the IDs were already on the client. Also fixed the row doc-count badge (both the name-cell "N docs" and the team-cell compact "N"): it counted only the 3 BB docs and was gated on `bb_doc_lizenz`, so a reg with licence + 2 IDs showed "1" — centralized into module-level `DOC_FIELDS` + `countDocs(reg)` (all 5 fields), gated on `countDocs > 0`. Build (tsc -b) + eslint green. No version bump (admin-only fix; in-app changelog audience is end users).

- **2026-06-26** **Per-member notification opt-out ("Email notifications") → dev + prod, v1.12.0.** Members silence specific alert categories from their profile. **Migration 156** (renamed from a 155 collision with the parallel `155-finance-sync-trigger.sql`) adds 5 default-TRUE booleans on `members` (`email_notify_registrations/join_requests/form_submissions/announcements/events`) + `directus_fields` reg — schema-only/idempotent. Fields added to `MEMBER_EDITABLE_FIELDS` (`setup-permissions.mjs`, self read+update). **Enforcement gates only the email (or, for forms, the push), never the in-app bell:** `event-notify.js` (events), `kscw-hooks` announcement fanout, `registration.js getSportAdminEmails` (new-reg admin alert), endpoints `index.js` join-request loop, `public-forms.js notifyOwner` (push). **Frontend:** `EmailNotificationCard` in `ProfilePage` — role-gated rows (registrations→admins, join/forms→coaches+TRs, news/events→all) — `Member` type + en/de i18n. **Deploy tangled by the shared tree:** while `deploy:dev` ran, two finance commits (`194a533f` Sync-now, `9baf3c91` dispatcher fix) landed on top of mine and were pushed to **both** origin branches, so the parallel finance `db:deploy` had already applied my migration's columns on dev+prod but had NOT run setup-perms or rsynced the new ext. Completed it: `db:setup-perms:{dev,prod}` (password login, **507/0** each; verified `members` update+read field-lists include the flags via `directus_permissions`), `ext:deploy:{dev,prod}` (enforcement markers confirmed live on both servers; finance Sync-now backend rode along as intended). **Renumber tidy-up:** file 155→156, all refs updated, deleted the orphan `155-member` tracker rows on both DBs + re-migrated to record `156`. Build (tsc -b) + eslint green. Version 1.11.0 → **1.12.0**.
- **2026-06-26** **Native double-entry ledger (book of record) → prod, v1.11.0.** Built Program B in-house (user chose "build it inside", not the integrate-Banana recommendation from the prior audit/benchmark). Key insight: `finance_transactions` is already a 2-leg table (`debit_account`/`credit_account`/`amount_chf`, `source` supports `native`), so a journal entry = one balanced `source='native'` row — no Sammelbuchung header needed. **Four layers, each adversarially reviewed (3 workflows) + hardened + live-QA'd on dev:** (1) **write path** — migration **150** (native Beleg seq `J-YYYY-NNNN`, `reversal_of`, FY close metadata, immutability trigger `trg_finance_native_txn_lock`; **151** extends it to BEFORE INSERT — native rows in a CLOSED fiscal year can't insert/update/delete, only reversal; clubdesk-source rows untouched so the importer's delete+reinsert works); endpoints in new `finance-ledger.js` (accounts CRUD, entries post/reverse/delete, trial-balance). (2) **Year-end close** — `finance-close.js` `planYearEndClose` (pure, unit-tested) + `POST /finance/ledger/fiscal-years/:id/close`: Erfolgsrechnung → Abschluss (nominal→equity) → Eröffnung (real balances → next FY via opening-clearing that nets to 0), in one txn with a `forUpdate` lock (concurrent double-close → 1 set), next-FY guards, dry-run preview. Review: 0 crit / 5 high, all fixed. (3) **Auto-posting (self-functioning)** — `finance-autopost.js` + migration **152** (`finance_ledger_settings` singleton account-map + `autopost_enabled`; `finance_transactions.ref_kind/ref_id/auto` + partial-unique idempotency index) + **153** (`prepayment_account`). Accrual model (issue=Debitoren/Ertrag, payment=Bank/Debitoren, refund/credit/writeoff, team sponsoring=Bank/Sponsoring); tracks a running open-receivable so Debitoren reconciles (overpayment→prepayment liability, not negative A/R); CANCEL removes standing posts; issue post self-heals on amount change; per-invoice transactional. Best-effort real-time hooks (never break the A/R op, gated on the master switch) in `finance.js` (create-invoice, confirm, POST+DELETE payments, cancel, dues-run issue+cancel, team-entry) + `finance-camt.js`. `POST /ledger/reconcile` (idempotent backfill), `POST /ledger/seed-chart` (mirror ClubDesk Kontenplan, infers type from Swiss number range). Review: 0 crit / 6 high, all fixed. (4) **UI** — new **Ledger** tab (`LedgerTab.tsx`): Journal, Kontenplan, Trial balance, Year-end close (dry-run+commit), Auto-posting settings (seed-chart + reconcile buttons); `useFinance` hooks; en+de i18n. Dashboard P&L/Balance now reads `source='clubdesk'` only (native is a parallel book) — no double-count. **Deploy:** prod DB had 138-150 already applied (prior shared-tree merge) — `db:deploy:prod` applied only the 3 pending (**151/152/153**) → setup-perms → smoke; `ext:deploy:prod`; push `prod` (CF Pages frontend). Build (tsc -b) + eslint green. **UI not yet headless-click-tested** (playwright bridge unavailable) — needs a manual click-through. **Deferred:** A/R cutover at close (open invoices→Debitoren carry-forward); VAT/MWST (none — fine while club <CHF 100k); validate camt vs a real UBS file. `SCHEMA.sql` re-baseline after prod migrate per policy.
- **2026-06-25** **Deep dual-repo security + permission + UI audit → fixes (deploy in progress).** Ran `/kscw-security-audit` extended to BOTH `wiedisync` + `kscw-website` and a UI dimension: 13 parallel read-only finders → adversarial verify → 66 raw / **63 confirmed** (2 high, 7 medium, 41 low, 13 info), 3 refuted (expense IDOR already `uploaded_by`-scoped; website roster `yob` minor-stripped server-side; website admin `/users` manager-only). **wiedisync backend:** scorer-delegation duty-hijack (HIGH, HOOK-1+PG-1) closed 3 ways — migration **148** forces `status='pending'` on insert; `transferDelegatedDuty` gated on actor==recipient AND delegator-holds-duty; accept endpoint re-checks ownership + `writeUserLog` (also decline, EP-SCH-3). PERM-1 `directus_files` Member read now `_nin:[finance,feedback]` (feedback-screenshot PII); PERM-2 `user_logs` create actor force-stamp hook; HOOK-2 members filter strips `is_spielplaner`+`finance` not just `role`; INT-1 `/contact` per-IP limit + no message echo; migration **149** finance FK CASCADE→RESTRICT (PG-2/3); + EP-SCH-2 register limits/validation, INT-2 OTP freshness, EP-COMM-1/2/3/4, EP-FIN-2/3, HOOK-3, INT-4, FE-1 CSP `form-action`, FE-2 Sentry pseudonym. **wiedisync UI:** ~145 en i18n Title→Sentence case, all dates `de-CH`, native-select dark bg, form-label/`aria` a11y, BugfixDashboard+VolleyFeedback record-lists→`<Table>`. **kscw-website:** XSS sinks closed (WEB-ADM-3/SEC-3/SEC-7), DOMPurify vendored local SRI-verified 3.2.4 (WEB-SEC-4), nav keyboard a11y + 44px + lang `aria-pressed` (UI-WEB-1/4/5/6/7), 238 en i18n Sentence case; **new `kscw-website/SECURITY.md`**. **Accepted/deferred (in SECURITY.md):** PERM-3 (cross-member `user` UUID — load-bearing for roster edit-attribution), PERM-4/EP-SCH-1 (member-facing scheduling reads, PII-excluded), website `script-src 'unsafe-inline'` (needs nonce build step; sinks closed). Four-way sync done (SECURITY.md ×2 + PERMISSIONS.md + setup-permissions.mjs + migrations 148/149). Build green both repos; pre-existing test reds (dateHelpers/wadmin-knex/website i18n-helpers) unrelated. **Deploy:** schema-first dev → smoke → prod (per CLAUDE.md rule 4). Sits on the user's parallel finance commits `91501221`+`b912a0bf` (no file overlap).
- **2026-06-25** **Spielplanung mailbox → standalone tab + two-account (Volleyball/Basketball) mail client, v1.10.0 (code; deploy pending).** The embedded mailbox moved off the Terminplanung dashboard into its own **Mailbox tab** (`/admin/terminplanung/mailbox`, new `MailboxPage.tsx` + `MailboxRoute` guard that admits volleyball *or* basketball admins) with a **Volleyball/Basketball toggle** (gated per-sport: vb = admin/`vb_admin`/`is_spielplaner`, bb = admin/`bb_admin`), **Inbox/Sent** folder split, and **Reply / Reply-all / Forward / New email**. **Migration 144** (`scheduling_emails.account varchar NOT NULL DEFAULT 'volleyball'` + CHECK, drops the global `message_id` UNIQUE → composite **`UNIQUE (account, message_id)`**, account-aware unread index, `directus_fields` reg — schema-only/idempotent; **deploy schema-first**, the endpoint's `ON CONFLICT` now targets `(account, message_id)`; renumbered from 141 to clear a collision with the parallel finance-email migrations 141/142/143). **Backend** (`scheduling-mailbox.js`): hardcoded VB constants → an **`ACCOUNTS` map** keyed by sport (imap user/pass + from + signature + msgid domain), `resolveAccount`/`accountConfigured`, account threaded through `imapClient`/`syncFolder`/`parsedToRow`/`runMailboxSync` (loops every configured account, per-account run-guard `Set`), per-sport `authForSport(req,sport)` replacing the single admin/spielplaner gate, every route takes `?sport=` + scopes DB reads by account, **reply-all** = server strips our own address out of To/Cc + dedupes overlap, **forward** = new `forward_from_id`/`forward_attach_indices` on the reply route re-fetches the source's attachments live from IMAP (shared `fetchMessageAttachments` helper, also now backs the single-attachment route) and enforces caps across the combined set, `/assign` rejects `sport=basketball` (volleyball-only). New env: `SCHEDULING_IMAP_PASSWORD_BASKETBALL` (+ optional `…USER_BASKETBALL`); `SCHEDULING_IMAP_PASSWORD` unchanged (back-compat). **Signature** (`scheduling-signature.js`): parameterised `lightCard` + `SCHEDULING_SIGNATURE_BASKETBALL_{LIGHT_HTML,TEXT}` (generic BB placeholder — no VB names/WhatsApp until operators supply; VB exports + `buildSchedSignatureRows` untouched). **Cron** (`kscw-hooks`): dormancy guard runs if **either** account password is set. **Frontend**: `useMailbox(enabled, sport)` (all calls `?sport=`, reply via query so auth runs before multipart parse; payload gains `cc`/`forward_from_id`/`forward_attach_indices`); `MailboxPanel` gained folder tabs + reply-all/forward + a Cc field + sport-aware attachment downloads; dashboard drops the bottom mailbox card and its "N emails" button now **deep-links** to `…/mailbox?sport=volleyball&opponent=<id>`; nav tab in `SchedulingLayout`; route in `SchedulingApp`; i18n ×5 (`nav.mailbox` + `mailbox*` keys). Build (tsc -b) + eslint green on all new files (4 pre-existing `set-state-in-effect` errors in `AdminDashboardPage` VM-check effects untouched). **basketball@spielplanung.kscw.ch confirmed live at Migadu** (vault `Migadu - basketball@spielplanung.kscw.ch`). **Deploy pending**: schema-first `npm run deploy:dev` (db:deploy → ext:deploy) + add BB env to dev `.env` + container **recreate**; then dev QA → prod (`SCHEMA.sql` re-baseline after prod migrate, per policy). Version 1.9.1 → **1.10.0**.
- **2026-06-25** **Finance: recurring membership-dues billing (dues run) → dev + prod, v1.9.0.** First ClubDesk-parity feature off the new `.planning/clubdesk-finance-parity.md` roadmap (a 14-agent workflow mapped the gap: Program A = collection layer buildable ~2-3mo; Program B = legal ledger 12-18mo, gated on Revisor + parallel-run). **Migration 138** (`finance_dues_rates` = per-(fiscal_year,category[,sektion]) CHF schedule, unique on `(fiscal_year, lower(category), coalesce(sektion,''))`; `finance_dues_runs` = issued batches; `finance_invoices.dues_run` FK + **partial-unique `(dues_run, member)` double-bill guard** + a fiscal-year/member lookup index; full Directus collection/field/relation registration — schema-only, idempotent, follows 128/137). **Endpoints** (`finance.js`, raw-knex + `canManageFinance` gate + `writeUserLog`): GET/POST/DELETE `/finance/dues-rates` (GET also returns live `beitragskategorie`/`sektion` values so the UI only offers real categories), POST `/finance/dues-runs/preview` (pure dry-run, per-member `missing_rate`/`missing_email`/`already_billed` flags + totals), POST `/finance/dues-runs/issue` (one txn: run row → mint native invoices w/ `N-YYYY-NNNN` seq + best-effort SCOR → totals; **idempotent** — skips members already holding a non-cancelled dues invoice this fiscal year so re-running can't double-bill; pure `pickRate()` = sektion override beats category default), POST `/finance/dues-runs/:id/cancel` (voids still-open, keeps paid), GET `/finance/dues-runs`. **Perms** (`setup-permissions.mjs`): both tables → Vorstand + Finance **read** only; all writes endpoint-gated. **Frontend**: new **Dues run** tab (`src/modules/finance/DuesRunManager.tsx`) — rate-config `<Table>` w/ inline add/delete, run wizard (category chips + only-active + due date → dry-run preview `<Table>` w/ status badges → issue), past-runs `<Table>` w/ cancel; `useFinance` hooks/actions + types; en/de i18n (fr/gsw/it fall back). **Parser harness** also landed (`kscw-endpoints/src/__tests__/camt.test.js`, 10 vitest pass + a dormant real-file gate at `…/fixtures/real-ubs-camt054.xml` — drop a real UBS camt.054 there to finally validate the dormant reconciliation parser). **Deploy**: dev `db:deploy:dev` (snapshot → migrate, also applied the previously-pending **137** → setup-perms **507/0** → smoke) + `ext:deploy:dev`; **E2E-validated on dev** (FY13, "BB Student/Lehrling 1. Liga" 2 members: rate→preview CHF240→issue `N-2026-0001/0002` w/ SCOR refs→409 idempotency→cancel→cleanup, dev left pristine). Prod `db:snapshot:prod` → `db:migrate:prod` (applied **138 + 139**) → `db:setup-perms:prod` (password login `kscw-prod-admin-user`, **507/0**) → DB-verified (no prod member token for smoke) → `ext:deploy:prod`; merge dev→prod + push (CF Pages prod). **Note:** the prod migrate also carried the user's committed **139-vm_authority_date** (per-season SV-feed takeover date) — backward-compat (NULL = pre-139 behaviour), its `sv-sync.js` rode along in the same `ext:deploy:prod`. `SCHEMA.sql` **re-baselined** from prod (folded the drifted 129/134/137 + 138/139). Build (tsc -b) + eslint green throughout (0 errors). Version 1.8.0 → **1.9.0**. **Follow-on same day (folded into 1.9.0):** **download all of a run's bills as one QR-bill PDF** — new read endpoint `GET /finance/dues-runs/:id/invoices` + client-side `downloadInvoiceBillsPdf` (`qrBillPdf.ts`, multi-page A4, club-creditor + SCOR + "Rechnungsnummer:" message, mirrors `InvoiceQrBill`) reusing the existing swissqrbill/pdfkit chunk → **no schema/perms/infra change**, `ext:deploy:prod` + frontend merge only (no migration, no setup-perms — raw-knex read of already-granted `finance_invoices`). "Download bills" action per run in `DuesRunManager`. Gives the treasurer the **print/post** channel. **Still missing (Phase 2):** can't EMAIL the bill server-side (members self-discover on `/finance/dues`, or treasurer prints/posts/attaches the PDF) — server-side bulk email-send is the next capability (needs swissqrbill+pdfkit in kscw-endpoints + MailService + the never-mass-email guards).
- **2026-06-24** **Spielplanung: suffix-tolerant opponent fixture matching → prod (ext only).** Limmattal HU23 (a 2026/27 *Dreifachrunde* = 2 home + 1 away) showed only 1 home card. Root cause: `opponentSvrzFixtures()` matched the opponent side by **exact** `team_name`, but VM appends a team designation for multi-round groups ("VBC Limmattal" → "VBC Limmattal HU23-1") — exact match returned 0 fixtures → tool fell back to the legacy single home/away game, dropping the extra home fixture. **Data fix (prod):** opponent 112 `team_name` → "VBC Limmattal HU23-1" + stamped the existing confirmed booking (id 200) onto the first home fixture #406697. **Code fix** (`game-scheduling.js`, dev commit `6653ef89`): keep the exact match as the fast path (no behaviour change for already-correct opponents); add a fallback that re-fetches this team's season fixtures (KSCW either side, our side pinned by stable `staticTeamIdentifier`) and matches the opponent by a word-boundary suffix-tolerant name compare — `keepOurs()` already pins our side so a loose opponent compare can't leak another KSCW team's opponent in. Verified on dev (Limmattal still un-suffixed there → now 2 home + 1 away; RTS Volley Talents exact-match opponent unchanged 1/1) then `ext:deploy:prod` (smoke ✓). Prod now resolves 3 fixtures and the opponent completed all 3 bookings (both home + away confirmed). **Branch note:** committed on `dev` + deployed to the prod *container* via rsync; the `prod` *branch* gets it at the next dev→prod merge (don't run `ext:deploy:prod` from a stale `prod` checkout before then, or it reverts). No migration, no perms change.
- **2026-06-24** **Finance role + per-member explorer + editable billing + invoice-PDF attachments → dev + prod, v1.8.0.** Two increments, both shipped to prod. **(A) Finance role + explorer** (migrations **132/133**): new ORTHOGONAL `finance` app-role (treasurer / finance team) = member perms + a per-user `KSCW Finance` policy attached like `is_spielplaner`/Terminplanung — reconciled by a new `reconcileFinanceAccess` in the kscw-hooks role-sync action AND `setup-permissions.mjs §13` (deploy-time). Chose orthogonal over a base-role tier so `finance`+sport-admin combos don't lose finance (the base-role mapping picks one role; finance is a capability). Policy grants club-wide finance reads (same `finance_*` set as Vorstand), a field-scoped `members` read (`FINANCE_MEMBER_FIELDS` — contact/IBAN/AHV/membership/billing), `member_teams` read, and a `members` UPDATE scoped to billing fields ONLY. Endpoint gate `isVorstand`→`canManageFinance` (adds `finance`) in `finance.js` + `finance-camt.js` so finance is a full treasurer at the endpoint layer. **132** drops+recreates the `members_role_values_valid` CHECK adding `finance` (fix-forward over applied migration 005); **133** adds `billing_{different,name,email,address,plz,ort,phone}` + directus_fields + a one-time backfill from the latest linked invoice whose recipient differs from the member's email — **460/532 flipped on DEV (scrubbed-email artifact: dev scrubs `members.email` to `member_N@devsink.invalid` but leaves `finance_invoices.recipient_email` real → 100% "differ"), 0 on PROD (all 494 member-linked invoices are email-matched to the member's own address; guardian cases live in unlinked orphans, set manually)** — logic correct, dev noise only. Frontend: `isFinance`/`canAccessFinance` in useAuth, new `FinanceRoute` guard (replaces VorstandRoute on `/admin/finance`), nav + `RoleChipPicker` + `StatusBadge` + ProfilePage + members.json/setup-schema choices + invitations i18n; new **Members tab** in `FinancePage` → `FinanceMemberExplorer` (member `<Table>` → detail: contact/IBAN/membership + editable billing block [finance-only via `canEdit=isFinance`] + their invoice list w/ status pills); `useFinanceMembers` hook; finance i18n ×5. Verified on dev: policy = 11 perms, §13 attach/detach (set finance via DB→setup-perms attached 1→cleaned up). The kscw-hooks role-strip filter blocks `members.role` writes from non-admin callers (so the deploy admin account's API PATCH was stripped — expected; the real app assigner is always an admin/superuser member). **(B) Invoice-PDF attachments** (migration **134**): `finance_invoice_documents` link table keyed by `match_clubdesk_id` (ClubDesk-mirror, survives the nightly delete+reinsert — 1-1 with the ClubDesk invoice, the integrity guarantee the treasurer asked for) OR the `invoice` FK (native); private Directus folder `f1a0d0c5-…`. **Security:** member `directus_files` read narrowed from fully-unfiltered to `{_or:[{folder:_null},{folder:{_neq:<finance folder>}}]}` — SURGICAL (only the new private folder excluded; folder-less photos + feedback screenshots still member-readable, so Vorstand/SportAdmin feedback review and all photo loading are unaffected — verified the folder map: 86 root + 2 feedback + 0 registrations on prod). Finance gets `directus_files` create + folder-scoped read + `finance_invoice_documents` CRUD; Vorstand gets folder read + doc read. Served via `/assets` (honours the policy) — no streaming endpoint. Frontend: per-invoice attach/view/remove in the explorer; `uploadFile(file, folder)` + `openPrivateAsset` (credentialed blob open, robust cross-site) in `lib/api.ts`; `useFinanceInvoiceDocuments` hook; doc i18n ×5. PDFs are pure items-API + /files + /assets — **no extension change**. **Deploy**: dev `db:migrate` (132/133/134) + `db:setup-perms` + `ext:deploy` (for A's hook + gate); prod via two dev→prod merges (`6c70d579`, `a874b9cb`), `db:migrate:prod` (132/133/134 ✓), `db:setup-perms:prod` (489→497 perms, 0 errors; one transient `fetch failed` → re-ran clean), `ext:deploy:prod` (A only). Prod snapshot: the v132/133 deploy ran `db:snapshot:prod` (local 251M done; offsite rclone to GDrive was slow ~15min but the local backup secured safety, proceeded); the 134 deploy reused it (134 additive + perms idempotent). Build (tsc -b) + eslint green throughout (only pre-existing useAuth lint). PERMISSIONS.md + SECURITY.md updated. **PDF was deferred-then-built**: storing PDFs as normal Directus files would let any logged-in member read them (member `directus_files` read was unfiltered) — solved with the private folder + surgical member-read exclusion rather than a streaming endpoint. Version 1.7.0 → **1.8.0**.
- **2026-06-24** **Finance reconciliation engine (camt.053/.054 cross-check) + SCOR refs → prod, v1.7.0.** Promoted the dev-only reconciliation stack (L1–L4, built 2026-06-23/24) to prod per user request ("send the crosscheck; camt-file validation later"). **Migrations 130** (`finance_invoices.reference_type` NON|SCOR|QRR) + **131** (`finance_payments` → reconciliation ledger: debtor/reference/unstructured/match_status/clubdesk_guess + unique camt_reference) applied to prod — they were the only pending (prod was at 135 from parallel Spielplanung/SQL work; 130/131 are finance-only + independent, applied out-of-numeric-order safely). **ext:deploy:prod** ships `camt.js` (adds fast-xml-parser), `finance-camt.js` (`POST /finance/camt-import`, `GET /finance/payments`), `finance.js` SCOR generation, `index.js`. Frontend via dev→prod merge: `InvoiceManager` camt upload + reconciliation report, `InvoiceQrBill` now emits `Rechnungsnummer: {n}` (replicates ClubDesk so in-app payments reconcile there), `useFinance.importCamt`, i18n ×5. **Matching ladder**: SCOR ref → invoice number in the Mitteilung (exactly how ClubDesk reconciles — works with NO QR-IBAN) → fuzzy amount+name (flag only) → unmatched; native → auto-confirm (`confirmed_via=camt`), ClubDesk → flag only (never mutated), deduped by bank entry id. **CAVEAT: camt parser NOT yet validated against a real UBS/ZKB export** — shipped dormant on purpose: auto-confirm only touches native invoices (amount + reference guarded) and the ClubDesk cross-check is read-only, so no corruption risk before validation; validate on the first real file. Branches twin/divergent (v1.6.1 Spielplanung present on both via separate commits) → promoted with a merge-tree-clean dev→prod merge. No setup-perms needed (no new collections/grants). **EBICS/API automation researched + PARKED**: ClubDesk can't auto-pull (manual camt upload only); ZKB "Datalink EBICS" (free interface, incl. camt.054) is the path if/when wiedisync goes primary; bLink lacks camt.054 + is TPP-only. dev verified throughout (parser fixtures + live e2e: native auto-confirm by SCOR + by message-number, ClubDesk number-match → invoice 1798, dedup, all cleaned up).
- **2026-06-24** **Spielplanung dashboard counter fix — fixture-aware home/away totals (v1.6.1, dev+prod).** User flagged the dashboard cards showing an impossible **away 75/74**. Root cause: the cards (and the per-team header counter) put a **booking-count numerator** over an **opponent-count denominator** (`gamesTotal = activeOppIds.size`, assuming 1 home + 1 away per opponent) — but multi-game junior pairings (2H+1A / 1H+2A, migration 105) add legs on one side, so confirmed away bookings overran the 74-opponent denominator while home undershot. The exact item flagged "deferred" in the multigame design notes. **Fix**: new read-only `GET /admin/terminplanung/season-summary?season=` (`game-scheduling.js`) resolves each manageable-team opponent's SVRZ fixtures via the existing `opponentSvrzFixtures` helper and tallies per side (`homeTotal = home-side fixtures`, 1-fallback), floored at the confirmed count (`Math.max(fx, confirmed)`) so a stale orphan booking can't read numerator > total — same per-fixture model `finalize-notify`/`invites/remind` already use; returns `{ totals, byTeam }`, scoped via `spielplanerCanManageTeam`, no actor logging (read). `AdminDashboardPage.tsx` fetches it (re-runs on `bookings` change), feeds the Home/Away cards + per-team `homeAwayCounter` (i18n gained separate `{{ht}}`/`{{at}}` totals × 5 locales), falling back to the old client counts until it lands. To-confirm/awaiting untouched (fixture-independent). **Verified on prod DB** by replicating the endpoint's exact logic (incl. static-id `raw->encounter` refinement) in SQL: home **74/77**, away **76/78** — both denominators now ≥ numerator, totals reflect real fixture counts. Build (tsc -b) + eslint green (the new effect adds one more pre-existing `set-state-in-effect` lint, matching the 3 sibling VM-check effects; no hook-rule issues). **Deploy**: `ext:deploy:dev` (smoke ✓), then merge dev→prod + `git push origin prod` (CF Pages) + `ext:deploy:prod`. Version 1.6.0 → **1.6.1**. No schema/migration/perm change.
- **2026-06-23** **Native invoices (Scope C write-path) + Finances nav section + ClubDesk orphan member-linking — v1.6.0 (dev shipped + live-verified; prod promotion same session).** Three asks: (1+2) move Fines into a **Finances** nav group and rename the board dashboard to **Club finances** / member page to **My finances** (`useNavItems.tsx`, `MoreSheet.tsx`, nav+finance i18n × 5); (3) let the board issue invoices to a member **or a team** (e.g. a Swiss Volley fine) payable in-app, with **ClubDesk kept as source of truth**. **Matching detour (rejected, data-backed):** user asked whether to match ClubDesk invoices by a stable ID instead of email — investigated on dev: `finance_invoices.cd_benutzer_id` (the Rechnungen export's `Benutzer-Id`) is the only person-stable field the invoice export carries, but `members` stores no ClubDesk id, so a backfill is circular (needs email/licence anyway) and recovers only **1 of 459** orphans (953 invoices, 494 matched). Orphans are **432/459 non-members** (sponsors/passive/ex-members billed to a parent's email) — no key can attach them. So skipped ID-matching; native invoices set member/team **directly** at creation and reconcile by our own number. **Schema** (migrations **128** native-invoice columns on `finance_invoices` — `team` FK, actor `created_by_*`/`confirmed_by_*`/`reported_paid_by`, `reported_paid_*`, `confirmed_*`, `cancelled_at`; `clubdesk_id` relaxed to NULL so native rows survive the sync's delete+reinsert; `finance_native_invoice_seq`; native lifecycle on the shared `status` col: open→pending_confirmation→paid/cancelled — and **129** `finance_invoice_member_overrides` keyed by email or clubdesk_id; both register fields/relations in `directus_*`, idempotent, applied+recorded dev). **Endpoints** (`kscw-endpoints/src/finance.js`, raw-knex + `writeUserLog` actor capture, Vorstand/recipient-gated in code — NOT items-API): `POST /finance/invoices` (create member|team, generates `N-YYYY-NNNN`), `GET /finance/my-invoices` (own + team coach/captain/TR via server-side union — dodges the M2M policy-walk-empty trap), `POST .../report-paid` (member self-report → pending_confirmation), `.../confirm` + `.../cancel` (Vorstand), `POST/DELETE .../link-member` (orphan link/unlink). **Importer** (`import-clubdesk-finance.mjs`): step **5b** re-applies member-link overrides after the clubdesk delete+reinsert (links persist); step **5c** (Phase 2) auto-confirms a native invoice pending/open→paid (`confirmed_via='sync'`) when its ClubDesk counterpart imports settled, matched **strictly by number** (never amount alone). **Frontend**: `FinancePage` new **Invoices** tab → `InvoiceManager` (New-invoice modal w/ searchable member picker + team select; native-invoice table w/ Confirm/Cancel; unmatched-ClubDesk list w/ Link-to-member + search/cap); `FinanceDuesPage` native pay flow (QR-bill + "I've paid" → pending badge); `useFinance` switched `useMyInvoices` to the endpoint + action helpers; i18n × 5. **Permissions** (`setup-permissions.mjs`): only added Vorstand **read** on `finance_invoice_member_overrides` (admin visibility) — all writes are endpoint-gated; PERMISSIONS.md updated same commit. **Verified on dev**: migrations idempotent (re-run = INSERT 0 0); endpoints mounted+gated (401/403, unknown=404); **live HTTP flow** through the dev container via a temp static token on member 11 (Vorstand, coaches H2) — member-invoice create→my-invoices(open)→report-paid(pending)→confirm(paid), team-invoice create→appears under coach as "H2"→cancel, orphan 1235 link(affected 1)→unlink(cleared 1); all test rows + temp token cleaned up (0 native/0 overrides left, orphan restored to member NULL). Build (tsc -b) + eslint green (only pre-existing `exhaustive-deps` warnings). **Deploy dev**: `db:migrate:dev` (128/129 recorded) + `ext:deploy:dev`; frontend `git push origin dev` (commits `e806de72` feature, `9cc377b6` Phase 2). `db:setup-perms:dev` **deferred** (needs Directus token; non-blocking — feature works without the override-read grant). Prod: `db:migrate:prod` → `ext:deploy:prod` → merge dev→prod; setup-perms:prod + smoke deferred to a token-holder run. Version 1.5.0 → **1.6.0**.
- **2026-06-22** **Junior home-pick slot promotion (Friday → Sunday-cluster → fallback) + cross-team conflict calendar overlay (dev + prod) — v1.5.0.** Triggered by "why so few slots for HU23-1?" — prod investigation found the scarcity is by design + data: the club-wide **Döltschi season cap (10) is full** (`doltschiFull` → all Tuesday slots dropped for every team), all 4 Spielsamstag dates committed (incl. one away game), and **H3 home games eat 5 Fridays** via the cross-team same-day rule (H3 ↔ HU23-1 share 3 full players: Wittlinger 16, Stephan 14, Zbinden 15) + Sportferien KWI A/B closures + the ±3-day gap. **(1) Slot logic** (`game-scheduling.js`, 3 surfaces kept in sync — `GET /terminplanung/slots/:token`, the team-availability copy, and the confirm guard): for junior teams (`/u\d/i`) promote last-resort tiers to **strict** (pick 1 & 2) when the strict pool (own slot / Spielsamstag / Döltschi, counted by **distinct DATES** not rows) can't fill both picks — **Friday Spielhalle** first (when <2 strict dates), then **cluster Sundays** (a Sunday another junior volleyball U-team already plays — new `juniorSundaysInUse()` helper, reuses the existing `preferred` steer — becomes strict so HU20/DU23/HU23 play together), then a **blanket any-Sunday fallback** so a team is never unable to propose (and can seed the cluster). Confirm guard recomputes the same per-tier signals (`homeFridayPromoted` / `homeJuniorSundays` / `homeSundayBlanket`) so submit matches the offer. Verified on prod: HU23-1 went 1→**11 strict dates** (1 Friday 12.03 + 10 Sundays via fallback; no U-team has a Sunday yet so clustering is dormant until one is placed). **(2) Cross-team overlay** — new `GET /terminplanung/admin/cross-team-conflicts?teams=` (reuses `sharedPlayerTeams` + games/booked-slots/confirmed-away, grouped by date w/ matchup + kind + affected team) feeds a `useCrossTeamConflicts` hook → sky **`CrossTeamBadge`** (mirrors `AbsenceBadge`) on month + week day cells behind a "Show cross-team conflicts" toggle (off by default, scoped to selected team(s), disabled until one picked). i18n × 5 locales. **Deploy**: backend `ext:deploy` (dev then prod; endpoint 401 unauth = live both); frontend dev via `git push origin dev` (CF Pages), **prod via surgical promotion** — checked out only the 16 feature+release files from `origin/dev` onto a `prod` **git worktree** (`/tmp/wsprod2`), build-gated (tsc -b) green, committed `c5ae5648` + pushed, **deliberately leaving the concurrent worker's in-progress boot-spinner/diagnostics/bricks commits off** (zero file overlap, confirmed). Version 1.4.0 → **1.5.0** (CHANGELOG + ChangelogPage + package.json). Build + eslint green (the new hook's `set-state-in-effect` lint fixed by deriving `byDate`/`isLoading`, setState only in async callbacks). No schema/migration/perm change. **Open lead for the user**: if HU23-1↔H3's 3 shared players are stale roster entries, downgrading them to guest on H3 would unblock those 5 Fridays.
- **2026-06-22** **RSVP bricks dropped to zero on click: stop clobbering `is_staff` on participation update (dev `8b1165fd` + prod `b84117f5`, frontend) + stray-confirm data cleanup.** Reported symptom: games showed a "yes" the user never set (green banner + ✓1 + "Coach present"), and clicking any Yes/Maybe/No made the participation bricks vanish. Investigation (prod DB + `directus_activity`): the "phantom yes" were **real** `confirmed` rows written by the user's **own** login (member 8, player-coach of H3 92) — **not** auto-confirm (member `auto_confirm_games=false`, team has no `game_auto_confirm`, games have no `auto_confirm_rsvp`) — i.e. leftover test RSVPs (game 421 toggled yes→maybe→yes on 18.06; game 455 a single confirm today). Root cause of the *vanishing*: `useParticipation.setStatus` wrote `is_staff: isStaff ?? false` on **update**, unlike every other RSVP entry point (`GameCard`/`TrainingCard`/`EventCard`/`ParticipationButton` all set `is_staff` **only on create**). When a viewer's role context drifts so `isStaffOnly(team)` flips true (e.g. a season-lagged `member_teams` row drops the team from `memberTeamIds` while `coachTeamIds` keeps it), each click re-tagged the existing **player** RSVP as **staff**, pulling it out of `ParticipationSummary`'s player tally → all three bricks → 0 (`ParticipationSummary` renders nothing at `data.length===0`). Doesn't bite member 8 today (their 92 `member_teams` row is current-season `2026/27`), but a real latent bug for coaches-not-on-roster. **Fix**: omit `is_staff` from the update call (`src/hooks/useParticipation.ts`), preserving the row's original classification — matches the other four call sites. **Cleanup** (user-approved): deleted the two stray `confirmed` rows — prod `participations` 7665 (game 455) + 6231 (game 421); dev had only 6231 (455's row postdates the nightly clone). Build (tsc -b) green, `useParticipation.ts` eslint-clean. Frontend-only ⇒ no ext/schema deploy; both CF Pages auto-build. Promoted to prod by cherry-pick (branches twin-by-message/divergent). No version bump (niche bug fix). **Follow-up (same day):** user reported "yes" still showing green + away games "auto yes". Re-check: member 8 had `confirmed` on **all 17** H3 games — but `directus_activity` showed each was a **manual click** by `luca.canepa@gmail.com` at human pace (e.g. 14:43–14:44, ~3s apart down the list, including re-creating 455/421 right after the cleanup); `auto_confirm_games` still `false` → **not** auto-confirm. Root of the confusion = a **second bug**: participation mutations invalidated only `keys.collection('participations')` (`['participations']`), which doesn't match the combined grid query key `['activities-with-participations', …]`, so an RSVP set from the **detail modal** never refreshed the card grid's counter or the viewer's own "reply" banner (realtime was the only refresh path, unreliable on mobile/PWA) — no feedback, so the user re-clicked. **Fix**: `useMutation` now also invalidates `['activities-with-participations']` on any `participations` write (`invalidateForCollection` helper; dev `5cf3e6c3` → prod `b66fb311`). Cleared all 17 member-8 game confirms on prod (dev re-clones nightly). Build (tsc -b) green, eslint-clean, frontend-only.
- **2026-06-22** **Filter-aware manual-game modal + working "Show absences" overlay (dev + prod, frontend) — v1.4.0.** dev `a29e5851` → cherry-pick prod `0d3c6529` (branches are twin-by-message/divergent, so promoted my single commit onto prod's 1.3.0 base — clean, no conflicts; **not** a wholesale merge). Four asks on the Spielplanung season calendar + `ManualGameModal`: **(1)** the "Add manual game" dialog now seeds **sport / team / home-away from the page's active filters** on open (create mode) — sport via a new selector, team prefilled when exactly one is filter-selected, H/A from the game-type chip. **(2)** New **Sport selector** above Team narrows the team dropdown (volleyball/basketball/all); edit mode derives it from the game's team; changing sport drops a now-mismatched team. **(3)** The **"KWI A + B"** combo hall is now offered for **every** team (was basketball-only) — books both halls and trips the existing `additional_halls` hall-overlap conflict check if either half is taken (relabelled, dropped "(basketball)"). **(4)** The **"Show absences"** toggle was wired to **nothing** (no code read `filters.showAbsences`) — now fetches in-scope absences (`useTeamAbsences`, scoped to selected teams → else the filtered sport → else all), expands them per-day via `absenceCoversActivity` (new `utils/absencesByDate.ts`, dedupes per member, clamps indefinite rows to season), and renders an amber **count badge** (`AbsenceBadge`, Popover lists who + their team on hover/tap) on month-calendar day cells + week-view day headers. i18n en/de/gsw/fr/it (`manualGame.sport`, `absenceBadge.title/aria`). Frontend-only ⇒ no ext/schema deploy; both CF Pages projects auto-build on push. Build (tsc -b) green on both branches; new files eslint-clean; the 7 `set-state-in-effect` lint errors in `ManualGameModal` are the pre-existing form-seeding pattern (6 already on HEAD), not the `rules-of-hooks` crash class. Version bump 1.3.0 → 1.4.0 + changelog (single batched entry). NOTE: the absence overlay does up to ~30 sequential per-team requests when toggled on with no team filter (opt-in, only while ON) — candidate to batch later.
- **2026-06-22** **Spielplanung stale-duplicate opponent merge + orphan-fixture dashboard guard (data fix prod; safeguard dev + prod).** SVRZ renamed VBC Limmattal's DU23-1 opponent mid-season ("VBC Limmattal 1" → "VBC Limmattal DU23-2"), and because the sync keys opponents by `team_name`, the rename made a **new** record (201, with the real open fixtures #408224 home / #408188 away) and **orphaned** the old one (185). The agreed games had been confirmed on 185 (bookings 179 home / 180 away, `svrz_game_id=NULL`) → `vm-push` returned `no_fixture` and `reconcileBookingsToGames` skipped them, so they never reached the calendar though the card looked done. **Prod data fix** (user-approved, backup at `/tmp/limmattal-backup.out`): re-homed 179→#408224 / 180→#408188 onto opp 201 (+`booked`), `POST /kscw/terminplanung/admin/reconcile-games {season:1,silent:true}` (created `games` `vb_408224` home 29.09.2026 20:00 Döltschi 2 + `vb_408188` away 25.01.2027 20:15), `POST /kscw/admin/terminplanung/vm-push {179}` → #408224 pushed (Tue 29.09 20:00, Schulhaus Döltschi), deleted opp 185. Endpoints run in-container via `DIRECTUS_ADMIN_TOKEN`. It was the **only** orphan that season. **Safeguard** (frontend-only, `AdminDashboardPage.TeamBookingsContent`): amber banner (`orphanFixture*` i18n × 5 locales) flags any opponent with confirmed bookings but **0 matching SVRZ fixtures** and names the likely correct sibling (same first-2-token club name carrying fixtures); guarded by a `fixturesLoaded` flag. Detection only — no auto-merge. Reuses the existing `svrz-clubs` data ⇒ no backend/migration; build (tsc -b) green, no new eslint errors. Frontend push to dev + prod (CF Pages). No version bump (user declined).
- **2026-06-22** **Scorer home-team roster + duty calendar events (dev + prod).** Two deliverables for the Schreibereinsätze. **(1) Roster access** — the assigned *Schreiber* (scorer / scorer_scoreboard / bb_scorer — **not** the Täfeler/timekeeper/24s roles) can view the **home (playing) team's** roster (jersey # desc, "Last, F.", **full DoB incl. minors** for match-sheet eligibility) only **±1h around kickoff**, home games only. New time-gated endpoint `GET /kscw/scorer/game/:id/roster` (`scorer-roster.js`, mirrors `scorer-contacts.js` Zurich-window math): resolves caller→member, checks the three SCORER member FKs, gates the window + `type='home'`, returns `{number,last_name,first_initial,birthdate}`, and **audit-logs the read** (`writeUserLog`) since it surfaces minor PII. Frontend: `RosterModal` (shadcn `Table`), a "Roster" button on `ScorerRow` (shown only to the assigned Schreiber in-window via `isWithinGameContactWindow`), and a `/scorer?roster=<id>` deep-link handled by `ScorerPage`. The endpoint is the **only** path that returns full/minor DoB — the public `/kscw/public/team/:id` minor-stripping is untouched; `members` read perms are field allow-lists that exclude the new columns. **(2) Duty calendar** — duties become auto-accepted calendar entries. **Migration 125**: `members.ical_token` (unique, backfilled, hidden field; never in any items-API read allow-list → endpoint-only). iCal feed gains `source=duties` (token-scoped, **not** in `source=all`) emitting `STATUS:CONFIRMED`/`TRANSP:OPAQUE`/`X-MICROSOFT-CDO-BUSYSTATUS:BUSY` VEVENTs with a roster deep-link `URL` + 1h `VALARM`; `GET /kscw/me/ical-token` (lazy-gen) + `POST /kscw/me/ical-token/rotate` (audit-logged). `ICalModal` gets a "Your scoring duties" personal-feed block (built from `window.location.origin` via the existing `/kscw/ical` Pages-Function proxy, which forwards `source=duties&token=` untouched). In-app calendar: new `scorer-duty` `CalendarEntry` type fetched from the member's six duty FKs in `useCalendarData`, wired through `MonthGrid`/`WeekGrid`/`MobileWeekGrid`/`MobileMonthView`/`UnifiedListView`/`CalendarEntryModal`/`CalendarFilters` (indigo) + i18n. i18n en/de/fr/it (gsw inherits de). **Migration 126** (`members.website_name_private DEFAULT true` journal-reconcile, no-op on live) rode along from a parallel edit. **Deploy (schema-first)**: `db:migrate:{dev,prod}` (125+126 ✓; `setup-perms` **not run** — no token in shell, but **no perm changes** so a no-op) → `ext:deploy:{dev,prod}` (restart, smoke ✓) → `db:baseline:prod` regenerated `SCHEMA.sql` (captures `ical_token` + prior 123/121 drift). Endpoints live-checked on both: roster + `me/ical-token` `401` unauth, `source=duties` `200` empty VCALENDAR, `source=all` regression `200`, on-brand proxy `200`. Build (tsc -b) + eslint green. **No permission change, no version bump** (user declined changelog). Prod promotion: **wholesale `git merge dev→prod`** (`bbe0c804`) per user request (dev was 97 ahead / prod 21, twin-by-message); 5 conflicts resolved to dev (authoritative superset) + DEVLOG union; merged tree build-gated green before push. **Not E2E-verified here** (an authed in-window assigned-scorer roster fetch needs a live duty + session the sandbox guards) — verify in-app.
- **2026-06-22** **RSVP declined-count drift fix: guests no longer auto-declined on games (dev + prod).** Symptom: the game card/detail RSVP "bricks" (`ParticipationSummary`) showed e.g. **2 declined** while the roster modal showed **1** — on most/all games of any team carrying a guest with a standing absence (e.g. H3 game 509 `vb_406201`: Alex Leonhardt, `guest_level=1`, auto-declined). Root cause: `autoDeclineForAbsence` (kscw-hooks) inserted a `declined` GAME row for **any** member on the team, missing the `mt.guest_level = 0` guard the auto-**confirm** path has. Guests can never play league games (`trg_participations_guest_block` + UI hide RSVP), and the roster modal already excludes them — but `ParticipationSummary` counts every participation row regardless of roster membership, so each guest row pushed the declined tally one above the roster. **Fix**: added `AND mt.guest_level = 0` to the games auto-decline INSERT (mirrors auto-confirm). **Migration 124** (idempotent data cleanup) deletes historical guest game auto-decline rows — **96 rows** on both dev + prod (7 guests across teams). Also moved the **"Show roster" button** in `GameDetailModal` from the buried bottom "Participation details" section to directly beneath the RSVP tallies (visible for any scheduled game; old section now self-hides when it has no deadline/coach content). Deploy: `ext:deploy:{dev,prod}` (hook first → no new noise) → `db:migrate:{dev,prod}` (124) → frontend push. Verified post-migration: 0 remaining guest game rows, game 509 declined = 1. Build green; eslint errors in `GameDetailModal` are pre-existing (`set-state-in-effect`/`refs`, not in touched code). No perm change, no version bump. **INCIDENT (same day, self-inflicted, resolved ~22 min):** the games-fix commit (`36b68aea` dev / `e76477ee` prod) put backticks in an SQL *comment* inside the `database.raw(\`…\`)` template literal — the backtick closed the template, so `kscw-hooks` failed to register on BOTH dev + prod (`WARN: Couldn't register hook … SyntaxError: missing ) after argument list`, ~07:01→07:23). All automation hooks (auto-decline/confirm, notifications, absence handling, audit) were down for the window; `trg_participations_guest_block` is a PG trigger so guest-confirm stayed blocked. **`ext:smoke` gave a false pass** — it curls a `kscw-endpoints` route, which loads independently of `kscw-hooks`. **`node --check` is useless here** (mis-parses regex literals inside `${…}` template substitutions — flags even known-good code); validate hooks/endpoints with an **esbuild parse** instead (`npx esbuild … --bundle --external:* --outfile=/dev/null`), which pinpointed the backtick. Fixed (backticks → plain text), redeployed `ext:deploy:{prod,dev}`, confirmed clean hook registration in container logs. **Follow-up:** extended the same guard to the **training** auto-decline INSERT — it now respects the per-training `excluded_guest_levels` toggle (correlated jsonb check, mirrors `autoConfirmTraining`) so excluded guests aren't auto-declined either. Events have no guest-exclusion field → unaffected. Prod had **0** existing training noise rows (the lone excluded-guest training decline was a real user decline, `auto_declined_by` null) → code-only, no migration. esbuild-validated; hooks re-registered clean on both.
- **2026-06-22** **Scorer per-duty confirmation (drop "Assigned") + delegation-transfer fix (dev + prod).** dev `c0ebd14b` → cherry-pick prod `ac44260e`. **Per-duty confirm** (supersedes game-level migration 122): a duty is Open or Confirmed (= has a person) — no "Assigned" state; `DutyStatus` = Confirmed only when every applicable duty is filled, else Open. Auto-confirm on assign; de-confirm = clear the dropdown (disabled on past games via `effectiveCanEdit`). **Migration 123**: per-role `*_confirmed_by_name`/`*_confirmed_at` (6 roles), drops the day-old 122 game-level cols; confirmed = member presence (no boolean/backfill); `duty_confirmed` left vestigial. **kscw-hooks** `filter('games.items.update')` stamps who/when per role on member set, wipes on clear; "Confirmed by … · time" shown per `AssignmentEditor` **admins only**. **Delegation fix**: kscw-hooks `action('scorer_delegations.items.{create,update}')` transfer the duty member (+ team for cross-team) to the recipient on accept (the live items-API path only set status) and re-stamp confirmed-by. `db:migrate:{dev,prod}` (123 only) → `ext:deploy:{dev,prod}` (smoke ✓) → frontend push. Build green, new code eslint-clean, no perm change. Built on top of another session's v1.3.0 (disjoint files); no version bump. **Not E2E-verified here** — verify confirm + delegation in-app.

- **2026-06-22** **Planning nav consolidation + PWA breakout (dev + prod, frontend) — v1.3.0.** MoreSheet's two scheduling rows ("Manual game calendar" `gameplan` + "Match scheduling" `terminplanung`) collapsed into one **"Planning"** entry, matching the desktop `TopNav`/`useNavItems` (single `schedulingItem`, role-based target: `/admin/terminplanung` for admin/full-Spielplaner, `/admin/spielplanung` for per-team). New `src/utils/pwa.ts` (`isStandalonePWA` / `openExternalApp` / `handlePWAExternalClick`): when wiedisync runs as an installed PWA, the external Spielplanung-subdomain link now opens in the **system browser** (`window.open(_blank)`) instead of being trapped in the standalone window; normal tabs navigate in place as before (SSO cookie hop preserved). Wired into `MoreSheet` + `TopNav` (both the non-admin top-level link and the admin-dropdown `go()`). `gameplan`/`terminplanung` i18n keys kept (still used by the in-app `SchedulingLayout`). Build green, eslint-clean. Frontend-only ⇒ push only (CF Pages). NOTE: prior-session per-duty-confirm WIP (migration 123 + kscw-hooks + types) left uncommitted in the tree — NOT shipped in this commit.
- **2026-06-22** **Calendar polish (dev + prod, frontend).** `d0c46a5a` (dev) → cherry-pick `52ab9194` (prod). (1) Removed the redundant "Monthly/Weekly/… overview of all events" subtitle. (2) `ViewToggle` now renders a **dropdown on mobile** (the Hall/Calendar/Schedule chips didn't fit), segmented control on ≥sm — shared component, so the spielplanung pages get the same. (3) `MobileMonthView` day panel **collapses 2+ absences into one "Absence: A, B, C" row** (names parsed from each absence title; collapsed row is informational/non-clickable); a single absence is unchanged. Build green; new code eslint-clean. Frontend-only ⇒ push only (CF Pages).

- **2026-06-21** **Scorer surfaces — game-detail contact gate, final/delegation note, English duty columns (dev + prod).** `ff5b943a` (dev) → cherry-pick `b457df15` (prod). (1) **Contact gate**: `GameDetailModal` showed scorer contact to any team coach anytime (`isCoachOf`, independent of admin mode) — aligned to the scorer-page rule (admins anytime; coaches/TR only ±1h of kickoff). Extracted `dateHelpers.isWithinGameContactWindow`, reused in `ScorerPage` (dedup); game-level "Confirmed by" line now admin-only (`isAdmin` prop). Display-only tightening (coaches already read own-team contact via roster). (2) **Modal + info panel**: warning rewritten to clearly state FINAL + delegation-only (5 locales, bold, DOMPurify); same note added next to the fine warning. (3) **English columns**: `scorer-reminders` cron + dead delegation-accept endpoint used legacy German names absent from `games` → aligned to real columns + frontend role keys. Build green; backend `node --check` OK; no residual German names. No schema/perm change ⇒ frontend push + `ext:deploy:*`. **Flagged**: live delegation accept (items-API) never transfers the game scorer (pre-existing; the transfer endpoint is dead code). **Deferred**: per-duty confirmation rebuild ("drop Assigned", per-role confirmed-by admin-only) — migration drafted, not built.

- **2026-06-21** **Scorer duty — record + show who confirmed it and when (dev only).** The scorer card showed "Assigned" (officials filled, `duty_confirmed=false`) vs "Confirmed" (`duty_confirmed=true`) but never the actor/time. Now captures + displays it, like game-scheduling's `confirmed_by_*`. **Migration 122** (`games.duty_confirmed_by_name` + `duty_confirmed_at` + `directus_fields`); **name only** — `games.read` is `['*']` for members so a confirmer's email must not ride along. **kscw-hooks**: `filter('games.items.update')` injects name (accountability→member) + `now()` into the SAME write that flips `duty_confirmed` true, clears both on false — a *filter* not an *action* so it rides the existing payload (a second raw write would re-fire `trg_games_notify` and double-notify the team); writers are only LEADER/admins (`games.update` is `['*']` for them) so the injected fields pass field perms. **Frontend**: `Game` type + `ScorerRow` renders "Confirmed by … · dd.mm.yyyy HH:MM" (`formatDateTimeCompact`); `confirmedBy` i18n in all 5 locales. Build (tsc -b) green; eslint clean (new code); hook `node --check` OK. **dev**: `28d32adb` pushed; migration applied (`db:migrate:dev` → only 122 pending, ✓); `ext:deploy:dev` (directus-kscw-dev restarted, smoke ✓). **Not E2E-verified here** (authed confirm round-trip needs the dev token, which the sandbox guards) — verify in-app. **prod**: promoted same day — cherry-pick `28d32adb` → prod `65ba395c`; `db:migrate:prod` (122 only, ✓) → `ext:deploy:prod` (directus-kscw restarted, smoke ✓) → frontend pushed.

- **2026-06-21** **Scorer official-contact — time-gated to ±1h of kickoff (dev + prod).** Tightening of the entry below (coaches/TR were seeing the assigned official's contact for **all** their duty games, anytime). Now restricted to **1h before the game's start time → 1h after**. Server (`scorer-contacts.js`): skip games outside `[start−1h, start+1h]` — Zurich wall-clock date+time → UTC instant via a DST-correct two-pass offset (unit-checked: summer CEST `19:00→17:00Z`, winter CET `19:00→18:00Z`, date-as-Date, ±55/65min boundaries) — so cross-team official contacts are only returned around the game. Frontend: `showContact` is now **per-game** (`gameInContactWindow` reuses `toUtcIsoFromDatetimeLocal`) — admins still see contact on any game; coaches/TR only in-window, which also gates own-team officials whose contact is already in the base roster query; `useOfficialContacts()` polls every 60s so contact appears/disappears as the window opens/closes. `npm run build` (tsc -b) green; eslint clean on changed code; endpoints live-checked (`401` unauth) on both. **No schema/permission change** ⇒ `ext:deploy:*` only. **dev**: `7c23bf1a` pushed + `ext:deploy:dev` (smoke ✓). **prod**: cherry-pick `7c23bf1a` → `e776db7f` pushed + `ext:deploy:prod` (smoke ✓). **Assumption**: "1h after" = 1h after the **start** time (window can close mid-game for long matches) — flagged to the user; switch to end-based if desired.

- **2026-06-21** **Scorer official-contact visibility for coaches/TR (dev only, extension + frontend).** Assigned scorer/official email+phone was shown only to sport admins. Coaches and team-responsibles now see the contact of officials assigned to games **their team holds the scorekeeping duty for — and only those** (the user's "only for that game" requirement). Enforced server-side by a new scoped read endpoint, **not** a Directus permission row (a per-game contact filter would walk games→members through a policy = the documented "deep filter + policy walk → silent empty" trap). `GET /kscw/scorer/official-contacts` (`directus/extensions/kscw-endpoints/src/scorer-contacts.js`, wired in `index.js`): resolves the caller's coached/TR teams (`teams_coaches`/`teams_responsibles`), finds `type='home'` games where one of those teams owns a specific duty role, and returns `phone`/`email`/`hide_*` for just that role's assigned official (incl. delegated cross-team officials a coach can't read via the items API). No led teams → `{}`; raw-knex read with authz in code, so coaches still can't bulk-read member contacts through the items API; read-only ⇒ no audit/actor capture. Frontend: `useOfficialContacts()` fetches the scoped map (skipped for admins — items-API already — and non-leaders), merges it onto member objects so `AssignmentEditor` renders it (still respecting `hide_phone`/`hide_email`); `showContact` now true for leaders; duty-game visibility widened to coached/TR teams so a **staff-only** coach (not a roster member) also sees their team's duty games. `npm run build` (tsc -b) green; eslint clean on new code; endpoint live-checked (`401 {"error":"Authentication required"}` unauth). **No schema/permission change** (so no migration, no `setup-permissions.mjs` edit). **dev**: `372bb57c` pushed (frontend CF Pages auto-deploy) + `npm run ext:deploy:dev` (directus-kscw-dev restarted, smoke ✓). **prod**: promoted same day — cherry-pick `372bb57c` → prod `635c1ffc`, pushed (CF Pages auto-deploy) + `npm run ext:deploy:prod` (directus-kscw restarted, smoke ✓); prod endpoint live-checked (`401` unauth).

- **2026-06-20** **Error-log-audit prevention hardening: schema-before-extension deploy chain + interleaved perms clears (dev + prod, tooling/script only).** Two follow-ups to the error-log audit below. (1) **Deploy ordering** — added `deploy:dev`/`deploy:prod` (`npm run db:deploy:* && ext:deploy:*`) so a migration that adds a column always lands before the endpoint code that selects it restarts; prevents the 06-19 `public/team` 500 race (`ext:deploy` shipped `members.website_name_private` ~43 min ahead of migration 116). Documented in CLAUDE.md → Migration & Permission Policy §4. (2) **Permission-reconcile window** — `setup-permissions.mjs` cleared ALL policies up-front (§4) then recreated them section-by-section, leaving every policy empty for the whole run; a concurrent `/users/me` during a reconcile resolved the degraded role `['user']` → 403 wall (the 06-19 permission burst, secondary to the container restart). The API-based script can't wrap clear+recreate in a DB transaction, so the **Member + Team Responsible** clears (the policies every user holds) now run at the START of their own recreate sections (§6/§7), shrinking each empty window to its own sub-second section; Public/Vorstand/Sport Admin/Admin/Terminplanung stay cleared up-front (niche/admin-bypass). **No change to the resulting permission set** — pure reorder, no migration needed; `node --check` clean + clear-count audit (each policy cleared exactly once); live-validated on the next `db:deploy` (no admin creds in this env). **dev**: `13b0daea` pushed; **prod**: cherry-picked `363ec59b` pushed. **Not done (needs a call)**: the deeper fix for the container-restart window — frontend resilience so a transient `/users/me` failure during a Directus reload doesn't collapse to `['user']` and fire a 403 wall — left as an optional follow-up.

- **2026-06-20** **Error-log audit (`/kscw-error-logs`) — roster position write-on-read fixed + dashboard reconciled (dev + prod, frontend only).** Full audit of 30 days × 1417 log entries (prod+dev, both projects) → 160 distinct error groups. Platform healthy: exactly **one genuinely-live code bug**. **Fixed**: `useTeamMembers` (`src/hooks/useTeamMembers.ts`) ran the sport-position normalization auto-heal as a **write-on-read** — every stale roster row issued `updateRecord('members', …, {position})` for *any* viewer, so plain members merely opening `TeamDetail`/`RefereeExpenseSection` fired a `members.position` PATCH they aren't granted (leader-scoped to `COACH_TEAM_MEMBERS`) → 28 harmless-but-noisy 403s on 06-19 (page rendered fine; the write was `Promise.allSettled`). Now `persistNormalization` is opt-in (default off); read-only surfaces normalize for display only, and only the already-`isCoachOf`-gated `RosterEditor` persists. Build (`tsc -b`) green. **No version/ChangelogPage bump** — invisible to end users (background error only). **dev**: `1ad80275` pushed; **prod**: cherry-picked `6480baee` pushed (parallel histories — cherry-pick, not merge). **Triage**: the two alarming 06-19 bursts were deploy-window artifacts that self-resolved — public/team(s) 500s (`members.website_name_private` missing until migration 116 applied ~43 min later on prod; a stale dev build selecting non-existent `trainings.hall_address`) and an 81-error permission wall (`setup-permissions.mjs` clear-then-recreate left `/users/me` resolving the degraded role `['user']` during the 08:34 container restart). Both zero-recurrence on 06-20; grants + columns correct in the live DB. **Dashboard reconciled**: bulk-annotated **~752** resolved/transient entries solved across prod+dev (vm_sync `8922e3b1`, `_lte`-on-string `60cf7fef`, fine_rules `teams.member_teams` `c11e96d8`, public/team(s) drift, the 06-19 perm burst, mailbox_sync 404 self-healed on container recreate) + cleaned up the audit's own verification probes — the view now shows only benign baseline (failed logins, validation 400s, mobile "Load failed"). **Flagged + handed off**: kscw-website client telemetry is silently dropped — `public/js/error-logger.js` `navigator.sendBeacon` sends a raw string (Content-Type text/plain), which the JSON-only `/kscw/client-error` parser discards, so the JSONL "single source of truth" has received **zero** website errors (Sentry unaffected); one-line Blob fix delivered as `/tmp/error-logger-telemetry-fix.patch` (repo on `prod` with concurrent edits, left for the user to apply). **Prevention noted (not implemented)**: make `setup-permissions.mjs` clear-then-recreate atomic (build-then-swap) so a restart can't expose an empty policy set; keep `db:deploy` ordered before/with `ext:deploy` so endpoint code never references a column ahead of its migration.

- **2026-06-20** **Minor-protection: no under-18 PII leaves the public API (dev + prod).** Audit found the unauthenticated `GET /kscw/public/team/:id` (`directus/extensions/kscw-endpoints/src/index.js`) returned the **full roster of any team by id with no age filter** — leaking the first names (+ team/age-band, #/position) of ~100 children on the youth teams (U18/U16/U14/U12, Mini) to anyone hitting the URL, even though the website never rendered those pages (BB youth `hasDetailPage:false`; only VB U23/U20 + seniors render rosters). Privacy flags mitigated the worst (yob suppressed via `birthdate_visibility` default hidden, photos via `website_visible` default false, surnames → initial) but full first names + roster membership still left the server. **Fix (3 layers):** (1) endpoint now returns **no roster** for teams underage-by-definition (name `U≤18` or Mini) and **age-filters every other team** by birthdate (`<18` dropped; missing/unparseable birthdate ⇒ treated as minor & hidden — "prove adult or hide"); coaches kept; game-card officials gated by the same age rule (youth scorers no longer named). (2) `setup-permissions.mjs` public `/items/members` read now `_and(website_visible=true, birthdate ≤ $NOW(-18 years))` so an under-18 can never return even if `website_visible` is mistakenly set. (3) **ClubDesk birthdate backfill** — the "hide if no birthdate" rule initially emptied basketball senior rosters (BB had ~no birthdates in `members`); ClubDesk staging (`clubdesk_export.geburtsdatum`, 97% populated) was the source of truth, so backfilled `members.birthdate` from it (NULLs only, licence-then-email match + first-name safeguard, dd.mm.yyyy→ISO) via the Directus API: **246 patched, 0 errors, 0 ambiguous**. Prod after: 348 adults shown (was 148), 120 real minors correctly hidden, 22 rows still birthdate-less (stay hidden per decision). Verified live on prod across youth/U23/senior teams — youth rosters empty, minors stripped, no yob leak, BB seniors restored. Committed `8a48153b` on dev; backend via `ext:deploy:dev`+`ext:deploy:prod`; perms via `db:setup-perms:dev`+`:prod` (477 perms, 0 errors). No frontend/changelog change (website just receives less data). **Follow-up:** make the birthdate backfill part of the weekly ClubDesk sync so new members don't regress to hidden (privacy stays safe-by-default regardless — missing birthdate hides, not exposes).

- **2026-06-20** **v1.2.0 — rankings season selector + multi-season archive (dev + prod).** Swiss Volley publishes new-season standings via `api.volleyball.ch/indoor/ranking` (current-season only; the `season` query param is ignored on our club-scoped key — verified against the Apiary spec, which exposes `season` only on `/indoor/teams`). **Root cause fixed**: the ranking syncs (`sv-sync.js`, `bp-sync.js`) upserted by `(team_id, league)` only, so each new season *overwrote* the prior season's rows in place (season-less captions like "Herren 2. Liga" collide) — only youth/regional groups whose caption bakes in "Saison YY/YY" survived. Now `sv-sync` derives each group's season from its real `league.season` (games feed) and both syncs key upserts on `(team_id, league, season)`; **migration 121** adds a unique index on the triple (0 dup triples pre-verified on dev+prod). **Backfill**: `directus/scripts/backfill-rankings-history.mjs` reconstructs 2024/25 from the per-group endpoint (`games?dateStart/dateEnd` → groupIds → `ranking/{groupId}`), idempotent ON CONFLICT upsert, non-destructive (kept the 8 pre-existing youth rows) — 2024/25 went 78→203 rows on both dev+prod. **Frontend**: season `<Select>` on the Games rankings tab + the Home rankings widget (`useEffectiveSeason` → latest-with-data default; current season offered as a "Data to be shared later by Swiss Volley" placeholder; `formatSeasonLong` for YYYY/YYYY labels). **Why home/website looked "stuck on the new season"**: all 31 active teams already rolled to 2026/27, so anything keyed off `team.season` (the public team endpoint) returned an empty table — the app uses `useEffectiveSeason` so it was unaffected; the website team page was fixed separately (see kscw-website DEVLOG). Validated the new sync path locally against the live API (134 rows, all keyed on the triple, all labelled 2025/26). Bump `1.1.0` → `1.2.0` + CHANGELOG/ChangelogPage. Build + eslint green (0 new findings). **dev**: `f481ea52` (feature) + `cca7e90e` (home) pushed; backend via `ext:deploy:dev`. **prod**: migration `121` applied, `ext:deploy:prod`, 24/25 backfill applied; frontend cherry-picked (`f41448d7` + this batch — dev↔prod are parallel histories, cherry-pick not merge). No frontend default change for the new season (latest-with-data, per product decision).

- **2026-06-19** **Data Health page rework (dev + prod, frontend only).** Reworked the super-admin Data Health page (`/admin/data-health`). **Fixed a data-loss bug**: date-less games were offered as a one-click (and bulk "Fix all") delete on the false assumption they were placeholders — but the Swiss Volley sync legitimately inserts real future fixtures with a pending date, so deleting them destroyed genuine games (incl. duty/scorer assignments) and the next sync just re-created them. Now a non-fixable **warning for manual review**; `checkGames` skips cancelled games for date/time checks (uses the previously-fetched-but-unused `status`). Also: auto-run the read-only scan on mount with the Wiedikon `LoadingSpinner` (load-then-render) + dim cards on rescan (matches InfraHealthPage/HomePage); issues now render in a `<Table>` (per "lists → tables") sorted issues-first with an errors-vs-warnings summary rollup (`aria-live`); a11y pass (`aria-expanded`/`aria-controls`/`aria-busy`, clean header as non-interactive `div`, decorative icons `aria-hidden`); i18n completed — added the full `dh*` block to **fr + it** (were English fallback) and localized the issue labels themselves via a stable `issueKey` (were hardcoded English in ALL locales), fixed Title-Case "Run Scan" → "Run scan", de-CH timestamp via `formatTimeZurich`; hardened `dataHealthChecks` (dropped the silent `.catch(()=>[])` on the coach/TR junction fetches that could false-flag the whole roster; `autoFixAll` now `Promise.allSettled` + reports which records failed). Build (`tsc -b`) + eslint green. **No version bump / ChangelogPage entry** — super-admin-only, not end-user-facing. **dev**: `7426e18e` pushed. **prod**: cherry-picked as `6d82205b` pushed (dev↔prod are parallel histories — cherry-pick, not merge). CF Pages auto-deploys both. No backend/schema change.

- **2026-06-19** **v1.1.0 — app-wide loading UX (dev + prod, frontend only).** Enhanced the shared `LoadingSpinner` (`src/components/LoadingSpinner.tsx`) with a gold progress bar + percentage and rotating playful messages ("Fluttering…", "Bamboozling…", …); md/lg show the bar + messages, `size="sm"` stays a bare mini-spinner (no forced 60vh) for inline/modal use. Reworked the app to **load-all-then-render**: every page now ORs its primary-data `isLoading` flags into one gate so tables/cards render fully formed instead of popping in — safe because TanStack Query reports `isLoading=false` for disabled queries. Touched ~30 files incl. the scheduling AdminDashboard (waits on teams + intra-club games), Home, Absences, the app-boot spinner in `useAuth`, JoinPage, and a parallel sweep of teams/games/scorer/trainings/forms/messaging/events/calendar/fines/spielplanung pages; converted remaining bespoke text loaders to `<LoadingSpinner />`. Bump `1.0.0` → `1.1.0` + CHANGELOG/ChangelogPage. Build green, no new lint errors. **dev**: commits `b34ce1ba`+`70bb8415` pushed. **prod**: cherry-picked as `5bfd4272`+`baf96b9d` pushed (dev↔prod are parallel histories — cherry-pick, not merge). CF Pages auto-deploys both apps (main + scheduling). No backend/schema change.

- **2026-06-19** **v1.0.0 baseline.** Consolidated the entire pre-1.0 development history (wiedisync had reached internal v5.2.0) into a single official **v1.0.0** release: `CHANGELOG.md` + the in-app "What's New" (`src/modules/changelog/ChangelogPage.tsx`, `APP_VERSION` → `1.0.0`) collapsed to one launch entry grouped by feature area, `package.json` bumped `5.2.0` → `1.0.0`, and this DEVLOG reset to a fresh baseline with the prior dated entries moved verbatim to [`DEVLOG-archive.md`](DEVLOG-archive.md). The companion `kscw-website` was re-baselined to `1.0.0` in the same pass. No code/schema/runtime change — docs + version metadata only. Not yet committed/deployed at the time of writing.
- **2026-06-19** Scheduling dashboard: landing-page swap + one-click "Sync with VM" for away games (no app-version bump — admin/spielplaner flow, not member-app-visible; **dev + prod** — frontend + ext deploy both, **no migration / no perms**; dev `3ee89e51`, prod cherry-pick `c0390632`): **(A) Landing/nav** — the scheduling shell's section root `/admin/terminplanung` now renders the **Dashboard** (was the Season Setup page); Setup moved to **`/admin/terminplanung/settings`**, and both are top-level tabs in `SchedulingLayout` (Dashboard · Settings — NavLink `end` on Dashboard so `/settings` doesn't co-activate it — alongside the existing Manual game calendar). Old `/admin/terminplanung/dashboard` redirects to the root (`<Navigate>`), the member-app `SchedulingRedirect` list gains `/settings`, and the redundant in-page cross-links ("Dashboard →" / "← Setup") were removed in favour of the nav. nav i18n `dashboard`/`settings` ×5. **(B) Sync with VM** — extends the away-VM cross-check (`away-vm-check`, entry `d4c47c34`): away games whose agreed slot DIFFERS from VolleyManager (red `mismatch`) AND away fixtures VM has actually scheduled (real date past the placeholder) but where we hold NO confirmed booking now show a **Sync with VM** button — in the top red alert (per row) AND on each away game card (next to the VM badge / in place of "pending"). New **`POST /admin/terminplanung/sync-away-from-vm`**: `{booking_id}` → overwrite the confirmed proposal's slot in place (keeps `confirmed_proposal`); `{opponent_id, svrz_game_id}` → create a confirmed `away_proposal` from VM (mirrors manual-booking's away leg). Adopts VM's Zurich wall-clock `starting_date_time` into `proposed_datetime_<cp>` (+ gym into `proposed_place` from `games.away_hall_json` when present — "if available"), rejects the league placeholder (date ≤ season-open), captures the actor (`confirmed_by_*` + `writeUserLog`), reconciles into `games`, and deliberately does **NOT** push back to VM (date came FROM VM → the sv-sync ownership guard then sees booking==VM, no drift). `away-vm-check` now also returns `unbooked[]` (per-opponent `opponentSvrzFixtures` scan, DB-only / no live VM call, manage-scoped, placeholder-excluded). gameScheduling i18n `syncWithVm`/`vmSynced`/`awayVmUnbookedRow` ×5. No schema change (reuses confirmed_by_*/proposed_datetime_*/proposed_place_*), endpoint code-gated (admin/spielplaner via `spielplanerCanManageTeam`) → **no migration, no setup-perms**. `node --check` + `npm run build` green; only the pre-existing `set-state-in-effect` lint (data-fetch effects, untouched). **Deploy**: dev pushed (`3ee89e51`) + `ext:deploy:dev` (container booted clean — Extensions loaded, `/kscw/public/teams` 200); **prod = cherry-pick `3ee89e51`→`c0390632` in an isolated worktree** ([[lenovoserver-parallel-edits]] — the operator was mid-edit on dev: unpushed `f4ffc570` db work + uncommitted SCHEMA/clubdesk, left untouched) + `ext:deploy:prod` from the worktree (prod container booted clean — Extensions loaded, `/kscw/public/teams` 200). Both frontends auto-build on branch push.
- **2026-06-19** Basketball scorer-duty auto-assignment + dropped legacy `members.licences` column (**v5.2.0**; **dev + prod** — frontend + ext deploy + migration 119 + setup-perms, both envs): `/admin/scorer-assign` now supports **basketball** via a sport toggle. New `AssignmentAlgorithmBb.ts` assigns ONE `bb_duty_team` per home game (the team supplies all 2–3 officials, matching the member UI's `?? bb_duty_team` convention) — hard rules: own-game-same-day / one-duty-per-day / must field an OTR1 member (Anschreiber+Zeitnehmer); soft: **+25 full crew** (also has OTR2/OTN → can self-cover the 24s), +30 adjacent-game-same-hall, −20 training, −10×prior duties (fairness), +5 weekend. Per-game override dropdowns + per-team duty summary, mirroring VB. **Three latent VB bugs fixed**: the page queried only the legacy `licences` JSON so `m.scorer_vb` was undefined → scorer eligibility silently empty (now queries the boolean cols); hall names loaded so the Döltschi rule fires; games sport-filtered so the VB engine no longer scores basketball games (same person-filter fix applied to `ScorerPage`). **Dropped `members.licences`** (migration **119**, superseding the 067/068 dual-write): removed from the `Member` type + every query (`useData`, `ScorerPage`, `RosterEditor`, `useExplorerCache`, `ExplorerMemberFields`), from `kscw-hooks` (registration) + `vm-sync-check.mjs` (VM sync) — both now write ONLY the six boolean flags — and from `setup-permissions`/`setup-schema`; migration drops the column + its `directus_fields` row and **drops/recreates the `members_with_photo` view** (a Supabase-era artifact, not app-used, that selected `m.licences` and would otherwise block the drop), `SCHEMA.sql` trimmed to match. **Deploy**: committed on dev (only my 23 files — left the operator's parallel finance WIP untouched), pushed; **prod = cherry-pick `022db83c`→`2022db86` in an isolated worktree** (diverged finance history — [[lenovoserver-parallel-edits]], cherry-pick-not-merge). Order: ext:deploy (stop dual-write) → **waited for the new CF Pages bundle to go live** so no stale tab requesting `licences` would 400 on the drop (verified via live bundle-hash flip + content grep — caught the finance build going live first) → migrate 119 → setup-perms (**477 perms, 0 errors** both envs). Verified on both: col gone, view recreated+queryable, `directus_fields` row gone, 0 member-perm rows reference `licences`, 6 boolean cols intact. **Open follow-ups** (non-runtime): `clubdesk-diff-queries.sql` (admin debug script) still uses `licences::jsonb` — will error if run, wants a boolean rewrite; regenerate `SCHEMA.sql` canonically via `db:baseline:prod`.
- **2026-06-19** Member IBAN moved from the profile editor to the **Finance tab** + pre-filled from ClubDesk (no app-version bump — UX relocation + data backfill; **dev + prod** — frontend + DB backfill, **no ext/perms change**): IBAN editing left `ProfileEditModal` (field, state, validation, **and the `payload.iban` write** — the last is critical: leaving it would NULL the column on every profile save) and now lives in a new **`PayoutIbanCard`** at the top of the member finance page (`/finance/dues`) — inline add/edit/check via the existing ISO-13616 + mod-97 validator (`src/utils/iban.ts`), saved through the normal items API on the member's own row (members already hold own-update on `members.iban` from migration 117 / setup-permissions — so no perm change). The member finance page + its sidebar/More-sheet nav entry were **renamed "Your dues" → "Finance"** (`myDuesTitle` repurposed ×5 locales, subtitle broadened). The home IBAN-nudge banner now **links to `/finance/dues`** instead of opening the profile modal (dead `ProfileEditModal` import/state/render removed from `HomePage`); banner body copy repointed off "your profile" → "under Finance" ×5. **Pre-fill** = re-runnable **`backfill-member-iban.sql`** (+ `db:iban:backfill:{dev,prod}`) UPDATEs `members.iban` from the already-imported **`clubdesk_export`** staging (the treasurer's `export_all.csv` IBAN column), **NULL-only** (never clobbers a member-entered IBAN) + shape-validated (ISO-13616 skeleton; mod-97 re-checked on the next frontend edit), matched by email / alt-email / licence (same rule as `clubdesk-diff-queries.sql`); safe to re-run after each ClubDesk re-import. Ran both: **dev 38 filled**, **prod 44 filled (+1 pre-existing member-entered = 45)** — all valid 21-char CH. `npm run build` (tsc -b) green; eslint clean on the 4 touched files (the 8 `HomePage` errors are pre-existing `Date.now`-purity / `any` / `no-useless-assignment` debt). Frontend auto-deploys on dev push.
- **2026-06-19** Finance section — full prod cutover + per-invoice QR-bill payment + expense inbox env (in-progress **v5.1.0**; **dev + prod** — frontend push + `ext:`/`scripts:`/`setup-perms:prod` + container recreates): shipped the board finance dashboard tabs — **Income statement** (P&L excludes year-end `Abschluss` closing entries so it equals the booked result; verified the apparent P&L-vs-Jahresgewinn gap = a 2'794.82 profit appropriation booked to `2900`, NOT a bug — the close transfers exactly the 5'891.89 result to equity), **Balance sheet**, a ClubDesk-style **Accounts** drill-down tree (chart of accounts grouped 1/2/3/4/9 → click an account → its ledger: date/text/Gegenkonto/Soll/Haben/running Saldo, via shared `AccountLedger`), inline **P&L + balance-sheet drill-down** (P&L drill passes the Abschluss-excluded set so the ledger reconciles to the shown figure), and a **Sync-status** panel. Member `/finance/dues`: **per-invoice Swiss QR-bill** — tap an open invoice → scannable Swiss QR Code (`swissqrbill/svg`, QR + Swiss cross) pre-filled with the club's **regular** IBAN `CH65 0027 0270 N661 5228 0` (NOT a QR-IBAN → no QRR reference, open debtor) + the invoice's open amount + invoice-no as the unstructured message; pay with TWINT or any banking app, reconciliation stays in ClubDesk + mirrors back on the 04:00 sync. Home "Your dues" card gets a "Pay via TWINT" hint. TWINT QR rendered from the club's payee PDF via `pdftoppm`; the static-total TWINT QR was superseded by the per-invoice QR-bills. swissqrbill: only its SVG path is bundled (the audit-flagged pdfkit-tree vulns aren't in the browser bundle). **Prod cutover**: most was already on prod (cherry-picked through the session); the final `dev→prod` gap was 17 files (website name-privacy toggle + finance-scraper calibration + SCHEMA baseline) — landed as one "bring dev to prod" commit (`34bbc410`) because a merge conflicts on the diverged finance history (cherry-pick-not-merge rule). `ext:deploy:prod` + `scripts:deploy:prod` + `setup-perms:prod` (**477 perms, 0 errors**); DB already at migration **123** (nothing pending). **`FINANCE_INBOX_EMAIL=finance@mail.kscw.ch`** appended to **both** `/opt/directus-kscw{,-dev}/.env` (backed up) + **both containers recreated** (env-file changes need recreate) so the expense-reimbursement "Send to finance" delivers; `EXPENSE_OCR_MODEL` uses its default, `ANTHROPIC_API_KEY` already set. Both containers verified serving post-recreate (`/server/ping 200`, live member `finance_invoices` reads).
- **2026-06-18** Spielplanung subdomain — Phases 3+4: prod cutover + member-nav relink — **LIVE on prod** (no app-version bump — infra/auth; prod cherry-picks of `0f818abe`+`f806db97` → new SHAs `cfad1c12`/`07a1e6a7`; nav `3d9ff7d9` dev / `4be3e06f` prod): **Prod Directus** (`directus-kscw`) recreated with `SESSION_COOKIE_DOMAIN=.kscw.ch`/`SAME_SITE=lax`/`SECURE=true`/`TTL=7d` (**default** `SESSION_COOKIE_NAME=directus_session_token` — distinct from dev's `_dev`, no `.kscw.ch` collision), the existing **explicit CORS allowlist** extended with `https://spielplanung.wiedisync.kscw.ch` + `CORS_CREDENTIALS=true` (NOT reflect — prod stays a strict list), and `AUTH_PROVIDERS`+`AUTH_GOOGLE_*` stripped (operator confirmed no Google-only members). DB snapshot taken first; CORS preflight verified for both prod hosts. **Frontend to prod = cherry-pick, NOT merge**: dev carried the operator's finance `v5.0.0` + calendar/sentry work (already on prod as different SHAs), so a `dev→prod` merge would conflict + ship finance — instead cherry-picked ONLY the 2 auth commits onto prod in an **isolated git worktree** (main tree had uncommitted WIP — [[lenovoserver-parallel-edits]]); `sentry.ts` auto-merged clean. Member prod (`wiedisync.kscw.ch`) now on cookie-auth (verified `wiedisync_auth` in the live `useAuth` chunk); **all members re-login once**. Scheduling prod = `wiedisync-spielplanung` CF Pages project flipped to `prod` branch + custom domain `spielplanung.wiedisync.kscw.ch` + `VITE_DIRECTUS_URL=…kscw.ch` / `VITE_SCHEDULING_ORIGIN=https://spielplanung.wiedisync.kscw.ch`. **Phase 3 nav relink** (`Layout` + `MoreSheet`): the member app's PLANNING items ("Match scheduling" → `/admin/terminplanung`, "Manual game calendar" → `/admin/spielplanung`) now render as external `<a>` to `SCHEDULING_ORIGIN` when it differs from the current origin (SSO makes the jump seamless), else stay in-app (localhost/unset) — requires `VITE_SCHEDULING_ORIGIN` on the **member** project too. **GOTCHA that cost an hour — CF Pages Build cache ships stale `VITE_*`**: the scheduling app kept reading **dev** Directus after `VITE_DIRECTUS_URL` was switched to prod (symptom: opponent "Volleyball-Club Volketswil" showed games *Pending* on the subdomain but *Confirmed* on the member app — proven by `game_scheduling_bookings`: prod opp 190 confirmed, dev opp 190 none). Fix = **Build cache → Clear Cache → redeploy** on the affected project (recommend **disabling** build cache on both). Verify a built bundle's backend by counting `https://directus(-dev)?\.kscw\.ch` in the chunk: prod-baked = `directus.kscw.ch`×2 + `directus-dev`×1. See [[cf-pages-build-cache-stale-env]]. **Still open**: member app keeps the in-app `/admin/terminplanung`+`/admin/spielplanung` routes as a fallback (old bookmarks) — not yet removed/redirected; backend `FRONTEND_URL` still points at the member app (opponent register-confirm links land there, which still works); dev+prod Directus `.env` changes not yet mirrored to the Vaultwarden `*-env` entries (rbw writes need a TTY).
- **2026-06-18** Spielplanung subdomain — Phase 2 of 4: cookie-session SSO + Google/OAuth login removed (no app-version bump — auth infra; **DEV only**, prod cutover pending; commits `0f818abe` (session migration) + `f806db97` (OAuth removal); **verified working on `spielplanung-dev.kscw.ch`**: email/password login → dashboard, httpOnly `directus_session_dev` + readable `wiedisync_auth_dev` cookies set, opponent flow loads anonymously, logout clears): migrated the frontend from `authentication('json')` (access+refresh tokens in localStorage) to **`authentication('session')`** — the token now lives in an httpOnly Secure SameSite=Lax cookie scoped to **`.kscw.ch`** (Directus `SESSION_COOKIE_*`), shared across the member + scheduling subdomains (SSO), unreadable from JS → **closes the localStorage-token accepted risk** (SECURITY.md updated). `credentials:'include'` on rest + auth composable + uploads + every `kscwApi`; **anonymous opponent calls send `credentials:'omit'`** so a logged-in admin's `.kscw.ch` cookie can't trip the public-endpoint global-auth 401 (the original `anonymous`-flag first-load bug). The 4 remaining Bearer-header fetches (`useMailbox`, `AnnouncementsPage`, `InfraHealthPage` ×5, `SqlWorkspacePage` ×3) + the `sentry` client-error log switched to the cookie. A readable, **env-distinct** (`wiedisync_auth` / `wiedisync_auth_dev`) `.kscw.ch` hint cookie replaces the localStorage token for the synchronous `isAuthenticated()` gate — localStorage is per-origin (would break SSO); the cookie is shared, so a sibling-subdomain login is reflected. Remember-me toggle dropped (single 7d cookie). **Google/OAuth login deleted entirely** (`OAuthCallbackPage` + `/auth/callback` route + `useAuth.loginWithOAuth` + `orContinueWith`/`signInWithGoogle`/`oauthError` ×5 locales + changelog/sentry mentions; the button already had no caller). Dev Directus (`directus-kscw-dev`) recreated with `SESSION_COOKIE_DOMAIN=.kscw.ch`/`SAME_SITE=lax`/`SECURE=true`/`TTL=7d`, **`SESSION_COOKIE_NAME=directus_session_dev`** (prevents dev/prod cookie collision on `.kscw.ch`), **`CORS_ORIGIN=true`** (reflect) + **`CORS_CREDENTIALS=true`** (was `*` — illegal with credentials), and `AUTH_PROVIDERS`+`AUTH_GOOGLE_*` (incl. client secret) stripped. CORS preflight from `spielplanung-dev.kscw.ch` verified (creds allowed, origin reflected, not `*`). **Caveats**: localhost `npm run dev` + `*.pages.dev` can't do cookie auth (cross-site to `.kscw.ch`) → auth-requiring dev work happens on `spielplanung-dev.kscw.ch`; the migration invalidates existing sessions (one-time re-login). **TODO before/at prod cutover (Phase 4)**: (a) update the `services/directus-kscw-dev/kscw-dev-env` **vault mirror** to match the VPS `.env` (rbw writes need a TTY → manual); (b) mirror env to prod Directus but **keep CORS an explicit allowlist** (+ scheduling prod origin, `CORS_CREDENTIALS=true`) and use the **default `SESSION_COOKIE_NAME`**; (c) **confirm no prod member authenticates solely via Google** before stripping `AUTH_GOOGLE_*` on prod; (d) flip `wiedisync-spielplanung` to `prod` branch + `spielplanung.wiedisync.kscw.ch` + prod env; (e) set `VITE_SCHEDULING_ORIGIN` + backend `FRONTEND_URL` to the prod scheduling origin; merging dev→prod re-logs-in every member once.
- **2026-06-18** Spielplanung → own subdomain — Phase 1 of 4: dual build target + standalone scheduling-app shell (no app-version bump — build/infra plumbing, member app behaviour unchanged; **dev frontend only**, the scheduling Pages project doesn't exist yet so the new app isn't reachable; commit `e192fe91`): decision recorded in `.planning/2026-06-18-spielplanung-subdomain.md` (gitignored) — move the WHOLE game-scheduling feature (opponent flow + admin dashboard + slot/derby planner) to **`spielplanung.wiedisync.kscw.ch`** as a SECOND Cloudflare Pages project built from THIS repo (same `src/`, NO monorepo, NO fork) for independent deploy/rollback; cookie-session SSO scoped to `.kscw.ch` to follow in Phase 2 (also closes the localStorage-token "accepted risk" in `api.ts`). This commit lands the **build split only**: `main.tsx` now dynamic-imports either `App` (member) or new **`SchedulingApp`** based on build-time **`VITE_APP_TARGET`** (Vite statically replaces the literal → Rollup tree-shakes the untaken branch; verified the scheduling `dist` carries **no member `App` chunk**, ~360 kB gzip vs the member 673 kB). New **`npm run build:scheduling`** + new **`SchedulingLayout`** (minimal shell: logo, role-gated nav, language/dark/logout — deliberately NOT the member `Layout`, whose nav targets member-only routes) + new **`SchedulingHome`** (the `/` dispatcher → login / terminplanung / planner / no-access; **unguarded** to avoid the guard→`/` redirect loop). Opponent-invite links now read a new **`SCHEDULING_ORIGIN`** (`src/lib/api.ts`, env `VITE_SCHEDULING_ORIGIN`) — a **no-op until that env var is set** (falls back to current origin), so existing invites are unaffected; old `/terminplanung/:token` routes stay live on the member app as a fallback. `npm run build` + `npm run build:scheduling` both green; 338/341 unit pass (the 3 fails — `dateHelpers` dd.mm.yy ×2 + `wadmin` knex import — are pre-existing/environmental). NB an earlier same-day standalone-scheduling-app attempt (mentioned under `8c1fc05b`) had been reverted (no stash, no dangling commit) — this is a clean re-implementation. Next: create the dev CF Pages project (`wiedisync-spielplanung`, build `npm run build:scheduling`, env `VITE_DIRECTUS_URL=…dev` + `VITE_SCHEDULING_ORIGIN=…pages.dev`; dev Directus is `CORS_ORIGIN=*` so no backend change), then Phase 2 cookie-session SSO.
- **2026-06-18** Scheduling: per-team Excel/PDF export + attach the report to the notify-coaches email (no app-version bump — admin/spielplaner flow, not member-app-visible; **dev + prod** — frontend + ext deploy both; commit `8c1fc05b`): **(1)** the schedule report engine (rows + Excel + PDF builders) was extracted out of `ExcelExportButton.tsx` into a shared **`src/modules/gameScheduling/lib/scheduleExport.ts`** with an optional `teamId` filter, so the all-teams export bar, the new per-team buttons and the email attachment all build byte-identical files (single source of truth). **(2) Per-team export** — each team's expanded dashboard section gains compact **Excel** / **PDF** buttons in the same action row as "Notify coaches" / "Send reminder", exporting only that team's confirmed+pending games (filename slugged with the team name, e.g. `kscw_spielplanung_vb_H1_180626_1542.xlsx`); disabled when the team has no exportable games. **(3) Notify-coaches attachments** — `handleFinalizeNotify` now builds that team's Excel + PDF client-side and uploads them base64 to `finalize-notify`; the endpoint decodes (whitelisted to xlsx/pdf, capped **4 files / 8 MB**) and attaches them via a new `attachments` param on **`sendSchedulingMail`** (→ `MailComposer`), so coaches can check the dates in the file directly. Best-effort: a client-side generation hiccup never blocks the email (the body already lists every game). i18n `exportExcelShort`/`exportPdfShort` ×5. `npm run build` green, backend `node --check` OK, both containers booted clean (endpoints loaded, server started). NB the working tree also holds an unrelated in-progress "standalone scheduling app" effort (`SchedulingApp/Layout/Home`, `main.tsx`, `api.ts`, `InvitesPanel`, `package.json build:scheduling`) — those were deliberately **left uncommitted**; only the 10 export/notify files shipped.
- **2026-06-18** Manual-booking form: absentees + adjacent-game spacing as a date hint (no app-version bump — admin/spielplaner flow, not member-app-visible; **dev + prod** — ext deploy both + frontend; commit `0a6b84a8`): extends the proposal-review slot context (entry `44b77dab`) to the manual "enter agreed game" form. As the spielplaner types a home/away date, a muted line shows **who'd be absent** + **days since the previous / until the next** scheduled game for that team — indication only (does NOT block the deliberate manual override; complements the existing weekday hint + out-of-window guard). New `GET /admin/terminplanung/date-context?kscw_team=&dates=YYYY-MM-DD,…` returns `{absences, absent_names, prev_game, next_game}` per typed date (≤6); the absence name/count lookup was factored out of `homeProposalHealth` into a shared **`teamAbsenceLookup`**, reused with `teamGameDateList`/`adjacentGames`. Admin/spielplaner-only + `spielplanerCanManageTeam`-scoped; absent names never reach opponent routes. Frontend: `ManualBookingForm` gains an optional `fetchDateContext`, **debounced in the date onChange/prefill handlers (not a useEffect → lint-clean)**, rendering the shared `ProposalContextHints` under each date. `node --check` + `npm run build` green; the only eslint errors are pre-existing `set-state-in-effect` (the fixture-default effects at 130/133 + dashboard 220/233, all untouched). Both containers booted clean, route mounted. Live authed smoke deferred (rbw re-locked) — but `date-context` is directly testable with just a team+date (no pending proposal needed, unlike proposal-health).
- **2026-06-17** Scheduling dashboard: red alert counting away games that DIFFER from VolleyManager (no app-version bump — admin/spielplaner flow, not member-app-visible; **dev + prod**, frontend only — no ext deploy; commit `d4c47c34`): new top-of-dashboard red banner rolling up every confirmed away game whose agreed date/time conflicts with VolleyManager — i.e. `away-vm-check` status `'mismatch'` (red). **"VM not updated yet" (`unset`) is deliberately excluded** per the operator ("VM not updated is fine") — only genuine date conflicts are flagged. Each row shows `team · opponent` + `Agreed dd.mm.yyyy HH:MM · VM dd.mm.yyyy HH:MM`. Frontend-only: the `GET /admin/terminplanung/away-vm-check?season=` endpoint already returns per-booking `{status, agreed, vm}` (scoped to the teams the user manages); the banner resolves booking→opponent→team labels from already-loaded data and filters to `mismatch`. i18n ×5 (`awayVmMismatchAlert`, `awayVmMismatchRow`). `npm run build` green; only the pre-existing `set-state-in-effect` lint at 220/233 (untouched).
- **2026-06-17** Opponent invites + reminders default to the team's OWN responsibles (no app-version bump — admin/spielplaner flow, not member-app-visible; **dev + prod** — frontend + ext deploy both; commit `eeb06008`): clubs that register one big club-wide **Spielplanverantwortliche** list (e.g. **Volley Uster** club 912450 = **25** bulk rows, `team_identifier` NULL) were being blanket-merged onto **every** opponent team's invite, so the 2L game vs Uster's team **454** "took all" instead of just team 454's people. Root cause was NOT the per-team keying (the `tr:t<staticId>:*` rows ARE correct — knex reads `team_identifier` straight from PG, so an earlier "Directus field not registered" theory was wrong) — it was the **default merge**. Fix: **(1)** `SendInvitesModal` default recipient group `'all'` → `'team'`, so an opponent team gets only ITS OWN responsibles; the send endpoint already falls back (`team_contact_email || contact_email`) so clubs with no per-team data still get reached, and the All/Calendar toggles are unchanged. **(2)** Reminder cron (`POST /admin/terminplanung/invites/remind`) was emailing `contact_email` (the full union) + team responsibles on every reminder → now sends to the team's responsibles when present, else falls back to the saved contact. No schema/permission/migration changes. For Uster 454 this drops the ~8 club-level people who actually run Uster's *other* teams (Brasseur, Gasche, Kraus, Andres, Romanutti, Briegoos, Keller, Bühlmann); team-only is still 16 because Uster genuinely tags that many across teams. `npm run build` green (pre-existing `set-state-in-effect` lint in that file untouched); both containers booted clean (`pong`, imapflow/mailparser intact, no log errors).
- **2026-06-17** Scheduling proposal-review: absent players (by NAME) + adjacent-game spacing when confirming a slot (no app-version bump — admin/spielplaner flow, not member-app-visible; **dev + prod** — ext deploy both + frontend; commit `44b77dab`): when the spielplaner confirms one of an opponent's proposed slots, each proposal row now shows decision context from the proposal-health check. **(1) Who's absent — by name** (was a bare count): on the **lenient home pick 3** (picks 1 & 2 are strict = 0 absences by construction) and on **every away proposal** (away dates carry no 0-absence guarantee). **(2) Adjacent-game spacing** on each proposal: "X d since last game (dd.mm.yyyy) · Y d until next game (dd.mm.yyyy)". Backend: `homeProposalHealth` gains `absent_names` + `prev_game`/`next_game` per proposal via new `teamGameDateList` (raw distinct game dates: games + booked slots + confirmed away + confirmed derby legs) + `adjacentGames` helpers, and the absences query now joins `members.first_name/last_name`. It also covers **away proposals behind an `includeAway` flag** — ON only for the dashboard `proposal-health` route, kept OFF for the home-only `request-new-slots` caller so its `find(opponent_id)` still matches the right home booking (away entries have `all_dead:false` and would otherwise wrongly block a re-request). **Absent names are admin-only — never returned to opponent-facing routes** (the slots/:token flow uses a separate count-only path). Frontend: shared `ProposalContextHints` renders the lines under each proposal in both `Home`/`AwayProposalReview` (away review now receives `health`); i18n ×5 (`absentPlayers`, `gapAfterPrev`, `gapBeforeNext`). Verified: `node --check`, `npm run build` green, gap-math unit-checked, both containers booted clean, route mounted; authed dev smoke returned empty (the prod→dev refresh left no pending proposals to populate) but ran without error — the new SQL mirrors the prod-proven `committedGameDates`/`resolveActingUser` queries.
- **2026-06-17** Mailbox compose/reply: rich-text editor + file attachments (no app-version bump — admin mailbox flow, not member-app-visible; **dev + prod** — ext deploy both + frontend; commit `fb0e9551`): the Terminplanung mailbox could only send plain-text replies with no attachments. **(1) Rich text** — the compose body now uses the app's TipTap `RichTextEditor` (bold/italic/H2-H3/lists/quote/link); its output is a constrained, sanitised HTML whitelist. Sent replies now also store `body_html` so they render richly in the detail view. **(2) Attachments** — the `POST /admin/terminplanung/mailbox/reply` endpoint now accepts **`multipart/form-data` (parsed with `busboy`, a NEW extension dep)** so files bypass Directus's 1 MB JSON body limit (`MAX_PAYLOAD_SIZE` unset → default); caps 10 files / 10 MB total, enforced front + back. Sent attachments are appended to the Migadu Sent folder so they're re-downloadable from the message detail. Backend also: defence-in-depth HTML scrub (strips script/style/frames, `on*` handlers, `javascript:`/`data:` URLs — the editor is already constrained, this guards the raw endpoint), plain-text part derived from the HTML for the `text/plain` MIME + search/storage. Plain-text JSON replies still accepted (back-compat). i18n ×5 (`mailboxAttach`, `mailboxRemoveAttachment`, `mailboxAttachTooMany`, `mailboxAttachTooLarge`). Verified: busboy ESM import OK in-container, multipart parse round-trip + sanitiser/html→text unit-checked, both containers booted clean (`~61 routes`), route mounted; live send happy-path left for UI (avoided emailing a real opponent).
- **2026-06-17** Delete-a-confirmed-game + weekday on date inputs (no app-version bump — admin/spielplaner flow, not member-app-visible; **dev + prod** — ext deploy both (delete-game) + frontend; commits `87ea8532` (delete) + `61f34701` (weekday)): **(1) Delete confirmed game** — new `POST /terminplanung/admin/delete-booking { booking_id }` cancels a CONFIRMED booking so the matchup can be rescheduled: under the per-team advisory lock it frees the home slot (a `source='manual'` slot is deleted, a real one set back to `available` — mirrors manual-booking's release path), deletes the booking, and clears the member-calendar mirror (`games` row `vb_<svrz_number>`+kscw_team, never a `completed` row) inside a `kscw.skip_games_notify` txn so the cancellation doesn't fan out a push (a later rebook re-notifies with the new date). Actor captured via `writeUserLog`. **VM is NOT un-pushed** (no VM delete API) — the dashboard delete dialog warns when `vm_push_status` is pushed so the operator removes/reschedules it in VolleyManager by hand. Frontend: a red "Delete game" button on confirmed home/away review cards (`HomeProposalReview`/`AwayProposalReview`) → `useConfirm` dialog (with the VM warning) in `AdminDashboardPage.handleDeleteBooking` → `useAdminBookings.deleteBooking`. The leg then shows Pending again → rebook via the manual form / request-new-slots. i18n ×5 (`deleteGame`/`deletingGame`/`deleteGameConfirm`/`deleteGameVmWarning`/`gameDeleted`). User chose "reopen for rescheduling, no auto-email". **(2) Weekday on date inputs** — new `WeekdayHint` shows the de-CH short weekday (e.g. "Di.", via `formatWeekdayZurich`) next to every native `<input type=date>` in the admin: manual-booking home/away date (below), season open/close + derby-leg date. The shadcn Calendar-popover pickers (away/home proposal forms, Spielsamstage) already show the weekday (`'EEE'` format) so they're untouched. Verified: dev+prod containers booted clean, `delete-booking` route live (400/404 guards probed; destructive happy-path left for UI testing to avoid mutating dev data). `npm run build` + targeted eslint green.

- **2026-06-17** Mailbox search now searches email CONTENT (no app-version bump — admin mailbox flow, not member-app-visible; **dev + prod** — ext deploy both + frontend; commit `35e6f1be`): upgraded the mailbox search (prev entry shipped snippet-only) to full **server-side** search. `GET /admin/terminplanung/mailbox` now takes `?search=` and matches `subject` + `from_name`/`from_address`/`to_addresses` **AND `body_text`** (Postgres `ILIKE`, LIKE wildcards in the term escaped, ≥2 chars, capped at `LIST_LIMIT`=500). Frontend (`MailboxPanel.tsx` + `useMailbox.searchMessages`): the search box debounces 300ms and calls the endpoint **in the change handler (not a useEffect**, to avoid `set-state-in-effect` lint), with a seq guard dropping stale responses; results live in a separate `results` state so the cached `messages` list (and the opponent thread/chip matching) stay intact. UX: "Searching…" indicator, a found-count, no-match state; the "Show all" collapse only applies when not searching. Replaces the earlier client-side snippet-only filter. i18n ×5 (`mailboxSearchCount` → "{{count}} found", new `mailboxSearching`, placeholder now says "content"). Functionally verified on dev AND prod (admin login + endpoint): unfiltered 432/433 → `bestätigt` 85, `Volleyfriends` 12, gibberish 0; both containers booted clean. `node --check` + `npm run build` + eslint green.
- **2026-06-17** Spielplanung emails follow-ups + mailbox search (no app-version bump — admin email/mailbox flow, not member-app-visible; **dev + prod**; commits `cb3933c5`+`aeb3d55e`+`e563f029`): after operator review of the signature (prev entry): **(1) manual mailbox signature is now German-only** (dropped the EN card — Swiss clubs, High German per the email-copy convention); `SCHEDULING_SIGNATURE_LIGHT_HTML` is a single DE card, `SCHEDULING_SIGNATURE_TEXT` German-only. Automated opponent emails keep their per-language dark footer (matches the localised body). **(2) Crest swapped to the official tricolour KSCW shield** (`kscw_email_crest.png`, copied from `kscw-website/public/images/kscw_trio.png`; **new filename** so no client serves a cached copy of the old blue `kscw_email_logo.png`, which was deleted; sized 73×80 for the 1080×1182 aspect). **(3) Mailbox search** (`MailboxPanel.tsx`, frontend only): client-side search box atop the panel filters the loaded ≤500 messages by sender/recipient, subject, preview snippet, and the matched opponent name; result count + no-match empty state; "Show all" collapse only applies when not searching. Full body isn't in the list payload (`GET /admin/terminplanung/mailbox` returns a 160-char snippet), so only the snippet is searchable — a backend `?search=` over `body_text` is the upgrade path if needed. i18n ×5 (`mailboxSearchPlaceholder/Count/Empty`). NB `cb3933c5` was a botched commit (a bad `git add` pathspec captured only the logo deletion); `aeb3d55e` added the actual signature-module edits + crest asset — backend was already correct (ext:deploy rsyncs the working tree). `npm run build` + targeted eslint green. Test re-sent (`mailbox/reply` → id 3292).
- **2026-06-17** Spielplanung emails: KSCW contact signature (no app-version bump — admin/opponent email flow, not member-app-visible; **dev + prod** — ext deploy both + frontend asset; commit `42338d9a`): added the Spielplanung contact signature (KSC Wiedikon · Spielplanung Volleyball · Luca · Martin · Hella · `volleyball@spielplanung.kscw.ch` · WhatsApp) to outgoing scheduling emails, two flavours for the two email families. **(A) Manual mailbox** (`scheduling-mailbox.js` `POST /admin/terminplanung/mailbox/reply`) was **plain-text only** — now sends a light branded HTML part (typed body → `escHtml`+nl2br, then the bilingual DE/EN signature card) AND appends a plain-text signature to the text part; stored `body_text` includes the sig so the Sent record matches. **(B) Opponent-facing automated** (invites, reminders, proposal receipts, confirmations) fold the contact info into the **dark** `buildEmailLayout` footer via a new **opt-in `signatureHtml`** param + `buildSchedSignatureRows(lang)` (localised per opponent language, gsw→de), wired only from `schedEmail`/`buildHtml` + `inviteEmail` in `terminplanung-emails.js`. New shared module `scheduling-signature.js` (`SCHEDULING_SIGNATURE_LIGHT_HTML` bilingual, `SCHEDULING_SIGNATURE_TEXT`, `buildSchedSignatureRows`). **Scope-guarded:** ~10 other features sharing `buildEmailLayout` (newsletter, registration, OTP, events, scorer…) pass nothing → unchanged; internal admin-mailbox alerts + finalize-to-coaches deliberately get NO signature. **Crest is HOSTED** (`public/kscw_email_logo.png` = the official KSC Wiedikon blue crest copied from `kscw-website`, served like the existing email logo) NOT inline base64, since Gmail/Outlook strip `data:` image URIs. **Gotcha:** dev container has no `FRONTEND_URL` (only `PUBLIC_URL=directus-dev`), so email assets resolve to `https://wiedisync.pages.dev`, which (by bundle hash) actually serves the **prod** branch — so the crest only renders in dev OR prod emails once the asset is on **prod** (same reason existing email logos work). Hence promoted to prod for the logo to render anywhere. Verified end-to-end with a manual-path test send (`mailbox/reply` → id 3280) to the operator. NB this session also untangled a stale `git stash pop` ("VM-push WIP", 2026-06-10) left conflicting `CLAUDE.md` + `game-scheduling.js` on the shared tree — resolved by keeping dev (every stashed hunk was superseded by current dev: per-fixture VM push, games reconcile, season-window guard, migration 112/113 confirmer logging), stash retained; my commit was rebased onto a parallel `18b3ee5e` confirm-buttons push. `node --check` on all touched extension files + offline render test green.
- **2026-06-17** Scheduling dashboard: per-opponent Copy link + truthful "remaining games" badge (no app-version bump — admin/spielplaner flow, not member-app-visible; **dev + prod**, frontend only — no ext deploy/migration; commit `f51cd8e9`): (1) **Copy link is now PER OPPONENT TEAM** (correcting an earlier misread that put it on the KSCW-team header). The old team-header "Copy link" copied a public `?team=` *self-registration* link via `copyTeamLink`; removed it (+ its now-orphaned `copyTeamLink`/`copyTeamLinkHint` i18n ×5). Each opponent card in the expanded team view now has its own "Copy link" (`copyOpponentLink` in `TeamBookingsContent`), copying the opponent's tokenized `…/terminplanung/<opp.token>` URL — exactly mirroring the invites-section button (reuses existing `copyLink`/`linkCopied` keys), placed next to the opponent's inline games/email actions. (2) **"Remaining games" badge fix** — the collapsed team-row blue badge counted `notProposed` (opponents with *zero* bookings), so a team whose home legs were confirmed but whose away legs were never proposed showed only the green "confirmed" badge and *looked* finished (the HU20 "says done but 3 missing" report). Blue badge now shows `teamPending` (matchups still missing a confirmed game — the app's canonical "done?" metric, same one behind `finalizeNotifyReady`/`Pending`); dropped the misleading green "confirmed" badge; a team that is actually finished (`pending === 0 && gamesTotal > 0`) now gets a **green row background** (matching the opponent-card all-confirmed tint) instead. Yellow "to confirm" badge kept (and since `toConfirm > 0 ⟹ pending > 0`, a green row never carries leftover badges). New `remainingGamesHint` i18n ×5. Caveat: at the collapsed-header level the page only has booking data (SVRZ fixture totals are lazy-loaded per expanded team), so `teamPending` assumes 1 home + 1 away leg per opponent — same assumption as the existing "H x/y · A x/y" counter; junior multi-game pairings can still under-count, but it's a strict improvement over the green-badge behaviour. `npm run build` green; targeted eslint clean (only pre-existing `set-state-in-effect` debt at lines 220/233, untouched). Promoted to **prod** via fast-forward (CF Pages auto-build).
- **2026-06-16** Scheduling mobile-UI polish (no app-version bump — admin/spielplaner + opponent flow, frontend only, **dev + prod**): fixed the **Mailbox panel list** running off-screen on phones — shadcn `<TableCell>` defaults to `whitespace-nowrap`, so the subject cell (only `align-top`) forced the email subject onto one line and overflowed the viewport while the full `dd.mm.yyyy HH:MM` date ate the left column. Rebuilt the row mail-client-style: subject cell `whitespace-normal` (wraps), date folded into a top-right slot beside a truncating sender on mobile (each keeps its own table column on `≥sm`), snippet now shown on mobile too, unread dot aligns to the first line. Then ran a parallel **mobile-audit sweep** over 25 gameScheduling surfaces (finder + adversarial verifier per file) → **36 confirmed defects fixed across 18 files**: same nowrap-overflow class in `InvitesDrawer` games-dialog matchup cell + `TeamAvailabilityDialog` hall cell (`whitespace-normal break-words`); non-stacking toolbars stacked on mobile (`AdminDashboardPage` action row, `InvitesPanel` status+buttons, `VmPushStatus` `flex-wrap`) via `flex-col … sm:flex-row`; secondary table columns hidden on phones (`InvitesDrawer` Contact + Source → `hidden sm:table-cell`); `PublicTerminplanungPage` Cloudflare Turnstile `size:'normal'`→`'flexible'` (300px widget was overflowing the captcha card); and ~20 sub-44px touch targets bumped to `min-h-11 sm:min-h-0` (preserving desktop density) across Manual/Away forms, OpponentNotes, DerbyPanel, SeasonConfig, ExcelImportPanel, SendInvitesModal, SlotGenerationPanel, TeamSlotConfigPanel, SpielsamstageEditor, MailboxPanel attachments. `npm run build` + targeted eslint green (only pre-existing `set-state-in-effect` debt). Committed + pushed to dev by the shared-tree process; promoted to **prod** via fast-forward (frontend-only — CF Pages auto-build, no ext/migration deploy).
- **2026-06-16** Scheduling actions now feed the superadmin audit log (no app-version bump — backend only, **dev + prod** ext deploy): the `/admin/audit-log` page reads `user_logs`, which the kscw-hooks `items.*` audit hook auto-feeds for everything going through the Directus items API — but the scheduling endpoints write raw knex, so confirm/manual/block actions never appeared (the gap behind "why was there no log of who did what"). New shared `writeUserLog()` helper (`kscw-endpoints/src/activity-log.js`) resolves the acting member and inserts a `user_logs` row; wired into `confirm-home`, `confirm-away`, `manual-booking` (both legs) and `block-slot`, so those now show up in the audit-log page (collection `game_scheduling_bookings`/`game_scheduling_slots`) alongside every items-API action. No new page — the superadmin viewer already existed (nav → Audit log). CLAUDE.md actor-capture rule updated to point at the helper. App-wide coverage of OTHER custom raw-knex endpoints (broadcast already self-audits; others TBD) is the remaining sweep.
- **2026-06-16** Infra: daily prod→dev DB refresh cron (no app-version bump — ops only, **VPS**): checked prod vs dev Directus — **schema identical** (both 118/118 migrations, 70 collections / 874 fields / 135 relations / 506 permissions / 9 policies), only **data** had drifted (prod 526 members / 507 games / 103 scheduling bookings / 74 opponents vs dev 524 / 400 / 7 / 23). Ran a full `npm run db:refresh-dev` (dev now == prod, all 526 member emails scrubbed to `@devsink.invalid`). Then **added a daily scheduled prod→dev sync** (previously on-demand only): new version-controlled `directus/scripts/refresh-dev-daily.sh` (self-contained VPS-side clone+scrub+repin, no SSH/npm, **no migrate reconcile** since the clone carries prod's full schema+tracker; rollback-on-failure restores dev from the safety dump so a bad nightly run leaves dev online on prior-day data; 7-day retention on `/data/backups/kscw_dev_pre-refresh_*.sql.gz`), deployed via `npm run scripts:deploy:dev` → `/opt/directus-kscw-dev/scripts/`, wired into the root crontab `0 3 * * * … >> /data/backups/refresh-dev-daily.log 2>&1`. Test-ran the cron script once end-to-end (gate passed, dev healthy, HTTP 200). Documented in `INFRA.md → Dev DB refresh from prod`. During active migration dev (dev schema ahead of prod), re-run `npm run db:deploy:dev` after a nightly sync or pause the cron. **NB:** `refresh-dev-daily.sh` is committed but not yet on `prod` branch — repo copy + crontab are the record.
- **2026-06-16** Scheduling: confirmer timestamp + derby reserves KWI A+B + "Reserved for BB" mislabel fix + actor-logging rule (no app-version bump — admin/spielplaner flow; **dev + prod** — migration 113 + ext deploy + data fix + frontend): (1) **confirmed_at** (migration **113**, schema-only) — dedicated timestamp set at confirm time in confirm-home/away + manual-booking (`date_updated` drifts on a later VM push); appended to the dashboard "Confirmed by …" line via `formatDateTimeCompact`. Backfill `confirmed_at := COALESCE(date_updated, date_created)`, but ALL booking rows have NULL Directus auto-timestamps (raw-knex inserts bypass the items service), so ~51 prod rows have no recoverable confirm-time (show operator without a date); the rest got `date_updated`. (2) **Derby reserves the gym A+B** — the H1↔H3 derby holds one KWI court (`source='derby'` slot, KWI B) but the OTHER courts were left available / blocked-as-BB. Blocked the `available` KWI A/B slots on both confirmed derby dates (23.09.2026, 08.01.2027) so the whole A+B gym is held for the derby; KWI **C** stays free (separate court — H1 keeps it on 08.01). Data op only touched `available` slots (no bookings lost); a durable generate-slots rule is a follow-up. (3) **"Reserved for BB" mislabel** — `SchedulingCalendar` labelled EVERY blocked slot "Reserved for BB"; D1's KWI B block on 23.09 is actually a derby hall conflict. Now a blocked slot on a derby date (a `source='derby'` slot exists that day) reads **"Reserved for derby"** (`reservedForDerby` ×5); genuine Friday KWI blocks still read "Reserved for BB". (4) **Actor-logging rule** — CLAUDE.md Key Patterns now requires every state-mutating custom endpoint to capture WHO (raw-knex writes bypass `directus_activity`). **Still open**: align the home VM badge/Re-push row with the away VM badge; build a superadmin app-wide activity-log viewer.
- **2026-06-16** Scheduling dashboard polish: admin-banner hidden on Terminplanung, non-assigned-gym warning, home/away calendar glyphs, confirmer logging (no app-version bump — admin/spielplaner flow, not member-app-visible; **dev + prod** — migration 112 + ext deploy + backfill + frontend): (1) **Admin-mode banner** — the gold "Admin mode" banner is now hidden on any `/terminplanung` page (`Layout` checks `location.pathname`); the toggle itself is untouched, only the banner is suppressed there (spielplaner tool has its own per-team scoping, so the banner was just noise). (2) **Non-assigned-gym warning** — the manual-booking form shows an inline amber note (`manualHallMismatchWarn` ×5) when the chosen home hall ≠ the team's open-slot gym (`defaultHomeHall`); warn-not-block, since manual entry is a deliberate override. (3) **Calendar glyphs** — `SchedulingCalendar` pills drop the `@` away prefix for lucide icons: `House` (home) / `Plane` (away), on confirmed + proposed, booking + games entries; tooltips unchanged. (4) **Confirmer logging** — new `confirmed_by_name`/`confirmed_by_email` on `game_scheduling_bookings` (**migration 112**, schema-only + `directus_fields` registration, fields:['*'] read → no perm change) captured from the authenticated member (`resolveActingUser`) in `confirm-home`, `confirm-away` and both `manual-booking` inserts; dashboard shows a "Confirmed by …" audit line on confirmed home/away cards (`HomeProposalReview`/`AwayProposalReview`, `confirmedBy` ×5) next to the existing "Proposed by" opponent line. Existing rows backfilled to the operator (member 8 Luca Canepa, matched by name → env-stable): **2** confirmed rows on dev, **102** on prod (0 confirmed rows left unattributed).
- **2026-06-16** Scheduling manual-game form polish + `team-availability` 404 fix (no app-version bump — admin/spielplaner flow, not member-app-visible; **dev + prod**, frontend only — no ext deploy/migration): (1) **Pre-fill on overwrite** — selecting a fixture that already has a confirmed booking now seeds the manual-booking fields from the existing booking (home: date / game-start via `gameStartForDate` / hall; away: date / time / place, sliced from `proposed_datetime_<n>` like `AwayProposalReview`), so re-entering an agreed game is "tweak the time", not retype. Derived in `AdminDashboardPage` (`homeLegPrefill`/`awayLegPrefill`), passed per fixture as `ManualFixtureOption.prefill`; the form applies it on checkbox-tick or "which game" change (event-driven, not effect-driven, to dodge the new-array-every-render clobber). (2) **Home hall defaults to the open-slot gym** — `defaultHomeHall` = most common hall among this team's `available` slots; pre-selected for a brand-new home game and floated to the top of the hall dropdown with an "(Open slot)" marker (`manualOpenSlotHall` ×5). An overwrite's own hall still wins. (3) **End-time field removed** — home leg now asks only for a start; end is derived (`plus90`) on save (backend `manual-booking` already treats `end_time` as optional). (4) **`team-availability` 404** — `TeamAvailabilityDialog` called `/admin/terminplanung/team-availability` but the route is registered `/terminplanung/admin/team-availability` (segments swapped); confirmed same mismatch on `prod` (backend route already deployed) → frontend path corrected, no backend change.
- **2026-06-16** Scheduling opponent page: capture WHO confirms + always-3 slot pickers + manual-game date guard (no app-version bump — admin/opponent flow, not member-app-visible; **dev + prod** — migration 111 + ext deploy + frontend): (1) **"Who is confirming" modal** — the opponent page's "Confirm home/away games" buttons now open a modal asking the opponent-club person's **name + email** (both required, validated front+back) before submitting. Stored **per booking** (new `proposed_by_name`/`proposed_by_email` on `game_scheduling_bookings`, migration **111**, schema-only, fields:['*'] → no perm change) since home/away are separate submissions and the invited `contact_email` is the club's contact *list*, not the individual. `propose-home`/`propose-away` require + persist it, the proposal **receipt email now goes to that person** (CC's the club contacts), and the admin dashboard shows a **"Proposed by: name · email"** line on each pending/confirmed proposal (`HomeProposalReview`/`AwayProposalReview`). (2) **Always show all 3 slot pickers** — `HomeProposalForm` previously hid slot 3 (and sometimes 2) via `Math.min(3, availableDays)`; now all 3 rows always render (away already did), still min-1 to submit. (3) **Manual-game date guard** — `manual-booking` rejects any home/away date outside the season offer window (auto-tracks `season_opens`/`season_closes`, currently 14.09.2026–14.03.2027) to stop typos like 10.02.2026; the dashboard date inputs also get `min`/`max` + a clear toast. (4) **Mobile fix** — Excel/PDF export buttons stack vertically (full-width) on mobile so "Download PDF" no longer overflows. i18n ×5 (`confirmer*`, `proposerRequired`, `confirmAndSend`, `proposedBy`, `manualDateOutOfWindow`).
- **2026-06-15** Scheduling dashboard: phantom duplicate opponents + away-date placeholder (no app-version bump; **dev + prod** — 2 ext changes + frontend): (1) **Duplicate opponent cards** — `POST /admin/terminplanung/invites` deduped by exact `contact_email`; the per-team-responsibles merge (next entry) grew the contact set (2→6 for Wetzikon DU23-2), so the email-string match missed the existing invite and re-inserted it → 8 phantom `invited` rows (ids 193-200) accreted on DU23-1, each shadowing a real `booked` row so confirmed games showed "Pending" on the dashboard. Dedup now keys on opponent **team name** within (kscw_team, season, active statuses) and refreshes contacts in place (commit `0ee7d1e8`). Cleaned the 8 prod phantoms (richer contacts merged onto survivors first; 0 bookings lost). NB cross-team same-club rows are NOT dups (H1/H3/HU23-1/HU20/Legends/DU23-1 all play e.g. Wetzikon H1 — different `kscw_team`). (2) **Admin "N games" modal** overlays the confirmed away booking's agreed date over the unscheduled SVRZ placeholder (shows `06.03` not `14.09`), read as wall-clock so it matches the member calendar (18:00, not a tz-shifted 19:00). (3) **sv-sync guard** — a tool-scheduled game (confirmed booking) keeps its agreed date/time/venue until it's played; the national feed can no longer regress it to the league's unscheduled placeholder (root of "updated away dates don't reflect"). Scores/status still sync; a real reschedule flows once completed. `svrz_games` stays a raw VM mirror (away dates lag until the opponent schedules in VM — member away dates come from the booking→games mirror, which is correct). Commit `6403d1e8`.
- **2026-06-15** Scheduling contacts: calendar responsible + team responsibles per team (no app-version bump, backend only, **dev + prod** — migration 106 + ext deploy + scripts rsync): opponent contacts were either/or — the club-level Spielplanverantwortlicher (calendar responsible) always WON, and the per-team Teamverantwortlicher was pulled only as a FALLBACK for clubs with no calendar responsible. So the person who actually handles a given team's scheduling was never added when the club had a calendar contact. Now MERGED (calendar + team responsibles, deduped by email) and **per team**: the scraper (`fetchTeamResponsibles` in `svrz-scheduling-sync.mjs`, replaces `fetchTeamResponsibleFallbacks`) harvests `getTeamContactInfosByGame` for EVERY opponent team (one live VM call per opponent team, not just clubs lacking a contact) and stores them keyed by the opponent team's `staticTeamIdentifier` (synthetic `tr:t<id>:<email>`, new `team_identifier` column via migration 106; falls back to `tr:c<club>:<email>` + NULL team_identifier = club-wide when raw lacks the id). Both no-login (`resolveSyncedOpponents`, feeds svrz-clubs auto-fill + ensure-from-svrz auto-create) and live (`import-from-svrz` "Refresh from SVRZ") paths union club calendar contacts with the team's own responsibles; legacy NULL-team_identifier tr rows still apply club-wide. Heads-up: the heavy SVRZ sync now makes ~1 extra VM call per opponent team (~+1 min, more load on volleyball.ch). migration 106 = schema-only (column + directus_fields registration so the REST-writing script can persist it; svrz_spielplaner_contacts is endpoint-gated → no perm change). Unit tests for the per-team keying in `svrz-scheduling-sync.test.mjs`.
- **2026-06-15** Scheduling opponent page: split home/away into separate submits (no app-version bump, frontend only, **dev + prod**): the opponent invite page (`OpponentFlowPage`) had a single "Confirm slots" button requiring EVERY shown home + away card complete before it enabled, forcing the opponent to fill 3 home + 3 away in one sitting. Now the page is split into a **Home games** section and an **Away games** section, each with its OWN confirm button directly beneath it — **Confirm home games** (`proposeHome` per home card) and **Confirm away games** (`proposeAway` per away card) — each enables/submits on its own, so an opponent can send home now and away later. The free-text remark sits last with its own **Save remark** button (shown when edited; reuses existing unused `saveRemarks`/`remarksSaved` keys) since there's no combined button to carry note-only edits; it also still saves alongside whichever side is submitted. Section headers `homeGamesTitle`/`awayGamesTitle` ×5. The two sections render side by side (2-col on `lg`, stacked on mobile) and each card shows the SVRZ fixture number (`#<number>`, threaded from the `slots/:token` games payload through `InviteGame.number` → `LegCard.number`). No data loss between sides (the other side's form state survives the post-submit refetch). Backend untouched — `propose-home`/`propose-away` were already independent; admin dashboard already tracks the two as separate bookings. i18n `confirmHomeGames`/`confirmAwayGames` ×5.
- **2026-06-12** Saturday-cap counter scoped to the scheduling season (no app-version bump, backend only, **dev + prod** ext deploy): `committedSaturdayDates()` counted every dated KSCW-home `games` row with no season filter — until the club rollover runs, LAST season's games sit on the ACTIVE team ids, so 2025/26 Saturdays ate the 2026/27 allotment (D4/H2 at 2/2 with zero real bookings → no Saturday offers at all; D3 at 4/3 with one). New `seasonFloorYmd(seasonId)` resolves July 1 of the season's start year from `game_scheduling_seasons.season` (falls back to the open season) and floors both the booked-slots and games queries; all 5 call sites (proposal-health, team-availability, slots/:token offer, propose-home guard, confirm-home txn) pass the season through. The away side was never affected (no away counter — just the per-proposal max-1-Saturday rule A3). Verified live on both envs: prod now D1 1/2, D3 1/3, D4 0/2, H2 0/2. Self-heals at rollover but needed for every pre-rollover scheduling window. **Same day**: availability dialog now also shows the informational Saturday AWAY count — new `committedAwaySaturdayDates()` (KSCW-away games rows + confirmed away proposals, same season floor) → `saturday.away_used` on team-availability + `availabilitySaturdayAway` line in `TeamAvailabilityDialog` (i18n ×5). No cap on away Saturdays — display only.
- **2026-06-12** Manage coaches & team responsibles from the app (no app-version bump, frontend only): new admin-only "Manage staff" button on the team page Coaches section header (section now renders for admins even when a team has no coaches yet) opens `ManageStaffModal` — current coaches + team responsibles listed with confirm-gated remove, plus a per-section active-member search to attach ANY club member as coach/TR **without needing a roster row** (previously Directus-admin-only; the MemberRow role dropdown still covers roster rows). Writes `teams.coach`/`teams.team_responsible` in junction-object format (`{ members_id }`), so the role-sync hook grants/revokes the LEADER policy as usual; staff lists resolve names from an unfiltered members fetch so inactive-membership staff still display. i18n ×5. Dev (frontend push).
- **2026-06-12** v4.29.0 — Scheduling: booked games mirror into `games` + per-team availability view + SVRZ game numbers (**dev + prod**, ext deploy + frontend both, no migration): (1) **booking→games mirror** — confirmed bookings create/update `games` rows in the exact sv-sync shape (`game_id vb_<svrz_number>`, source `swiss_volley`; weekday home 20:00 rule; away date/time read lexically from the confirmed proposal) so booked matches show on member calendars before the SV national feed serves the season (root cause of "calendars show derbies but not matches" — the derby rows were hand-created in that shape, regular bookings never became games rows; vm-push only writes VM). `reconcileBookingsToGames()` fire-and-forget after confirm-home/confirm-away/manual-booking (normal new-game notify) + admin `POST /admin/terminplanung/reconcile-games` backfill (silent via migration-095 GUC). Create/update only, never deletes, skips `completed`; sv-sync later adopts by game_id. Prod backfill ran 2026-06-12. (2) **Available slots** dialog per team card (`TeamAvailabilityDialog`): offerable home slots tiered Any-pick / 3rd-pick-only + away blocked-date ranges + Saturday cap info, Copy-as-text (opponent-email-ready) + CSV. Backed by `GET /admin/terminplanung/team-availability` — a no-opponent-context **copy** of the slots/:token offer computation (verified byte-identical on dev) — KEEP THE TWO IN SYNC when touching offer rules. (3) `svrz_number` on fixture payloads; games modal rows stacked two-line with `#number`, leg labels show number. (4) Dashboard **search box** filters opponent cards across all teams by opponent/club/contact/any booking date (dd.mm.yyyy + dd.mm.yy + ISO forms); matching teams force-expand with only matching cards. i18n ×5.
- **2026-06-10** Mailbox chip + compose subject (no app-version bump, frontend only): the dashboard mailbox matched messages to opponents by sender address alone, and one club contact often serves several opponent rows (one per KSCW pairing) — first match won, so a "VC Tornado Adliswil H2" mail chipped as D1. New `bestOpponentForMessage` disambiguates by team/club name in subject+snippet (longest match wins, so "…Adliswil H2" beats the shared club prefix); chip + thread title now prefer `team_name`. Composing from an opponent's thread pre-fills the subject `{opponent team} – {KSCW full_name} / Spielplanung {season}` (editable; outgoing mail thereby self-labels for the chip too). Unit tests in `useMailbox.test.ts`. Dev + prod.
- **2026-06-10** Scheduling: multi-game per opponent (no app-version bump; **dev + prod** — migration 105 + ext deploy + frontend on both): junior leagues are a triple round-robin (pairing played 3×: 2H+1A or 1H+2A — vs HU23-1: Wädi/Wetzikon/Limmattal 2 home each, Rämi/Uster 2 away), but bookings were keyed `(opponent, type)` → only 1 home + 1 away schedulable; the pairing's extra game couldn't go through the tool at all. Now keyed per SVRZ fixture: migration **105** adds `game_scheduling_bookings.svrz_game_id` (= `svrz_games.svrz_persistence_id`, soft ref; partial unique `(opponent,type,svrz_game_id)`; backfill assigns existing bookings to the FIRST fixture of their side — 40/40 matched on prod dry-run; fields:['*'] → no perm change). New `opponentSvrzFixtures()` (season start-year + our-side `staticTeamIdentifier` check — also fixes `slots/:token` leaking the OTHER KSCW team's fixtures when two of our teams share a group, e.g. H1/H3 in 2L); `propose-home/away` + `manual-booking` accept `svrz_game_id` (absent → first fixture, legacy-compatible; NULL booking = first fixture of its side everywhere); cross-game slot-reuse guard; `request-new-slots` scoped per `booking_id`; confirm-home/manual VM push forces the booking's fixture (`FORCE_SVRZ_ID` → no more `needs_pick` for multi-game opponents); finalize-notify "Noch offen" counts per fixture. Opponent page: one card per fixture ("Spiel 1/2" suffix), per-card picks, combined submit, dup-slot guard front+back; orphan bookings (fixture re-synced away) never vanish; non-SVRZ opponents keep the legacy 2-card flow. Admin: per-fixture Home/Away rows per opponent card, card colour = ALL games confirmed, ManualBookingForm per-leg fixture picker. Per-team guards (gap/Sat-cap/cross-team/Döltschi/derby) unchanged — a booked sibling game already counts as committed. i18n ×5 (`gameN`, `manualWhichGame`, `duplicateSlotAcrossGames`). Verified e2e on dev (HU23-1 vs Wädivolley HU23 token: 2 home + 1 away cards, separate bookings 12/13/14, 400 on slot collision + bogus fixture, stable booking id on re-propose; test opponent id 68 `mgtest-…` left on dev for admin-card inspection). Dev: migration + ext deploy + frontend push (`4d0179be`) done; setup-perms skipped (vault locked, 105 has no perm change). Prod 2026-06-10: migration 105 + ext deploy + frontend merge (`ded94d01`); first setup-perms:prod run half-failed mid-recreate (Team Responsible left 4/106 rows — the known transient-502 mode), caught via per-policy count diff vs dev, re-run → 469/0, prod per-policy counts now identical to dev.
- **2026-06-10** v4.28.0 — Confirmed home games auto-push to VolleyManager: `confirm-home`/`manual-booking` fire-and-forget `scripts/vm-push-game.mjs`, which writes date/time/hall into VM (volleymanager.volleyball.ch) via the writable indoor game API (`validateGame` → `updateGameAndIgnoreErrorCodes`), status kept `open`, **never finalizes** (no `validateGames`/`waitingForApproval`). Fixture matched by OUR team's `staticTeamIdentifier` (`teams.team_id` minus `vb_`) vs `svrz_games.raw.encounter.teamHome.staticTeamIdentifier`: 1→auto, 0→`no_fixture`, 2+→`needs_pick`. Migration **104** (`halls.vm_hall_id` backfilled KWI A/B/C + Döltschi 1/2→single VM Döltschi uuid; `game_scheduling_bookings.vm_{game_id,pushed_at,push_status,push_error}`; both `fields:['*']` → no perm change; already on prod DB before code). Dashboard `VmPushStatus` badge + retry + needs_pick picker (`POST /admin/terminplanung/vm-push`). Worker has `DRY_RUN=1` (validate-only). NO dev VM — all pushes hit live Swiss Volley; reversible by re-pushing. `ext:deploy` does NOT sync `directus/scripts/` — worker rsynced to each container's scripts dir separately. i18n ×5. Dev + prod (ext deploy + worker rsync both; frontend merge; migration already applied both). See `vm-write-api` memory.
- **2026-06-10** Spielplanung embedded mailbox (no app-version bump; **LIVE dev + prod** — release `e640bea9`, migration 100 both envs, `SCHEDULING_IMAP_PASSWORD` from vault (`Migadu - volleyball@spielplanung.kscw.ch`, no folder) added to both `.env`s and both containers **recreated** (env-file changes need recreate, not `docker restart`). Verified e2e on both: ~175 messages synced in <12s, detail + read-marking, reply sent via SES + appended to Migadu Sent + threaded, `mailbox_sync` cron heartbeat ok. Known quirk: SES rewrites our outgoing Message-ID in transit — harmless (opponent-side threading uses THEIR ids; the UI threads by address), but a self-addressed mail shows as 2 rows): the Terminplanung dashboard gets a real mail client for `volleyball@spielplanung.kscw.ch` instead of switching to Migadu webmail (iframe-embedding webmail was ruled out: `X-Frame-Options: DENY`). New `kscw-endpoints/src/scheduling-mailbox.js` syncs INBOX + Sent over IMAP (`imapflow` + `mailparser`, installed INSIDE the extension folder — fresh checkouts must `npm install` there) into `scheduling_emails` (migration **100**, endpoint-gated like derbies, deduped by Message-ID); kscw-hooks cron every 10 min (`sync_runs` source `mailbox_sync`, dormant without the env var). Replies compose raw MIME (own Message-ID + In-Reply-To/References for threading), send via the existing SES SMTP (DKIM-aligned), append the same bytes to Migadu's Sent folder, and log as `direction='out'`. Attachment bytes never stored — streamed on demand from IMAP (410 + re-sync if the UID moved). Frontend: `MailboxPanel` card on the admin dashboard (unread badge, message list, sandboxed-iframe HTML view, reply/compose) + per-opponent "N emails" thread button on each opponent card — matching is computed client-side by address intersection with `contact_email` (no FK, nothing stale). i18n ×5. Env vars + ops documented in `INFRA.md → Spielplanung mailbox`.
- **2026-06-10** Terminplanung sync button now refreshes fixtures + team names in one click (no app-version bump, frontend only): the invites-panel button (relabelled "Sync SVRZ now" → "Sync now", i18n ×5) fires `POST /admin/terminplanung/svrz-sync` (SVRZ fixtures/contacts) AND `POST /admin/vm-sync` (VM team names/leagues — e.g. a junior team's Stärkeklasse rename DU23-1 → DU23-2). The VM call is admin-only + best-effort: a non-admin spielplaner gets 403 there, swallowed, so the SVRZ sync still starts. So when VM updates a name, one click in Terminplanung makes it flow through (VM owns `teams.name`; the scheduler matches by id so it never breaks). Heads-up: every click now also runs the heavier full-roster VM scrape (the VM cron is weekly for that reason). Reuses the existing `/admin/vm-sync` endpoint (mints its own cron-admin token, guards concurrency) — no backend change. Dev + prod (frontend push).
- **2026-06-10** Games list/detail show OUR team name from VM, not the SV caption (no app-version bump, frontend only): `GameCard` + `GameDetailModal` now render the KSCW side of a matchup from the linked `kscw_team.full_name` (VM-owned `teams.name`), falling back to the stored `games.home_team`/`away_team` string when `kscw_team` isn't expanded. So when VM renames a team ahead of the SV feed (e.g. a junior team's Stärkeklasse move DU23-1 → DU23-2), the two primary game surfaces mirror VM immediately instead of waiting for the SV caption to catch up. Opponent side keeps the SV caption (correct — it's how the league names them). Scoped deliberately to those two surfaces: the SV national API (`api.volleyball.ch`) already tracks VM, so the ~13 other render sites (home rows, calendar, scorer, hallenplan, referee expenses) + the `utils/teamColors.ts` hardcode + the standings↔games string-join self-heal on the next sync; a full uniform rename would touch all of them and carry rankings-join regression surface (deferred). The `sv-sync.js` `SV_TEAM_IDS` value labels were already dead code (only the keys are read, for KSCW detection); the team link is already ID-driven (`team_id vb_<staticTeamIdentifier>`). Dev + prod (frontend push).
- **2026-06-10** Scheduling: match KSCW team → SVRZ fixtures by `staticTeamIdentifier`, not name (no app-version bump): the scheduling tool resolved a team's fixtures/opponents by string-matching `"KSC Wiedikon " + teams.name` against the SVRZ feed labels. That silently breaks (0 opponents) when VM — the source of truth for `teams.name` — renames a team ahead of the SVRZ feed: a junior team changing Stärkeklasse becomes e.g. **DU23-2** in VM while the SVRZ feed still says **DU23-1** (live case: active DU23 = `team_id vb_2301`, feed labels all "KSC Wiedikon DU23-1" for season 2026). Both VM and SVRZ key the team by the SAME stable `staticTeamIdentifier`, which we already store as `teams.team_id` (`vb_<id>`). All three matchers (`import-from-svrz`, `resolveSyncedOpponents`, derby panel) now compare that id (read from `svrz_games.raw.encounter.team{Home,Away}.staticTeamIdentifier`) against `teams.team_id`, **falling back to the name** when a fixture's raw lacks the id. VM can now own the display name without breaking fixture resolution; also retires the old "U23 → KSC Wiedikon 1" name-drift caveat for every team. Verified read-only on dev: id `2301` uniquely isolates the 15 DU23 fixtures; every KSCW team's feed `staticTeamIdentifier` matches its `team_id`. Note: the member-app games list (`sv-sync.js` hardcoded `2301→DU23-1` map) is separate and still name-pinned. Dev + prod (ext deploy both).
- **2026-06-09** Scheduling invite "sent" tracking (no app-version bump): auto-created SVRZ invites sit at status `invited` *before* any email goes out, so the list showed "Invited" for opponents nobody had contacted. Migration **099** adds `game_scheduling_opponents.email_sent_at` (fields:['*'] → no perm change); the bulk **send** endpoint and the per-card **Draft email** mailto (new `POST /admin/terminplanung/invites/:id/mark-sent` + `markSent` hook) stamp it. `InviteRow` badge now shows **"Not sent"** (muted `outline`) until emailed, **"Invited"** (gold) after; never downgrades the lifecycle status (viewed/booked/revoked unchanged). `SendInvitesModal`/`InvitesDrawer` refetch after send so badges flip. **Also hardened `ensure-from-svrz`**: dedupe now runs against ALL existing opponent rows (any status), not just active ones — so a refresh never re-mints a link for an opponent that already has one, and a deliberately **revoked** invite is no longer silently resurrected with a fresh token (use Reissue to bring one back; same row, new token). Answers "why do invites regenerate on refresh / different links" — they don't: a given opponent keeps its token forever unless you Reissue. i18n `statusNotSent` ×5. Dev + prod (migration 099 + ext deploy + frontend merge, all both envs).
- **2026-06-09** Scheduling dashboard polish (no app-version bump): (1) **Manual booking** — new `POST /admin/terminplanung/manual-booking` lets the spielplaner record an already-agreed matchup directly (home leg books/reuses a slot, away leg stores a confirmed date), skipping the opponent propose/choose flow; no emails (agreement already happened), overwrite frees the prior slot, permissive on the soft guards (only blocks stealing another opponent's booked slot). New `ManualBookingForm` per opponent card. (2) **Games resurfaced per opponent** — each dashboard opponent card shows a "N games" button → modal with the SVRZ fixtures still to schedule (reuses `svrz-clubs`, matched by team name). (3) **Season overview calendar limited to Sep–Mar** (drop Apr/May, nav clamped to boundary months via `minMonth`/`maxMonth`). (4) **Toast on saving game Saturdays** (`SpielsamstageEditor`). (5) **Invites list → card grid** (`InviteRow` is now a card; `InvitesPanel` renders a 1/2-col grid instead of a cramped table). i18n `manual*`/`spielsamstageSaved` ×5 (renamed `manualHint`→`manualBookingHint` to dodge an existing key collision). Dev + prod (frontend push + ext deploy both). Lesson: gate frontend with `npm run build` (`tsc -b`), not `tsc --noEmit` — the latter missed the duplicate-key error.
- **2026-06-09** Directus admin fix — `spielplaner_assignments` o2m "relationship not configured" (no app-version bump, backend/admin only): the **Spielplaner Assignments** field (`members`/`teams` o2m) showed "The relationship is not configured properly or you don't have permission" **even for a full admin**, surviving hard-refresh/incognito/restart. Root cause: **duplicate foreign keys** on `member`/`kscw_team` — migration 031 created `*_fkey`, then UI-registration added `*_foreign` on the same columns; two FKs per column made Directus schema introspection unable to bind `directus_relations` → the o2m relations dropped from the live schema. Migration **097** drops the `*_fkey` duplicates (keeps the Directus-managed `*_foreign`); migration **098** restores `user_created` as a clean single-FK m2o to `directus_users` + unhides `date_created`/`user_created` + adds readable o2m display templates (assignments show who/when, not raw UUIDs). Both applied dev + prod with container restarts. **Never repair these relations via the admin UI when a DB FK already exists — the UI adds a 2nd FK and reintroduces the bug.** Diagnosis gotcha: `/relations/:collection/:field` returns FORBIDDEN for o2m **alias** fields even when healthy (only resolves real m2o columns) — reload the item page, don't trust that endpoint. Documented in `INFRA.md → Troubleshooting`.
- **2026-06-09** Season archive/rollover hardening (no app-version bump; deep-audit fixes): a full audit of how season archive/rollover is handled across teams/member_teams/games/other tables + frontend. Live prod data was clean (no coach-as-member, no duplicate rosters; the DU23-1/DU23-2 merge was intentional). Code fixes: (1) **`useAuth` now intersects coach/TR/spielplaner team-id lists with ACTIVE teams** — those junctions have no season and are cloned-not-moved on rollover, so stale archived-team ids leaked into every coach-scoped view (Trainings auto-select, Games dashboard, Home filters); root-cause fix. (2) **GamesPage `teamNameToId`** built from active teams only (post-rollover dup names couldn't resolve to the archived id → empty games). (3) **Rollover** now carries forward `fine_rules` + `spielplaner_assignments` + future `scheduling_blocks`, re-points future `source='manual'` games (synced re-sync on their own; `kscw_team` added to sv/bp `COMPARE_FIELDS`), expires pending `team_requests` on archived teams, archives the dead season's team chats, stamps `date_created`, stringifies `recruiting_positions`. (4) **`restore-season`** refuses double-activation when a newer active season exists (`{force:true}` override) + un-archives chats; **`archive-season`** archives chats. (5) **LEADER write perms** (teams/games/trainings/fines/fine_rules/scheduling_blocks) gated on `team.active` (reads open for history). (6) ProfileEditModal jersey check + admin dataHealthChecks now season-scoped via `getCurrentSeason()`; iCal sport feed filters active teams. Migrations **095** (games-notify silencer GUC) + **096** (`team_requests.team` ON DELETE CASCADE). Dev + prod (migrations + setup-perms + ext deploy + frontend merge, all both envs). **Plus policy consolidation (same day):** retired the legacy un-managed `KSCW Coach` policy (from old migrations 026/034/036/042) that was still attached to the Team Responsible role and **additively shadowed** the managed `KSCW Team Responsible` scoping (mine + the 2026-05-12 audit). Of its 46 unique grants, 26 were already covered by the Member policy (coaches hold it too); the other 20 were folded into `setup-permissions.mjs` under Team Responsible (filters verbatim except `participations.delete`, tightened from fully-open to the same scope as `participations.update`). Then `deleteLegacyPolicy('KSCW Coach')` detaches + drops it; `attachPolicyToRole` made idempotent (stops role-access row accretion). Verified zero capability change: effective (collection,action) set for the Team Responsible role is **154 → 154, 0 lost / 0 gained** on both dev and prod, `coach_policies=0`, and every leader write on teams/games/trainings/fines/fine_rules/scheduling_blocks is now active-gated with no shadow. Now **one** leader policy serving both coaches and team-responsibles. setup-perms 469/0 both envs. **Then junction-write tightening (same day):** the formerly fully-open writes the legacy policy carried are now scoped — `teams_coaches`/`teams_responsibles` `update`+`delete` to junctions whose team the caller coaches/is-TR-for (`JUNCTION_OF_TEAM_I_LEAD`; `create` stays unfiltered per Directus's not-yet-existing-row limitation, like `teams_sponsors`), and `carpools.delete` to `OWN_DRIVER` (matching how Member already scopes carpool create/update). Effective set still **154 → 154** on both envs; only filters tightened. `SCHEMA.sql` baseline regenerated from prod (`db:baseline:prod`) to include migrations 095–098.
- **2026-06-09** Bug fixes (no app-version bump, frontend only): (1) **Team absences stuck on "Loading…"** — `useTeamAbsences` derives `isLoading` from `loadedKey !== requestedKey`, but every fetch's `finally` unconditionally wrote `loadedKey`. Switching from "all teams" (slow, large query) to a single team (fast) let the stale all-teams response resolve last and clobber the key → loading never cleared, no console error, no new fetch. Added a `latestKeyRef` guard so only the latest in-flight request applies results + `loadedKey`; same guard applied to sibling `useTeamMembers`/`useMultiTeamMembers`. (2) **Calendar all-day/closure label** now wraps (`break-words` + `title` tooltip) instead of `truncate`-clipping to one line — long titles like "KWI Turnhalle für Handballturnier Pfadi Globus" were cut off in the day cell (`MonthGrid`). Dev + prod (frontend push).
- **2026-06-09** Juniors — pick-3 Sunday gating (no app-version bump, frontend only): `HomeProposalForm` pick pool now enforces the full priority via the 3 picks — picks 1 & 2 = strict pool (own slot / Spielsamstag / Döltschi, non-Sunday); **pick 3 offers non-Sunday slots first, and only when none remain falls back to Spielsamstag-weekend Sundays, then other Sundays**. So a junior opponent is only ever offered a Sunday when no Saturday/other slot is available, and only as the 3rd pick — matching "start using Sundays only when Saturdays and other slots are not available". Builds on the backend strict/preferred flags below. Dev + prod (frontend push).
- **2026-06-09** Juniors — Sundays last-resort for home games (no app-version bump): for Under teams, home Sunday slots (`source=spielsonntag`) are now `strict=false` in `slots/:token`, so the opponent proposal form (which restricts picks 1 & 2 to strict slots, pick 3 to any) only allows a Sunday as the **3rd** pick — picks 1 & 2 must be the own slot / Spielsamstag / Döltschi (non-Sunday). `propose-home` rejects a Sunday in picks 1 & 2 for juniors (server guard). The Sunday `preferred` steer now also flags Sundays on a **Spielsamstag weekend** (priority: Spielsamstag-weekend Sundays before standalone Sundays), on top of the existing shared-junior-Sunday clustering. Non-juniors + away proposals unchanged. Verified on dev (junior feed: all 207 Sunday slots strict=false, non-Sundays strict=true). Note: the calendar is chronological, so slot>Spielsamstag>Döltschi among the non-Sunday picks is not hard-ordered (all offered for picks 1 & 2); only the Sunday deprioritisation is enforced. Dev + prod (ext deploy both).
- **2026-06-09** Scheduling invites auto-create (no app-version bump): the invite list now **populates itself** — selecting a team fires `POST /admin/terminplanung/invites/ensure-from-svrz`, which mints a tokenized invite for every synced opponent that has a contact email and isn't already invited (idempotent, deduped by opponent name; opponents without a contact are skipped). No manual "Create invites" step; emailing stays a separate explicit action (toast "{{count}} invites added automatically"). Extracted `resolveSyncedOpponents()` shared by `svrz-clubs` + `ensure-from-svrz`; the drawer auto-fill now skips already-invited opponents (so it only surfaces ones still needing a contact). Verified on dev: team 94 → 11 created, re-run → 0. i18n ×5. Dev + prod (frontend push + ext deploy both).
- **2026-06-09** Scheduling invite contacts auto-fill (no app-version bump): the Invites drawer now **auto-fills opponents + contacts + games on open** from the already-synced SVRZ data (`svrz_games` + bulk `svrz_spielplaner_contacts`, populated by "Sync SVRZ now" / the nightly cron) — instant, no live login, no re-importing each time. `svrz-clubs` now (a) returns each club's games (feeds the "N games" modal), (b) **skips intra-club fixtures** (H1 vs H3 no longer self-invites), and (c) **matches contacts by season START YEAR (`2026%`) not `season_uuid`** — `game_scheduling_seasons.svrz_season_uuid` (8f94f449) differed from the uuid the bulk feed synced under (dcafddfe), so the old uuid match returned 1/27 opponents; now 26/27 resolve. `loadLeagueClubs` joins ALL of a club's contacts (was: only when exactly one). The slow live per-game fetch stays as an optional "Refresh from SVRZ (live)"; redundant "Add from league" button removed. (The two syncs: "Sync SVRZ now" = the heavy scraper that pulls games + every club's contacts into the DB, nightly; "Refresh from SVRZ (live)" = optional per-game live re-fetch for the freshest contacts.) i18n ×5. Dev + prod (frontend push + ext deploy both).
- **2026-06-09** Scheduling invite email send + preview + polish (no app-version bump): (1) **Bulk-send invite emails per team with preview+confirm** — new "Email invites" action on the Invites panel app-sends the tokenized invite to all of a team's opponents from the `volleyball@spielplanung.kscw.ch` identity (not mailto). `SendInvitesModal` doubles as the confirmation: `POST /admin/terminplanung/invites/send { ids, dry_run, season_name, kscw_team_name, kscw_league }` with `dry_run:true` returns rendered previews (subject + recipients + sandboxed HTML iframe, byte-identical to send) → admin reviews each via a recipient `<select>` → "Send N emails". New bilingual DE+EN `inviteEmail()` in `terminplanung-emails.js` (club hasn't picked a language yet), branded via `buildEmailLayout` + CTA; `parseRecipients()` splits multi-contact addresses; link base from env-aware `FRONTEND_URL`. i18n ×5. (2) **Generic greeting** "Hallo / Hello," (no recipient name, since contact_email may list several contacts). (3) **Fixed 30.06.2026 invite expiry** (season VolleyManager cutoff) replacing the rolling 90-day TTL; migration **094** backfills existing open invites. (4) **"N games" → modal** — clicking the game count on an invite draft row opens a Dialog with the full game list (date + home/away match) in a `<Table>`, replacing a hover-only `title` tooltip. Dev + prod (frontend push + ext deploy both + migration 094 both).
- **2026-06-09** Scheduling invite contacts fixes (no app-version bump): (1) **Invite drafts reset on team switch** — the `InvitesDrawer` stays mounted while the panel switches teams, so D1's imported SVRZ contacts lingered into D2's drawer until a fresh import; added a `useEffect` keyed on `kscwTeam.id` to clear staged drafts + CSV box, so each team gets its own contacts. (2) **`POST /admin/terminplanung/invites` 500 fixed** — "value too long for type character varying(255)": the SVRZ import comma-joins ALL of a club's Spielplan contacts into one shared-invite `contact_email`/`contact_name`, and clubs like Wädivolley H2 (6+ contacts) overflowed varchar(255); migration **093** ALTERs both columns to `text` (idempotent, fields:['*'] → no perm change), `setup-schema.mjs` uses `text()` for fresh installs. `parseRecipients()` already split them for sending, so width was the only breakage. Dev + prod (frontend push + migration 093 both; SCHEMA.sql baseline regen deferred to next schema:pull).
- **2026-06-09** Scheduling go-live polish (no app-version bump): (1) confirm errors now surface as a toast in the admin dashboard (`handleConfirmHome/Away` wrap + rethrow; the review components swallow the already-toasted rejection) — was failing silently; `confirm-home` rejection reasons (Saturday cap, cross-team, gap, Döltschi, slot taken, hall closure) now visible. (2) `homeProposalHealth` pre-flags extended with `saturday_cap` + `cross_team` (reasons ×5 i18n). (3) **Cross-team same-day rule now counts COACHES + team-responsibles**, not just shared players — `sharedPlayerTeams` rewritten to union `member_teams`(guest 0/null) + `teams_coaches` + `teams_responsibles` both directions (affects offer/confirm/health). (4) Season overview calendar gets a multi-select **team filter** (empty = all; filters chips + open-slot counts). (5) Slot-config preview: Spielsamstag collapsed to a single label, Döltschi merged to one per-day entry (union window), equal-width Slot/Saturday buttons. Dev + prod (ext deploy both + frontend push).
- **2026-06-09** v4.27.0 — Scheduling remarks: per-opponent `kscw_note` (spielplaner→opponent, shown on their proposal page) + `opponent_note` (opponent→spielplaner, shown in dashboard) on `game_scheduling_opponents` (migration **092**, fields:['*'] → no perm change). Endpoints `POST /terminplanung/note/:token` (token-gated, independent of proposing) + `POST /admin/terminplanung/opponent-note`; slots/:token returns both. New `OpponentNotes` dashboard component + opponent-page gold "Note from KSCW" banner + "Your remarks" box. i18n ×5. Dev + prod (migration 092 + ext deploy both).
- **2026-06-08** v4.26.0 — Home-proposal invalidation + semi-auto re-request + Döltschi date-rule + calendar polish: pending home proposals are re-validated live (`GET /admin/terminplanung/proposal-health`, mirrors confirm-home guards: taken/event/block/closure/too-close/derby/Döltschi cap+date) — dead picks struck out, and when all 3 die the admin gets a confirm-gated "Email opponent for 3 new slots" → `POST /admin/terminplanung/request-new-slots` (race-guarded `proposals_still_valid`, deletes the dead booking, resets opponent, stamps `new_slots_requested_at`, emails them in their lang). Migration **091** adds `game_scheduling_opponents.new_slots_requested_at` (fields:['*'] → no perm change). New `home_reproposal_request` email kind ×5 (layout now does CTA buttons). **Döltschi = 1 slot per date** (time + hall 1/2 irrelevant; was per date+time) across offer/confirm/health/preview. Calendar: Spielsamstag gold + label, hall closures red bg (`CalendarGrid` opt-in `highlightClassName`/`highlightLabel`). i18n ×5. Verified e2e on dev; shipped dev + prod (migration 091 + ext deploy both). gcal also run on prod (24 closures, 23 trainings auto-cancelled).
- **2026-06-08** gcal closures + home-slot blocking + Spielplanung calendar (no app-version bump): `gcal-sync.js` rewritten to import only genuine closures (geschlossen/Turnier/Volleynight/Pfadi/ASVZ/Handball/Reserviert/extern), excluding the club's own VB/BB games + trainings, writing per-KWI-hall `hall_closures` via `ItemsService` (so the migration-028 training auto-cancel fires; defers to school holidays). Replaced an earlier "every event" version before its cron created ~285 bad closures. `game-scheduling.js`: hall closures (gcal OR school_holidays) now block **HOME** game slots whose hall is closed (slots/:token whereNotExists + confirm-home guard) — **away proposals untouched** (away games are elsewhere). New read-only **season overview calendar** on the Terminplanung dashboard (`SchedulingCalendar.tsx` — confirmed/proposed home+away, blocked, open-slot counts, Sep→May strip). Dev + prod (ext deploy both, frontend push); verified on dev (24 genuine closures, 22 trainings auto-cancelled).
- **2026-06-08** Döltschi scheduling rules (no app-version bump — admin/opponent flow, not member-app-visible): junior (Under) teams now always get the shared VB Döltschi pool (halls Döltschi 1+2) even when it's not their own slot, PLUS the Spielhalle pool (both) — `generate-slots` per-slot source tags. Döltschi capped at **10 games/season club-wide** + **no two simultaneous** (1+2 = one venue → one game per date+time): enforced in `slots/:token` (offer filter + dedupe) and the `confirm-home` txn guard. Setup-page slot preview (`TeamSlotConfigPanel.resolveStandard`) updated to mirror it (junior pool+Spielhalle, Döltschi 1+2 deduped by day+time). **DU20 removed from Terminplanung** (added to `SCHEDULING_EXCLUDED_TEAM_NAMES` + mirrored backend `generate-slots` skip). Dev + prod (ext deploy + frontend push).
- **2026-06-08** v4.25.0 — Intra-club derby anchoring (Art. 27 SVRZ): when two KSCW teams share a league group (e.g. H1 & H3 in 2L) their head-to-head games must be the first of the Vor- and Rückrunde (else home-team forfait). Migration 090 `game_scheduling_derbies` (knex-only, endpoint-gated — no item perms) + admin `DerbyPanel` (detects all-KSCW fixtures in the SVRZ feed, shows the round they're currently filed under, spielplaner fixes the two dates; confirm requires one per half). `game-scheduling.js` then clamps home-slot offers + away calendar + away-proposal check + gap for BOTH teams to after the relevant derby date per half. Vor-/Rückrunde split = 01.01 of season's 2nd year (derived). Inert until a derby is confirmed. i18n ×5 + `away_before_derby`. Dev + prod (migration 090 + ext deploy both).
- **2026-06-07** v4.23.0 — Leave a team from the Teams page: `TeamRequestModal` is now a combined "Manage teams" modal (join-request + per-team leave w/ inline confirm), button relabelled `manageTeams`; new `useAuth().refreshTeamContext()` updates team cards/counts after a leave without reload (also wired into ProfilePage's inline leave, which stays the sole leave UI there via `showLeave={false}`); i18n `manageTeams`/`manageTeamsTitle` ×5. Team-settings accordion now defaults open (`SettingsGroup` `defaultOpen=true`; Game/Training Defaults were collapsed) + `FinesSettings` opens by default. **Fixed `fine_rules` Member read filter** (`teams.member_teams` → `teams.members` o2m alias) — the bad relation produced `Invalid query` that broke `fine_rules` reads on home + roster editor for everyone (surfaced as DU23-1 roster-editor errors when a coach went to change the team picture). Shipped to dev + prod (frontend push + `db:setup-perms` on both; no new migration).
- **2026-06-03** v4.19.0 — Volleymanager is now the source of truth for volleyball team names/leagues + referee licences. `vm-sync-check.mjs` (weekly `0 4 * * 1`, was monthly) now writes `teams.name/full_name/league` (match `team_id`=`vb_<staticTeamIdentifier>`, current-season, **update-only** — app fields untouched; fixes stale rollover clones incl. the D1/D2 division swap) + fetches `clubreferee` to flag `members.referee_vb` and store managing assoc in `sv_vm_check.referee_assoc` (migration **081**: `is_referee`+`referee_assoc`). Referee-only non-players still get an `sv_vm_check` row so their flag syncs. New per-scraper "Run now" buttons on `/status` (`POST /kscw/admin/vm-sync` + `/svrz-sync` — fire-and-forget, mint token from `DIRECTUS_SYNC_EMAIL/PASSWORD` like the cron, record `sync_runs` on child exit; SV/BP/GCal stay synchronous). `kscw-website` (pushed to **dev** branch): VB team defs keyed by live team **name** (season-robust, swap-aware — `team_id` unusable since HU20/MiniVB have none), `displayName` via `expandDisplayName`, nav (desktop+mobile) + `[slug]` routing from live `getActiveTeams`, detail chip follows live name; **fixed latent bug** — `getActiveTeams` requested a non-existent `slug` field → `FORBIDDEN` → silent static-fallback (stale leagues everywhere); `getBadgeText` renders terse `2L`→`2. Liga`. Deployed surgically to dev + prod (migration 081, script + kscw-hooks/kscw-endpoints rsynced — NOT `ext:deploy`, to avoid sweeping parallel `game-scheduling.js`/`setup-permissions.mjs` edits); sync run on both. SCHEMA.sql baseline NOT regenerated — do on next `schema:pull`.
- **2026-05-30** Public site no longer leaks team-scoped events. New `GET /kscw/public/events` (kscw-endpoints `public-events.js`) returns only club-wide events — `whereNotIn` on `events_teams` + `events_members` server-side, since the public Directus policy can't read those junctions (migration 035). `ical-feed.js` events source got the same filter. kscw-website `events.ts` switched from `/items/events` to the endpoint. Fixes team-only events (e.g. the H3-scoped Rämi Turnier #21 + Wetzikon #22) appearing on the public club calendar + subscribed iCals. Deployed surgically (3 files) to dev + prod; verified both exclude #21/#22. No app version bump (public-site/backend only, not member-app-visible).
- **2026-05-18** v4.12.0 — Cancelled trainings now notify the team: kscw-hooks `trainings.items.update` → new `notifyTrainingCancelled()` (future-only, `payload.cancelled===true`) inserts `notifications` rows (`type/title:'training_cancelled'`, body `{date}` dd.mm.yyyy) for `member_teams` + `teams_coaches` + `teams_responsibles` (wiedisync_active) and fans a per-locale web push via new `trainingCancelled.title/body` ×5 in `push-i18n.js`. `TrainingDetailModal`: Cancel/Contact moved from `Modal` `headerAction` (overlapped title on `sm`) to a bottom `flex-wrap` action row. Batched in the same 4.12.0 release: `trainings.recruiting_positions` (migration 060 + 056-trigger fix-forward, types, i18n ×5) shown on the public team page, and barrage standings surfaced in `/public/team/:id`. Dev: migration 060 applied, ext deployed (kscw-hooks/kscw-endpoints), healthy. Prod pending.
- **2026-05-18** v4.11.0 — Email made mandatory after a member wiped their own via Profile edit: `email` input now `required` + trimmed JS guard in `ProfileEditModal` (rejects empty/whitespace/bad format, new `requiredProfileFields`/`invalidEmail` i18n ×5); migration 059 trigger `trg_members_prevent_email_blanking` (BEFORE UPDATE OF email) refuses set→blank server-side. Restored prod member 61 Thamayanth Kanagalingam → `thamayanth.kanagalingam@uzh.ch`. New `hide_email` privacy flag mirroring `hide_phone`: migration 058 (column + `directus_fields`), `setup-schema.mjs`, `setup-permissions.mjs` (MEMBER_VISIBLE/EDITABLE), kscw-hooks `members.items.read` privacy filter nulls `email` for non-self/non-admin, gated in MemberRow/PlayerProfile/AssignmentEditor/GameDetailModal + "Hidden" badge on ProfilePage, `hideEmail`/`hideEmailHint` i18n ×5. ClubDesk change-detector normalizes (trim) so empty↔undefined no longer emits a phantom diff. Shipped to prod: migrations 057/058/059, setup-perms, kscw-hooks, frontend; SCHEMA.sql baseline regenerated; trigger functionally verified on prod; Thamy #61 `hide_email` enabled on request.
- **2026-05-12** v4.8.9 — Participation modal: coaches attached only via `teams_coaches` (no `member_teams` row, e.g. Michelle Howald on H2/H3) now appear in the staff section even before they RSVP — staff-resolution effect seeds from `leadershipRoles` (coach + TR member IDs) on top of the existing `is_staff=true` participation rows. Removed nested `max-h-[60vh] overflow-y-auto` wrapper inside the modal so the member list no longer scrolls inside the sheet's scroll on mobile.
- **2026-05-12** v4.8.0 — Auto-confirm RSVP (PlayerPlus-style opt-out attendance). Two new team settings (`training_auto_confirm`, `game_auto_confirm` in `teams.features_enabled` JSON, both off by default). When on, new trainings/games auto-insert `confirmed` participations for all eligible members; members must actively decline. Hook in `kscw-hooks` `action('trainings.items.create')` / `action('games.items.create')` runs as a second pass after the existing absence-auto-decline INSERT — `NOT EXISTS` skip protects absence-declined rows and manual overrides. Trainings honour `excluded_guest_levels`; games include only `guest_level = 0` (guest-block trigger still applies). Out of scope: per-activity override, retroactive flip on toggle, events.
- **2026-05-10** v4.6 (4.6.0 → 4.6.7) — Roster export (CSV / PNG / PDF) gated by `canEditRoster`, with `TRAINING` / `GAME` / `EVENT` activity-kind header (games override `"<home> vs <away>"`), position-summary pill row, dark-mode-independent printable view portalled to body inside zero-size overflow:hidden wrapper. Mobile bottom-sheet scroll restructured (outer animated wrapper + inner scroll node), drag-down-to-dismiss on `MoreSheet` matches Vaul detail modals, full-width close strip + chevron button. Roster summary count corrected: dedupe by member with status priority + drop is_staff rows + memberIdSet filter (mirrors `ParticipationSummary`); excluded guests no longer leak in; player-coach `is_staff` rows no longer double-counted; `getMemberStatus` prefers non-staff row; "Coach present" walks full participations list via Set. Explicit RSVP `auto_declined_by==null` wins over absence overlay across `getMemberStatus`/badge/`statusLabelText`/server filter. `/status` page reads real cron heartbeats from migration-045 `sync_runs` table; new `logCronRun()` helper in `error-log.js` upserts via `onConflict('source').merge()`; sv/bp/vm/svrz crons + new gcal_sync cron at 04:00 UTC record heartbeat on every termination path; `GET /kscw/admin/sync-status` returns rows with `age_seconds`; `useInfraHealth` aggregates Swiss Volley as `sv_sync` ∪ `svrz_sync`, distinguishes stale from errored, shows "Awaiting first run" before first cycle. Localhost ALWAYS uses dev Directus regardless of `VITE_DIRECTUS_URL` (prod CORS rejects localhost); new `npm run dev:prod` reverse-proxies all `/directus/*` (REST + WS) to prod Directus when devs need live data, with red console banner on every page load.
- **2026-05-10** v4.5.4 — Roster modal hides excluded guests. Commit `af71850` (per-training `excluded_guest_levels`) hid the guest's input buttons + 403'd `participations.create`, but never filtered the staff-side `ParticipationRosterModal` — excluded guests sat as zombie "Hasn't replied yet" rows, inflating the count. Modal now takes `excludedGuestLevels` prop (wired from `TrainingDetailModal` + `TrainingsPage`) and drops members whose `member_teams.guest_level` is excluded. Also drops `guest_level > 0` for `activityType === 'game'` (hard rule from same commit).
- **2026-05-07** v4.5.3 — `member_teams` duplicate guard. Migration 044 adds `UNIQUE (member, team)` after dedupe (5 prod rows: Hanna Baumgartner D4, Isis Hemprich D1, Maëlle Leiser DU23-2, Livia Schlegel D4, Daniela Duc D4 — surfaced when admin saw Hanna twice on the D4 roster; 1 dev row: Pawel Kalaga H1). Pre-existing race: three frontend insertion sites (`RosterEditor.handleAdd`, `TeamDetail.handleApprove`, `TeamDetail.handleApproveRequest`) had no `(member, team)` pre-check, so a double-tap or refetch race produced twins. All three now look up first and no-op (or update `guest_level` in the request flow); the SQL constraint is the hard backstop. New REST script `dedupe-member-teams.mjs` (dry-run by default; `--apply` deletes, keeping lowest id, preferring higher `guest_level` on tie). Migration self-checks for duplicates and refuses to apply until cleaned up.
- **2026-05-05** v4.5.0 — Coach Dashboard expansion + game/event card parity. New /games dashboard tab (single-team, league-only toggle, per-row drilldown). Replaced season selector with team-scoped From/To range persisted on `teams.dashboard_range_from/_to/_league_only`. Bucket simplification: confirmed RSVP wins over absence; weekly + one-off both count as absent (was: excused). GameCard gains note input + respond-by + roster opener + edit/delete (delete only for manual games). EventCard note input always-visible. Migration 041 guards new fields by leaving them out of `PUBLIC_TEAM_FIELDS`.
- **2026-05-03** v4.4.14 — Home agenda row strip + "Coach present" badge. v4.4.12's composite-key fix only patched `useBulkParticipationStatuses`'s input filter and internal `partByKey`; the *output* `Map` still keyed by `activity.id` alone. `NextAppointments` passes mixed activity types — events 1-4 (confirmed) overwrote trainings 1-4 (declined Mondays for member 8) since the loop's `map.set(activity.id, …)` lets the last write win. Output map now keyed by composite `${type}:${id}` plus a `getStatus(type, id)` accessor; 7 call sites in HomePage updated. Also: "Coach present" missing on most trainings — `team.*` doesn't expand M2M relations, so `team.coach` and `team.team_responsible` were `undefined` and `teamCoachIds()` returned only the captain. Added `team.coach.members_id`, `team.team_responsible.members_id` to trainings fetch and equivalent `kscw_team.*` to all four games fetches.
- **2026-05-03** v4.4.13 — SVRZ daily sync (`svrz_sync` cron) was failing every morning at 04:30 with a misleading "CSRF token extraction failed" error. Real cause: every indoor SVRZ page except games returns 403 Forbidden until the session enters the volleyball sub-app (`GET /sportmanager.volleyball/main/dashboard`) — a step the browser does invisibly after login. `csrfFromPage()` only checked the regex, so the 403 page surfaced as a CSRF error. Added the sub-app context-enter step to `vmLogin()` and made `csrfFromPage()` report HTTP status on failure. Same fix protects the monthly licence sync.
- **2026-05-03** v4.4.12 — Training row strip now matches actual RSVP status. `useBulkParticipationStatuses` keyed its lookup map by `activity_id` alone with no `activity_type` filter on the Directus query — so a member with `training:4 declined` AND `event:4 confirmed` saw the second row overwrite the first, painting the training row green. Both the filter and the JS `Map` are now keyed on composite `${type}:${id}`. Also capitalized the relative-time sub-label in the roster modal (`Intl.RelativeTimeFormat` emits lowercase).
- **2026-05-03** v4.4.11 — Email i18n parity with push. `bucketEmailsByLocale` expanded to 5 buckets (de/gsw/en/fr/it) + `members.email` fallback so admin aliases without a Directus user resolve. Translated registration (vb/bb/passive + admin notif), ClubDesk update, contact form, event invite, team-join request, and `buildBroadcastEmail`. OWNER_EMAIL CC pattern in registration replaced with a separate localized send in the registering user's locale (was producing duplicate German copies via the kontakt@kscw.ch forwarding alias even when the actual admin had language=english). password-reset already had 5 locales.
- **2026-05-03** v4.4.10 — Migration 038 — weekly unavailability now hard-overrides existing confirmed RSVPs. `autoDeclineForAbsence` switched from `INSERT…NOT EXISTS` to UPDATE-then-INSERT for trainings/games/events; new `participations.items.create` filter flips fresh RSVPs to `declined` if a covering absence exists. `trg_participations_clear_auto_marker` reshaped (mirrors `auto_cancelled_by_closure` pattern) so the hook can write status + marker in one UPDATE without losing the marker; manual overrides still detach via the trigger. Backfill caught 6 stuck `confirmed` rows on prod. Roster modal now uses `absenceCoversActivity()` (respects `days_of_week` + `affects`) and labels weekly type as "Unavailable" (new `declinedUnavailable` key in 5 locales) vs "Declined (Absence)" for one-off absences.
- **2026-05-03** v4.4.9 — Push notifications i18n. All cron + event-driven pushes (upcoming_activity, deadline_reminder, team-join request, scorer delegation accepted/declined, event invite, announcement fanout, DM generic fallback) were hardcoded German. New `push-i18n.js` (`bucketMembersByLocale` + `tPush` over 5 locales DE/GSW/EN/FR/IT). Announcement fanout uses its existing per-locale `translations` field. Emails unchanged (already DE/EN bucketed).
- **2026-05-02** v4.4.8 — `user_logs` RLS fixed (KSCW Member filter compared int FK to UUID `$CURRENT_USER` → `Invalid numeric value`); vm_sync cron switched from blocking 120s `execSync` to async `spawn` with 10-min budget; SDK websocket re-auth race silenced in Sentry.
- **2026-04-30** v4.4.7 — Migration 037 — junction cascade pass 2 (`events_teams`, `events_members`, `hall_events_halls`, `hall_slots_teams`, `teams_sponsors` SET NULL → CASCADE; orphan cleanup) + iOS Safari `Invalid Date` fix in `dateHelpers.ts` (bare YYYY-MM-DD anchored to T00:00:00Z; V8 parsed `2026-05-07Z` but JavaScriptCore rejected → empty weekday/date on training cards).
- **2026-04-30** v4.4.6 — Migration 036 — third-pass audit. Coach/TR `members.update` scoped to my-team members; Member + Coach reads on `event_sessions`/`events_members` scoped via parent-event filter; deduped leftover M2M-recreation permission rows.
- **2026-04-30** v4.4.5 — Migration 035 — second-pass audit. Removed public reads on `participations`/`events`/`events_teams`/`slot_claims`; scoped Member reads on `polls`/`referee_expenses`; scoped Coach reads on `participations`/`absences`; revoked `event_signups` PG grant from anon/authenticated.
- **2026-04-30** v4.4.4 — Migration 034 — grants self-scoped `spielplaner_assignments.read` to all KSCW policies. Was missing since 031, masked by wide-open reads, surfaced once 032/033 tightened them — every member's `loadTeamContext` was silently failing.
- **2026-04-29** v4.4.3 — Migration 033 — `KSCW Member` reads on `absences` / `participations` / `events` scoped (own + same-team for the first two; own + club-wide + invited-team / -member for events). Adds `members.member_teams` o2m alias.
- **2026-04-29** v4.4.2 — Migration 032 — `trainings.read` scoped to user's teams (KSCW Member rule was unfiltered) + public read removed; adds `teams.members` o2m alias.
- **2026-04-23** v4.2.0 — Spielplanung sandbox mode: manual game CRUD on calendar (create/edit/delete + bulk Excel import), scoped Spielplaner role (`spielplaner_assignments`), Week view with drag-to-reschedule on manual games (15-min snap + conflict guard), richer chips + detail drawer, season nav unclamped, SVRZ field locking.
- **2026-04-23** v4.1.0 — SVRZ game-scheduling invites: admin-issued per-verein tokenized links (3-tier contact match from SVRZ game + club feeds), manual CSV paste fallback, invite lifecycle (invited → viewed → booked) with reissue/revoke, schema extended on `game_scheduling_opponents`.
- **2026-04-20** v4.0.4 — Mobile More sheet: `/inbox` first in secondary list (was getting lost below `/events`).
- **2026-04-20** v4.0.3 — Mobile/desktop nav parity: More sheet + SidebarOptions aligned; `/status` is a friendly health dashboard; changelog justified.
- **2026-04-20** v4.0.2 — Migration 030 — 4 more `members.read` gaps (Spielplaner menu / coach scorer-assignment / shell badge / Aktiv-Passiv + Beitragskategorie display).
- **2026-04-20** v4.0.1 — Migration 029 — consent modal accept-loop (6 messaging/consent fields stripped from self-read).
- **2026-04-20** v4.0.0 — Messaging live for all members (flag flip). Milestone complete.
- **2026-04-20** v3.17.x — "Coach da" badge on list cards + home rows; hall-closure auto-cancel trainings + full absence unwind (migration 028).
- **2026-04-20** v3.16.x — Security sweep (migrations 023–027): messaging RBAC scoping, PII removal, report rate-limit, Sport Admin delete lock, coach write scoping; full i18n sweep; participation + chat fixes.
- **2026-04-20** v3.15.x — `/games`+`/trainings` single round-trip endpoint; coach policy parity (migration 020); junction cascade (migration 021); proper UTC datetime via Intl Zurich; inbox races fixed; SVRZ Art. 102a rankings colours.
- **2026-04-19** v3.12.0–3.14.0 — Messaging v1 allowlist-gated; Broadcast v1 (email+push) + Plan 02 (in-app event chat); Supabase anon/authenticated revoked on all public tables.
- **2026-04-17/18** Plans 01–05 — Messaging foundation + team chat + DMs + requests/blocks + reactions/edit-delete/reports/polls + retention/nFADP export/push/consent/settings. Vereinsnews + Daten-Explorer shipped.
- **2026-04-10 → 04-14** v3.8.0–3.9.x — React Joyride guided tours; coach/TR inline participation editing; team page load-flash fix.
- **2026-04-05 → 04-06** v3.5.0–3.7.0 — SV licence card from `sv_vm_check` (16-col VM sync); bugfix dashboard + public `/status`.
- **2026-03-29 → 03-31** v2.7–3.1 — Directus migration complete: 7 roles, 322 perms, role-sync hook, branded emails, web push, 9 PG triggers, 30+ endpoints, Sentry, SSO.
- **2026-03-19 → 03-26** v1.0–2.6 — Core platform on PocketBase: teams/members, Schreibereinsätze, Hallenplan (virtual slots), RSVP + notifications, Google OAuth, sport-scoped roles, scoreboard, scorer delegation, team settings accordion, referee expenses.
